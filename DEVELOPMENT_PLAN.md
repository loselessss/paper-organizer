# Paper Organizer 개발계획서

- 작성일: 2026-07-23
- 대상 환경: Windows 데스크톱, 개인용 로컬 애플리케이션
- 프로젝트 위치: 저장소 루트(사용자 환경에 독립적인 상대경로 사용)
- 참고 구현: 저장소에 포함된 `vendor/spdf` submodule과 기존 Organizer 설계

## 1. 프로젝트 목표

다운로드 폴더에 새로 들어온 PDF 중 **학술 논문만 식별**하고, 논문의 제목·저자·연도·저널명 또는 학회명·분야·키워드·한국어 요약·핵심 기여·한계 등을 추출한 뒤 지정한 논문 보관 폴더로 안전하게 이동한다.

각 PDF 옆에는 사람이 읽고 다른 프로그램에서도 재사용할 수 있는 JSON 메타데이터를 보관한다. 전체 라이브러리용 통합 인덱스도 자동 생성하여 제목, 저자, 연도, 분야, 키워드와 요약을 검색·필터링한다. 검색 결과의 PDF는 앱에 포함된 **sPDF 모듈로 즉시 열어** 읽기·검색·복사·OCR·주석·편집 작업까지 이어갈 수 있게 한다.

핵심 원칙은 다음과 같다.

1. 분석에 성공하고 결과가 검증된 파일만 이동한다.
2. 판단이 불확실한 파일은 자동 이동하지 않고 검토 대기 상태로 둔다.
3. 원본 PDF와 JSON 색인·수정 이력을 담은 표준 ZIP 컨테이너 `*.paperpack`을 영구적인 원본 데이터로 삼는다.
4. 통합 인덱스는 언제든 `.paperpack`들로부터 다시 만들 수 있는 파생 데이터로 취급한다.
5. 논문 본문과 분석 내용은 기본적으로 PC 밖으로 전송하지 않는다.
6. Paper Organizer와 sPDF는 한 프로그램으로 배포하되, 모듈 경계와 설정 데이터는 분리한다.

## 2. 기존 구현 검토 결과

기존 sPDF의 Paper Organizer에는 아래 기능이 이미 구현되어 있다.

- PyMuPDF로 앞 8페이지와 뒤 3페이지의 텍스트 추출
- 로컬 Ollama의 구조화 JSON 응답을 이용한 분류·요약
- `category/subcategory` 폴더 생성과 PDF 이동
- PDF별 `*.pdf.spdf.json` sidecar 저장
- 임시 파일과 `os.replace`를 이용한 메타데이터 원자적 저장
- 처리 잠금 파일, 오래된 잠금 회수, 실패 시 원본 유지
- 설정 파일에 입력 폴더, 정리 폴더, 모델, 자동 처리 여부 저장
- PyQt5 기반 카테고리 목록, 논문 목록, 상세 정보 화면
- 이동·잠금·설정·정규화에 대한 기본 단위 테스트

따라서 신규 프로젝트는 위 코드를 초기 자산으로 재사용하되 다음 한계를 보완한다.

| 기존 한계 | 신규 설계 |
|---|---|
| 입력 폴더의 모든 PDF를 논문으로 간주 | `is_academic_paper`, 판정 근거, 신뢰도를 먼저 산출 |
| 자유 형식 분야명으로 폴더가 계속 늘어날 수 있음 | 설정 가능한 고정 분류 체계와 `Uncategorized`/검토함 사용 |
| 같은 논문·이름 변경 파일을 구별하지 못함 | SHA-256, DOI, arXiv ID 기반 중복 탐지 |
| PDF별 JSON만 존재 | 재생성 가능한 `index/library.json` 추가 |
| 스캔 PDF는 즉시 실패 | OCR 대기 상태와 선택적 sPDF OCR 연동 |
| 잘못 분류된 결과의 수정 흐름 없음 | 메타데이터 편집, 재분류, 다시 분석, 이동 취소 지원 |
| 검색이 카테고리 목록 중심 | 통합 텍스트 검색과 다중 필터 추가 |
| 손상된 JSON을 조용히 건너뜀 | 오류 표시, 검증 보고서, 인덱스 재구축 기능 제공 |

## 3. 범위

### 3.1 MVP 필수 범위

- 다운로드 폴더 또는 사용자가 지정한 입력 폴더 감시
- 파일 쓰기가 완료된 PDF만 처리
- 암호화·손상 PDF 감지
- 텍스트 PDF의 내용 추출
- 학술 논문 여부 판정
- 로컬 Ollama를 이용한 메타데이터·설명 생성
- PC의 CPU·RAM·GPU·VRAM을 감지한 로컬 AI 모델 자동 추천과 수동 선택
- 설치된 Ollama 모델 확인, 호환성 경고와 짧은 성능 테스트
- 설치본과 분리된 요약 AI 모델의 최초 사용 시 다운로드·진행률·삭제 관리
- 저우선순위·단일 작업의 절전 백그라운드 처리와 리소스 자동 일시정지
- 분석 큐 선택 항목의 수동 우선 처리와 백그라운드 실행 제어
- 고정 분류 체계에 따른 대상 폴더 결정
- 파일 해시와 표지 제거 본문 지문을 함께 사용하는 중복·판본 탐지
- ResearchGate 표지 등이 붙은 동일 논문을 하나의 work로 묶기
- 확인된 중복 파일의 사용자 선택 제거·복원과 대표본 유지
- PDF를 `.paperpack`으로 원자적 보관
- 통합 `library.json` 인덱스 생성·재생성
- 제목·저자·키워드·요약 검색과 분야·연도 필터
- PDF에서 추출한 검색용 텍스트의 보존과 단순 키워드 검색
- 제목·저자·분류·요약·태그 등 인덱스 내용 수정과 변경 이력
- 사용자 수정값을 보존하는 AI 재분석 비교·병합
- 검색 결과를 내장 sPDF 모듈로 바로 열기
- sPDF의 PDF 보기, 탭, 본문 검색, 복사와 기본 OCR 기능 사용
- 처리 실패 및 검토 필요 항목 표시
- 수동으로 “지금 검색”, “분석”, “승인 후 이동”, “무시” 실행

### 3.2 후속 범위

- 스캔 논문 OCR 자동화
- sPDF에서 선택한 본문 범위만 번역하거나 요약하고 원문 페이지·좌표와 함께 표시
- DOI/arXiv ID를 이용한 외부 서지정보 보강(명시적으로 켠 경우만)
- 의미 기반 검색용 임베딩 인덱스
- BibTeX/RIS 내보내기
- 태그·읽음 상태·별점·메모·관련 논문 연결
- 폴더 외부에서 이동·삭제된 PDF의 복구 도우미
- 마지막 개발 단계의 AI 실험정보 구조화와 자연어 질의(초기 실사용 범위에서 제외)

### 3.3 초기 비범위

- 여러 PC가 동시에 수정하는 서버형 동기화
- 연구실 단위 계정·권한 관리
- Paper Organizer 전용 PDF 편집 기능의 신규 개발(번들된 sPDF의 기존 편집 기능은 사용)
- 클라우드 업로드를 전제로 한 분석
- Zotero 전체 기능 대체

## 4. 사용자 흐름

```text
다운로드 폴더에서 안정된 PDF 발견
  → PDF 유효성·암호 여부 확인
  → SHA-256 중복 확인
  → 텍스트 추출 또는 OCR 필요 판정
  → 학술 논문 여부·신뢰도 판정
      ├─ 논문 아님: 입력 폴더에 유지하고 무시 목록에 기록
      ├─ 불확실/OCR 필요: 검토 대기함에 표시
      └─ 논문 확실: 메타데이터·요약·분류 생성
  → 스키마 및 폴더명 검증
  → 목적지에 임시 `.paperpack` 생성
  → PDF·메타데이터·본문 색인·이력의 해시 검증
  → 완성된 `.paperpack`을 최종 이름으로 원자적 교체
  → 입력 PDF는 패키지 검증 성공 뒤에만 정리
  → library.json 원자적 재생성
  → UI 목록과 검색 결과 갱신
  → 논문 더블클릭 시 내장 sPDF 창/탭에서 즉시 열기
```

자동 처리는 기본적으로 보수적으로 동작한다. 예를 들어 논문 판정 신뢰도 또는 분류 신뢰도가 기준보다 낮으면 이동하지 않고 사용자의 확인을 기다린다.

## 5. 권장 기술 스택

| 역할 | 선택 | 이유 |
|---|---|---|
| 언어 | Python 3.12+ | 기존 sPDF 코드와 라이브러리 재사용 |
| GUI | PyQt5 | Paper Organizer와 기존 sPDF가 같은 Qt 런타임과 `QApplication`을 공유 |
| PDF 보기·편집 | 내장 sPDF 모듈 | 기존 보기·탭·검색·복사·OCR·주석·편집 기능 재사용 |
| PDF 처리 | PyMuPDF (`fitz`) | Organizer의 분석과 sPDF의 렌더링·편집이 같은 PDF 엔진 사용 |
| 로컬 분석 | Ollama HTTP API | 로컬 처리, JSON Schema 출력, 모델 교체 용이 |
| 선택적 클라우드 분석 | OpenAI Responses API·Anthropic Messages API | 로컬 사양과 무관한 정밀 요약 선택지, 공통 JSON Schema 사용 |
| API 키 보관 | Windows 자격 증명 저장소(`keyring`), 환경 변수 폴백 | 키를 설정 JSON·로그·Git과 분리 |
| 사양 감지 | sPDF `vl.py` 확장 + Windows API/`nvidia-smi` | AI 런타임 설치 전에도 RAM·GPU·VRAM 판정 가능 |
| 모델 선택 | 버전 관리되는 모델 카탈로그 | 모델명·양자화·필요 메모리·작업 적합성을 코드와 분리 |
| 파일 감시 | 우선 주기적 스캔, 필요 시 `watchdog` | 다운로드 완료 판정은 주기적 스캔이 단순하고 안정적 |
| 데이터 검증 | 표준 라이브러리 + 명시적 검증 함수, 후속 `jsonschema` | 설치 의존성을 작게 시작 |
| 테스트 | `unittest` 또는 `pytest` | 임시 폴더 기반 파일 처리 테스트에 적합 |
| 패키징 | PyInstaller | 기존 sPDF Windows 빌드 경험 재사용 가능 |

분석 제공자의 초기 기본값은 로컬 Ollama의 `자동(권장)` 프로필로 한다. 사용자는 OpenAI 또는 Anthropic을 명시적으로 선택하고 제공자별 모델명을 따로 보존할 수 있다. 모델별 결과·비용·속도 비교 후 실제 권장 모델을 갱신하며, 클라우드 모델 ID는 앱 업데이트 없이 수정할 수 있게 한다.

## 6. 데이터와 폴더 설계

### 6.1 보관 폴더

```text
<library_root>/
  papers/
    Computer Science/
      Document AI/
        2025 - Example Paper.paperpack
  index/
    library.json
    errors.json
  state/
    ignored.json
    processing.json
  history/
    <paper_sha256>/
      revision-0002.paper.json
  trash/
    <delete_operation_id>/
      manifest.json
      <removed paperpack or unorganized PDF>
  logs/
    paper-organizer.log
```

실제 분류 폴더 깊이는 `분야/세부분야` 두 단계로 제한한다. 연도는 기본적으로 파일명에만 포함하고, 폴더 단계로 만들지는 설정에서 선택할 수 있게 한다. 너무 깊은 경로와 분류 체계의 파편화를 피하기 위해서다.

논문 하나의 영구 원본은 `*.paperpack` 한 파일이다. 이는 표준 ZIP 컨테이너이며 `document/paper.pdf`, `metadata/paper.json`, `content/content.json`, `history/revision-NNNN.json`, `manifest.json`을 담는다. PDF는 ZIP 안에서 압축하지 않아 원본 바이트를 그대로 보존하고 JSON만 압축한다. 일반 압축 프로그램으로 내용을 꺼낼 수 있으므로 앱이 없어도 복구 가능하다. 통합 인덱스나 검색 캐시가 손실되어도 `.paperpack` 전체를 스캔해 복구할 수 있다.

### 6.2 `.paperpack`의 `metadata/paper.json` 예시

```json
{
  "schema_version": 2,
  "id": "sha256:0123456789abcdef...",
  "file": {
    "original_name": "downloaded-paper.pdf",
    "current_name": "2025 - Example Paper.pdf",
    "relative_path": "papers/Computer Science/Document AI/2025 - Example Paper.pdf",
    "sha256": "0123456789abcdef...",
    "size_bytes": 1234567,
    "page_count": 14
  },
  "identity": {
    "file_id": "sha256:0123456789abcdef...",
    "work_id": "content:89abcdef01234567...",
    "edition_id": "sha256:0123456789abcdef...",
    "content_fingerprint": "89abcdef01234567...",
    "fingerprint_version": "paper-content-v1",
    "source_variant": "researchgate",
    "wrapper_pages": [
      {
        "pdf_page": 1,
        "type": "researchgate_cover",
        "confidence": 0.99
      }
    ],
    "content_start_pdf_page": 2
  },
  "bibliography": {
    "title": "Example Paper",
    "authors": ["A. Researcher", "B. Researcher"],
    "year": 2025,
    "venue": "",
    "doi": "",
    "arxiv_id": ""
  },
  "classification": {
    "category": "Computer Science",
    "subcategory": "Document AI",
    "tags": ["PDF", "classification"],
    "confidence": 0.93
  },
  "description": {
    "summary_ko": "이 논문은 ...",
    "research_question": "...",
    "methods": ["..."],
    "contributions": ["..."],
    "limitations": ["..."],
    "keywords": ["document AI", "classification"]
  },
  "detection": {
    "is_academic_paper": true,
    "confidence": 0.97,
    "reason": "제목, 초록, 저자, 참고문헌 구조가 확인됨"
  },
  "workflow": {
    "status": "organized",
    "needs_review": false,
    "review_reason": "",
    "processed_at": "2026-07-22T07:00:00Z",
    "updated_at": "2026-07-22T07:00:00Z"
  },
  "curation": {
    "revision": 2,
    "field_sources": {
      "bibliography.title": "user",
      "description.summary_ko": "ai",
      "experimental_details.culture_media": "user"
    },
    "locked_fields": [
      "bibliography.title",
      "experimental_details.culture_media"
    ],
    "last_edited_at": "2026-07-22T08:30:00Z",
    "last_edited_by": "user"
  },
  "provenance": {
    "extractor": "pymupdf",
    "ocr_used": false,
    "provider": "ollama",
    "model": "qwen3:8b",
    "prompt_version": "paper-v2"
  }
}
```

`metadata/paper.json`에는 원문 전체를 중복 저장하지 않고 검색에 필요한 서지정보,
태그와 요약만 넣는다. 검색용 본문은 같은 패키지의 `content/content.json`에 보존한다.
초기 실사용 버전은 키워드를 찾기 위한 텍스트만 다루며 실험정보 구조화나 근거 페이지
연결을 요구하지 않는다. 기존 `*.paper.json`과 `*.content.json`은 가져오기 호환용
레거시 형식으로만 유지한다.

본문 검색의 기준 데이터는 AI가 만든 요약이 아니라 PDF에서 직접 추출한 전체 텍스트다. 초록·Methods·Results·표와 그림 설명·부록·참고문헌까지 추출 가능한 텍스트를 페이지 경계와 원래 읽기 순서를 유지해 저장한다. 배지, 세포주, 시약, 장비 모델명처럼 요약에서 생략되기 쉬운 단어도 AI 실행 여부와 관계없이 정확 검색할 수 있어야 한다. 검색 정규화본을 만들더라도 원문 텍스트를 함께 보존하며, 하이픈·그리스 문자·대소문자·약어 차이는 검색 시 별도 정규화 계층에서 처리한다.

일반 논문의 추출 텍스트는 보통 원본 PDF보다 훨씬 작고 ZIP에서 다시 압축되므로 `.paperpack`의 기본 영구 데이터로 포함한다. 구현 시 논문별 원본 PDF 크기, 추출 텍스트 크기와 압축 후 크기를 진단 정보로 기록해 비정상적으로 큰 결과를 탐지하되, 고정 용량 비율을 정상 판정 기준으로 사용하지 않는다. 의미 검색용 임베딩은 용량과 모델 종속성이 크므로 기본 `.paperpack`에는 넣지 않고, 사용자가 기능을 켰을 때만 생성하는 재구축 가능한 로컬 캐시로 둔다.

### 6.3 통합 인덱스

`index/library.json`은 다음만 담는 경량 파생 파일로 만든다.

- 인덱스 스키마 버전과 생성 시각
- 전체 논문 수
- 각 논문의 ID, 상대 경로, 제목, 저자, 연도, 분야, 태그, 요약
- `work_id`별 대표 논문과 연결된 파일·판본 목록
- 검색용으로 정규화한 문자열

인덱스 갱신 도중 앱이 종료되어도 기존 파일이 손상되지 않도록 임시 파일에 쓴 후
`os.replace`로 교체한다. 인덱스가 없거나 JSON 파싱에 실패하면 `.paperpack` 전체를
다시 스캔해 재생성한다. 초기 버전은 SQLite를 영구 DB로 도입하지 않고 표준 ZIP 안의
PDF와 JSON, 재생성 가능한 `library.json`만 사용한다.

## 7. 주요 기능 설계

### 7.1 입력 탐색과 다운로드 완료 판정

- 기본 입력 폴더는 Windows의 사용자 다운로드 폴더로 자동 제안한다.
- 기본은 최상위의 `*.pdf`만 탐색하고 하위 폴더 탐색은 설정으로 둔다.
- 수정 시각이 최소 30초 이상 지났고, 두 번의 스캔에서 파일 크기가 같을 때만 안정된 파일로 판단한다.
- `.crdownload`, `.part`, `.tmp`가 연관되어 있거나 파일을 독점적으로 열 수 없으면 다음 스캔으로 미룬다.
- `%PDF-` 시그니처와 PyMuPDF 열기 결과를 함께 확인해 확장자만 PDF인 파일을 거른다.

### 7.2 논문 여부 판정

일반 매뉴얼·영수증·보고서까지 이동되는 것을 막기 위해 분석을 두 단계로 나눈다.

1. 규칙 기반 후보 판정: 페이지 수, 추출 텍스트 길이, `Abstract`, `References`, DOI/arXiv 패턴, 저자·소속 구조 등
2. Ollama 구조화 판정: `is_academic_paper`, `document_type`, `confidence`, `reason`

자동 이동 기준은 설정할 수 있게 하며 초기 권장값은 `is_academic_paper=true`이고 판정 신뢰도 0.85 이상이다. 그 미만은 검토 대기함으로 보낸다.

### 7.3 텍스트 추출과 OCR

- 빠른 논문 판정 단계에서는 문서 메타데이터와 앞부분, 초록·서론 후보, 결론·참고문헌 후보를 우선 추출한다.
- 정식 보관·색인 단계에서는 AI 요약 범위와 별개로 PDF의 추출 가능한 전체 텍스트를 페이지·블록·청크 단위로 저장한다.
- PDF 물리 페이지 번호와 읽기 순서를 유지해 검색 결과가 sPDF의 정확한 페이지로 이동할 수 있게 한다.
- 원문과 검색 정규화본을 구분하여 배지명·세포주명·시약명·장비 모델의 철자와 기호를 잃지 않는다.
- 추출 텍스트가 기준보다 적으면 `needs_ocr`로 표시하고 자동 이동하지 않는다.
- sPDF의 RapidOCR 서브프로세스를 설치본에 포함해 GUI 프로세스와 무거운 OCR 런타임을 분리한다.
- OCR 결과로 원본을 덮어쓰기보다 분석·검색용 텍스트만 얻는 모드를 먼저 구현하고, 일반 추출 텍스트와 같은 페이지 청크 스키마에 저장한다.
- Ollama 비전 OCR은 정확도가 더 필요한 사용자를 위한 후속 선택 기능으로 둔다. 요약
  모델과 OCR 모델을 별도로 선택하고, 기본 RapidOCR은 좌표 보존·저사양·오프라인
  경로로 유지한다.
- 텍스트 추출과 정확 검색은 요약 AI 또는 API 키가 없어도 항상 사용할 수 있어야 한다.

### 7.4 메타데이터 분석

선택한 Ollama·OpenAI·Anthropic 제공자에는 자유 문장 대신 같은 JSON Schema를
제공한다. 초기 출력 항목은 제목, 저자, 연도, 학회·저널, DOI/arXiv ID, 분야,
세부분야, 태그, 한국어 요약, 연구 질문, 방법, 기여, 한계와 각 신뢰도다. 배지·세포주
같은 실험정보의 AI 구조화는 첫 설치·수집·검색이 안정된 뒤 마지막 개발 단계에서만
검토한다.

모델 출력은 바로 신뢰하지 않고 다음을 검증한다.

- 필수 키 존재와 자료형
- 연도 범위, 신뢰도 0~1 범위
- DOI/arXiv 형식
- Windows에서 사용할 수 없는 폴더·파일명 문자 제거
- 허용된 카테고리 목록에 포함되는지 확인
- 제목·분류가 비어 있거나 신뢰도가 낮으면 검토 상태로 전환

프롬프트에는 버전을 부여한다. 나중에 모델이나 프롬프트를 바꿔 다시 분석한 레코드와 이전 결과를 구분하기 위해서다.

### 7.5 분류 체계

초기 카테고리는 사용자가 편집할 수 있는 설정 파일로 정의한다. 모델에는 이 목록 안에서만 고르게 한다. 예시는 다음과 같다.

- Computer Science
- Medicine & Life Science
- Engineering
- Mathematics & Statistics
- Physics
- Chemistry & Materials
- Social Science
- Economics & Business
- Humanities
- Multidisciplinary
- Uncategorized

세부분야도 분야별 허용 목록을 둘 수 있지만 초기에는 자유 입력 후 정규화하는 방식으로 시작한다. 사용자가 분류를 수정하면 `.paperpack`을 새 분류 경로로 이동하고 인덱스를 갱신한다.

### 7.6 중복 처리

중복은 `같은 파일`, `같은 논문의 다른 파일`, `관련 있지만 다른 판본`으로 구분한다. 모든 후보 PDF의 SHA-256은 계속 계산하지만 이는 파일 자체의 동일성만 판단하는 `file_id`로 사용한다.

#### 논문 동일성 계층

- **file_id**: PDF 전체 바이트 SHA-256. 완전히 같은 파일만 일치한다.
- **edition_id**: 출판사판, 저자 최종본, preprint처럼 특정 판본을 식별한다.
- **work_id**: 표지·워터마크·페이지 번호 차이와 관계없이 같은 연구 논문을 묶는다.

한 `work_id` 아래에 여러 `edition_id`와 파일을 연결할 수 있다. 검색 결과 기본 화면에서는 work 하나를 한 건으로 보이고, 상세 화면에서 출판사판·ResearchGate판·arXiv판 등 연결된 파일을 펼쳐 본다.

#### ResearchGate 표지와 래퍼 페이지 감지

페이지별 텍스트를 먼저 추출하고 다음 특징을 조합해 논문 본문이 아닌 래퍼 페이지를 판정한다.

- `ResearchGate`, `researchgate.net`, “See discussions, stats, and author profiles” 같은 저장소 표식
- 논문 제목·저자·초록·본문 구조 없이 다운로드 안내나 프로필 링크가 중심인 페이지
- 다음 페이지부터 제목, 저자, Abstract 또는 정식 저널 머리말이 시작되는 구조
- 기존 논문과 비교했을 때 첫 장만 추가되고 이후 페이지 텍스트가 순서대로 강하게 일치하는 경우

ResearchGate 문구 하나만으로 페이지를 버리지는 않는다. 신뢰도가 낮으면 래퍼 후보로만 표시하고 본문 지문 계산을 두 방식으로 비교한다. 감지 규칙은 ResearchGate에 한정하지 않고 기관 리포지터리, 저널 다운로드 안내와 스캔 표지에도 확장 가능한 규칙 목록으로 관리한다.

#### 본문 지문 생성

래퍼 후보와 반복 머리말·꼬리말, 페이지 번호, URL, 다운로드 시각을 제거한 정규화 텍스트로 `content_fingerprint`를 만든다.

1. 제목·저자·초록 후보의 정규화 지문
2. 본문 여러 구간의 단어 n-gram/MinHash 유사도
3. 결론·참고문헌 일부의 보조 지문
4. 텍스트가 부족한 경우 실제 논문 페이지 렌더링의 perceptual hash

첫 페이지만 고정적으로 제외하지 않는다. 정상 논문은 첫 페이지부터 본문이므로 페이지별 구조 판정 후 실제 본문 시작점을 선택한다. 래퍼를 제외해도 `pdf_page` 원래 번호를 보존하여 검색 결과를 sPDF의 정확한 물리 페이지로 열 수 있게 한다.

#### 판정 우선순위

1. SHA-256 일치: 완전히 같은 파일
2. DOI 또는 arXiv ID 일치 + 본문 지문 고유사: 같은 work의 다른 파일
3. 제목·저자·연도 일치 + 본문 지문 고유사: 같은 work 가능성이 매우 높음
4. 본문 지문은 유사하지만 DOI나 결론이 다름: 확장판·저널판 가능성, 사용자 검토
5. 제목만 유사: 약한 경고이며 자동 병합하지 않음

초기 자동 그룹 기준은 래퍼 제거 후 여러 본문 구간이 모두 높은 유사도를 보일 때로 제한한다. 학회 논문의 저널 확장판처럼 앞부분이 같지만 실험·결론이 달라진 문서를 잘못 합치지 않도록 단일 유사도 점수만으로 자동 병합하지 않는다.

#### 사용자 처리

동일 work 후보가 발견되면 새 PDF를 삭제하거나 즉시 이동하지 않고 다음 선택지를 제공한다.

- 기존 논문과 같은 파일이므로 무시
- 같은 논문의 다른 배포본으로 연결해 함께 보관
- 기존 대표 파일을 새 파일로 교체
- 별도 판본으로 연결
- 실제로 다른 논문이므로 분리

대표 파일은 사용자가 정하거나 `출판사 최종본 > 저자 최종본 > preprint > 저장소 래퍼판` 같은 설정 가능한 우선순위로 제안한다. 어떤 선택에서도 원본은 확인 전에 삭제하지 않는다.

#### 확인된 중복 파일 제거

중복 비교가 끝나면 사용자가 보관할 파일과 제거할 파일을 직접 선택할 수 있다. 앱은 중복이라는 이유만으로 파일을 자동 삭제하지 않는다.

비교 화면에는 다음 내용을 나란히 표시한다.

- 파일명, 전체 경로, 크기, 페이지 수와 수정 시각
- file/work/edition ID와 중복 판정 근거
- DOI, 제목, 저자, 판본 종류와 ResearchGate 래퍼 여부
- 대표 파일 여부, sPDF 열림·미저장 상태
- 각 파일에만 존재하는 페이지·주석·OCR 텍스트 여부

기본 버튼은 **“중복 파일을 휴지통으로 이동”**으로 한다. 정리 전 입력 PDF 또는 완성된 `.paperpack`과 판본 관계를 하나의 작업으로 함께 처리한다.

라이브러리 안의 파일은 먼저 `trash/<delete_operation_id>/`로 이동한다. 이 폴더의 `manifest.json`에는 다음을 기록한다.

- 제거한 PDF 또는 `.paperpack`의 원래 상대 경로와 해시
- 유지한 대표 파일과 work/edition 관계
- 삭제 요청 시각과 삭제 사유
- 제거 직전 인덱스 리비전
- 복원 가능 기한과 작업 상태

기본 보존 기간은 30일로 하고 사용자가 바꿀 수 있게 한다. 이 기간에는 “중복 제거 취소”로 모든 파일과 메타데이터를 원래 위치에 복원할 수 있다. 보존 기간이 지나도 자동 영구 삭제는 기본 비활성화하며, 사용자가 휴지통 비우기를 실행한 경우에만 영구 제거한다.

영구 삭제는 대상 파일 수·총 크기·경로를 다시 보여주고 별도의 확인을 받는다. 여러 파일을 한꺼번에 제거할 때도 work별 대표본과 제거 목록을 요약한 뒤 승인하도록 한다.

삭제 전 안전 확인은 다음과 같다.

1. 사용자가 검토한 대상 경로가 현재도 library root 또는 명시적으로 선택한 입력 폴더 안에 있는지 확인한다.
2. 검토 당시 해시와 현재 해시가 같고 파일이 바뀌지 않았는지 확인한다.
3. sPDF에서 열려 있거나 미저장 변경이 있는 파일은 삭제를 막는다.
4. work의 마지막 파일은 중복 제거 화면에서 삭제할 수 없다. 전체 논문 삭제 작업으로 따로 처리한다.
5. 대표 파일을 제거한다면 먼저 남는 파일 중 새 대표본을 선택한다.
6. PDF 또는 `.paperpack` 이동과 work 관계·통합 인덱스 갱신이 모두 성공한 뒤 작업을 완료 표시한다.

중간 단계가 실패하면 이미 옮긴 파일을 원래 위치로 되돌리고 인덱스를 이전 리비전으로 유지한다. 입력 폴더의 아직 정리되지 않은 중복 파일도 동일한 확인을 거쳐 Windows 휴지통 또는 앱 휴지통으로 보낼 수 있지만, 복구 가능한 방식을 기본값으로 한다.

### 7.7 안전한 이동과 복구

처리는 논문마다 잠금 파일을 사용하고 다음 순서를 지킨다.

1. 원본 해시와 목적지를 확정한다.
2. 목적지 폴더에 임시 `.paperpack`을 기록하고 디스크에 반영한다.
3. 패키지의 ZIP CRC, JSON, 크기와 SHA-256을 검증한다.
4. 임시 `.paperpack`을 최종 이름으로 원자적으로 교체한 뒤 입력 PDF를 정리한다.
5. 통합 인덱스를 재생성한다.

4단계 전에 실패하면 PDF를 원래 위치로 되돌린다. 되돌리기에도 실패하면 오류 레코드에 원본·목적지 경로를 모두 남기고 UI에서 복구 작업을 안내한다. 이동 기록에는 이전 경로를 보관하여 사용자가 최근 정리를 취소할 수 있게 한다.

입력/작업 폴더의 원본 PDF 처리는 `원본 유지`를 기본값으로 하고 `패키지 검증 후
입력 PDF 삭제`를 선택 옵션으로 제공한다. 삭제 옵션을 선택해도 `.paperpack`의 ZIP
CRC·PDF SHA-256·크기·JSON 검증이 모두 끝나기 전에는 원본을 건드리지 않는다.
삭제에 실패하면 새 패키지를 롤백하고 원본 PDF를 남긴다. CLI에서는
`--remove-source --confirm-remove-source`를 함께 요구하고 GUI에서는 대상 경로와
복구 불가능성을 다시 확인한다.

원본 유지 시 같은 PDF가 스캔마다 다시 나타나지 않도록 `state/imported-sources.json`에
입력 경로, 파일 SHA-256, 연결된 paperpack ID와 처리 시각을 기록한다. 경로가 같아도
파일 해시가 바뀌면 새 입력으로 다시 검토한다. 사용자는 설정에서 이 처리 기록을
지우고 재수집할 수 있다.

### 7.7.1 인수인계용 PDF 일괄 반출

선택한 여러 `.paperpack` 또는 라이브러리 폴더 전체에서 원본 PDF만 일반 폴더로
일괄 추출할 수 있게 한다. 기본 동작은 `.paperpack` 보존이며 기존 출력 PDF를
덮어쓰지 않는다. 같은 원래 파일명이 겹치면 `paper.pdf`, `paper (2).pdf`처럼
충돌 없는 이름을 제안하고 결과 보고서에 원본 패키지와 출력 PDF의 대응을 남긴다.

`PDF만 남기고 paperpack 제거`는 별도의 강한 옵션으로 둔다. 실행 순서는 다음과 같다.

1. 대상 패키지 전체의 ZIP CRC, 안전 경로, JSON과 PDF SHA-256을 먼저 검증한다.
2. 모든 PDF를 임시 파일로 추출하고 원본 PDF 해시·크기와 다시 대조한다.
3. 하나라도 실패하면 이번 작업의 출력물을 정리하고 어떤 `.paperpack`도 제거하지 않는다.
4. 전부 성공한 뒤에만 대상 수·총 크기·경로를 다시 보여주고 원본 제거를 재확인한다.
5. CLI 자동화에서는 `--remove-source --confirm-remove-source` 두 옵션을 함께 요구한다.

원본 제거 중 일부만 실패하는 예외 상황은 결과 PDF를 보존하고 제거 완료·실패
목록을 명확히 보고한다. GUI에서는 Windows 휴지통 이동을 우선 제공하고, 영구
제거는 별도 경고를 거친 고급 선택지로 둔다.

### 7.8 관리·검색 UI

권장 화면은 세 영역으로 구성한다.

- 왼쪽: 전체, 검토 필요, OCR 필요, 중복, 오류와 분야 목록
- 가운데: 제목, 연도, 첫 저자, 세부분야가 보이는 논문 목록
- 오른쪽: 서지정보, 한국어 요약, 연구 방법, 기여, 한계, 태그, 파일 경로

상단에는 통합 검색창과 분야·연도·저자·검토 상태 필터를 둔다. 검색은 대소문자와 한글·영문 공백 차이를 정규화한 뒤 제목, 저자, 키워드, 태그, 요약을 대상으로 수행한다.

논문을 더블클릭하거나 Enter를 누르면 OS 기본 앱이 아니라 내장 sPDF로 연다. 주요 작업은 sPDF로 열기, 새 sPDF 창으로 열기, 폴더 열기, 메타데이터 편집, 다시 분석, 분류 이동, 원래 위치로 복원, 인덱스 재구축, JSON 보기다.

### 7.9 sPDF 내장 통합

sPDF는 별도 설치 프로그램이나 외부 프로세스에 의존하지 않고 Git submodule로 이 저장소 안에 포함한다. Paper Organizer가 `pdfeditor`의 내부 믹스인들을 직접 호출하지 않도록 Organizer 쪽 브리지에 다음과 같은 작은 공개 API를 둔다.

```python
viewer = spdf_bridge.open_pdf(path, parent=main_window)
viewer.closed.connect(on_viewer_closed)
```

통합 원칙은 다음과 같다.

- 프로그램 전체에서 `QApplication`은 하나만 생성한다.
- sPDF 코드는 최초로 논문을 열 때 지연 로드하여 Organizer 시작 속도를 보호한다.
- 1차 구현은 검증된 sPDF `MainWindow`를 같은 프로세스의 자식 창으로 열어 기존 탭·메뉴·편집 기능을 그대로 보존한다.
- 2차 구현에서 sPDF 문서 영역을 재사용 가능한 `SpdfWorkspace` 위젯으로 분리하고 Organizer 중앙 탭에 내장할 수 있다.
- 같은 PDF가 이미 sPDF에서 열려 있으면 새로 열지 않고 기존 탭이나 창을 활성화한다.
- Organizer에서 PDF가 이동·이름 변경되기 전 sPDF가 해당 파일을 열고 있는지 확인한다. 열려 있거나 미저장 편집이 있으면 이동을 보류하고 저장 또는 닫기를 요청한다.
- Organizer의 `.paperpack`과 sPDF의 최근 파일·즐겨찾기·편집 상태는 서로 다른 데이터로 저장한다.
- OCR 모델과 PyMuPDF 같은 공통 의존성은 한 번만 패키징한다.
- sPDF 내부 구현 변경이 Organizer 검색·분류 코드를 깨지 않도록 `paper_organizer/integrations/spdf_bridge.py`만 통합 경계로 사용한다.

논문 레코드의 ID와 현재 경로를 sPDF 창에 전달해, 후속 버전에서는 PDF를 읽으면서 오른쪽에 Organizer 요약·키워드·메모 패널을 함께 표시할 수 있게 한다.

#### sPDF 선택 영역 번역·요약(후속 개발 목표)

sPDF에서 사용자가 드래그해 선택한 본문만 Paper Organizer의 현재 AI 제공자로
번역하거나 요약할 수 있게 한다. 전체 논문이나 라이브러리를 다시 읽지 않으며,
선택 영역 밖의 문맥을 자동으로 추가하지 않는 것이 기본이다.

- sPDF 우클릭 메뉴와 선택 도구에 `선택 영역 한국어 번역`과
  `선택 영역 요약`을 제공한다.
- sPDF가 선택 텍스트, 실제 PDF 페이지 번호, 페이지별 bounding box, 문서 ID를
  `spdf_bridge`의 공개 신호/DTO로 전달한다. Organizer는 sPDF 내부 selection
  객체에 직접 접근하지 않는다.
- 실행 전 선택 글자 수, 페이지 범위, 제공자·모델, 로컬/클라우드 전송 여부를
  미리 보여준다. 클라우드는 기존 지속 동의가 없으면 매번 명시적으로 승인받는다.
- 번역은 원문 용어·수치·단위·인용 표지를 보존하고, 요약은 선택 영역에 실제로
  있는 내용만 사용한다. 선택 범위 밖의 사실을 추론하거나 논문 전체 결론처럼
  표현하지 않는다.
- 결과 패널에는 원문 선택 영역과 번역/요약을 나란히 표시하고 페이지 링크를
  누르면 sPDF가 해당 위치로 돌아간다.
- 결과는 기본적으로 임시 보기와 클립보드 복사만 제공한다. 사용자가 명시적으로
  선택한 경우에만 PaperPack의 사용자 메모/주석으로 저장하며, 정식 논문 요약이나
  AI 분석 필드를 자동 덮어쓰지 않는다.
- 텍스트 레이어가 없는 선택은 자동으로 전체 페이지 OCR을 실행하지 않는다.
  사용자가 `선택 영역 OCR 후 번역/요약`을 선택한 경우에만 해당 영역을 OCR한다.
- 요청은 취소 가능해야 하며 백그라운드 일괄 분석보다 사용자 선택 요청을 우선하되,
  동시에 로컬 모델을 중복 적재하지 않는다.
- sPDF 공용 선택 이벤트가 필요하면 기능을 먼저 sPDF 원본 저장소에 구현하고,
  Organizer는 업데이트된 submodule과 브리지 계약만 사용한다.

### 7.10 마지막 단계의 선택 기능: 실험정보 구조화와 자연어 질의

이 절은 1.0 첫 실사용과 기본 검색 범위에 포함하지 않는다. 우선 PDF 추출 텍스트의
단순 키워드 검색으로 배지·세포주 이름을 찾을 수 있게 하고, 라이브러리 전체를 매번
LLM 컨텍스트로 읽지 않는다. 실제 사용자 테스트와 리소스 측정이 끝난 뒤 필요성이
확인된 경우에만 아래 구조화·근거 연결·자연어 질의를 개발한다.

“어떤 배지를 사용했는가?”처럼 초록이나 요약에 나오지 않는 질문을 지원하기 위해 검색을 두 층으로 구성한다.

1. **구조화 색인**: 배지, 세포주, 균주, 생물종, 시약, 항체, 장비, 소프트웨어, 데이터셋, 온도·시간·농도 같은 조건을 정규화해 `*.paper.json`에 저장한다.
2. **근거 전문 색인**: Methods, Results, 표·그림 설명과 부록을 페이지·섹션·문단 청크로 나눠 `.paperpack`의 `content/content.json`에 저장하고 SQLite FTS 캐시로 빠르게 검색한다.

전문 색인은 AI가 고른 문장만 저장하는 방식이 아니라 PDF에서 추출 가능한 전체 텍스트를 대상으로 한다. 구조화 색인은 편리한 필터와 자연어 질의를 보조하지만, 값이 누락되거나 잘못 분류되어도 사용자는 원문 전문 검색으로 해당 단어를 찾을 수 있다. SQLite FTS에는 본문 사본을 영구 원본처럼 의존하지 않고 `.paperpack`의 페이지 청크를 색인하며, 손상되거나 삭제되면 전체 라이브러리에서 다시 만든다.

배지의 경우 다음 요소를 각각 검색할 수 있게 한다.

- 원문명과 정규화명: `DMEM`, `Dulbecco's Modified Eagle Medium`
- 약어·상품명·철자 변형
- 첨가물과 농도: `10% FBS`, 항생제, 성장인자
- 함께 사용된 세포주·균주
- 사용 목적과 배양 조건
- 논문 페이지, 섹션, 근거 문장과 추출 신뢰도

검색 방식은 세 가지를 제공한다.

- 정확 검색: `DMEM`, `RPMI-1640`, `LB broth`
- 필드 검색: `배지:DMEM 세포주:A549`, `시약:"anti-CD3"`
- 자연어 질의: “A549 세포를 어떤 배지에서 배양했나?”

한국어 자연어 질의는 한국어 검색어만 FTS에 넣지 않는다. 질문에서 영문 약어,
세포주·유전자·단백질·시약·장비 모델 식별자를 원문 그대로 보존하고, 한국어
일반 용어에는 논문에 실제로 등장할 가능성이 높은 짧은 영문 표현을 함께 생성한다.
AI가 만든 검색 계획과 결정적 규칙의 결과를 합친 뒤 SQLite FTS로 후보를 찾는다.
검색 계획에는 실제 설치된 로컬 Ollama 모델을 먼저 사용한다. 사용자가 선택한
Ollama 모델이 설치돼 있으면 이를 우선하고, 아니면 설치된 검색 적정 크기 모델을
고른다. 호환 로컬 모델이 없을 때만 사용자가 설정한 클라우드 제공자로 넘어간다.

자연어 질의는 먼저 구조화 필드와 전문 청크에서 후보 근거를 찾고, 사용자가 선택한 AI 제공자가 그 근거만 사용해 답을 합성한다. 클라우드 제공자에는 전체 PDF가 아니라 필요한 근거 청크만 보내는 최소 전송 모드를 우선한다. 답에는 논문 제목, 실제 추출값, 페이지와 근거 미리보기를 표시한다. 사용자가 결과를 클릭하면 sPDF가 해당 PDF와 페이지를 바로 연다. 근거가 없으면 모델이 추측하지 않고 “본문에서 확인되지 않음”으로 답한다.

표 안에만 있는 배지·농도는 일반 텍스트 추출로 누락될 수 있으므로, 표 블록 추출과 OCR 결과도 같은 청크 형식으로 합친다. 정규화값과 원문값을 모두 보존하여 모델이 잘못 합친 항목을 사용자가 수정할 수 있게 한다.

### 7.11 요약 AI 제공자 선택과 클라우드 API

요약 엔진은 하나의 `SummaryProvider` 계약 뒤에 둔다. 로컬 Ollama, OpenAI Responses API, Anthropic Messages API가 동일한 요청·결과 구조와 JSON Schema 검증을 사용하므로 `.paperpack`과 인덱스 형식은 제공자와 무관하다.

| 제공자 | 초기 모델 후보 | 키·설치 | 기본 전송 정책 |
|---|---|---|---|
| Ollama | 하드웨어 검사 후 자동 추천 | 선택 모델을 설치 후 다운로드 | `127.0.0.1`에서만 처리 |
| OpenAI | `gpt-5.6`(설정에서 변경 가능) | OpenAI API 키 | 명시적 동의 요청만 HTTPS 전송, `store: false` |
| Anthropic | `claude-sonnet-4-6`(설정에서 변경 가능) | Anthropic API 키 | 명시적 동의 요청만 HTTPS 전송 |

OpenAI는 Responses API의 `text.format` JSON Schema, Anthropic은 Messages API의 `output_config.format` JSON Schema를 사용한다. 원격 응답도 앱의 명시적 검증을 다시 통과해야 저장하거나 파일을 이동할 수 있다. API가 지원하는 모델 목록은 키 입력 후 조회·새로고침할 수 있게 하며, 마지막으로 성공한 모델 ID를 제공자별로 기억한다. 서비스에서 모델이 폐기되거나 권한이 없으면 사용자에게 모델 변경을 요청하고 다른 클라우드로 자동 전환하지 않는다.

API 키는 `settings.json`, `.paperpack`, 로그, 크래시 보고서와 Git에 절대 기록하지 않는다. Windows에서는 OS 자격 증명 저장소를 사용하고, 개발·관리 환경에서는 `OPENAI_API_KEY`와 `ANTHROPIC_API_KEY` 환경 변수를 읽을 수 있다. UI에는 키 전체를 다시 표시하지 않고 등록 여부·마지막 검증 시각과 삭제/교체만 제공한다.

배포판의 OpenAI·Anthropic endpoint는 공식 HTTPS 주소로 고정하고 일반 설정으로 바꾸지 못하게 한다. HTTP 전송 계층도 인증 헤더가 붙은 요청의 호스트 허용 목록을 다시 검사하며, 인증 헤더가 있는 요청의 HTTP redirect는 거부한다. 제공자 객체는 평문 키를 장기간 보유하지 않고 요청 직전에 보안 저장소에서 읽는다. UI·진단에는 마지막 네 글자만 표시하며 `Authorization`, `x-api-key`, `sk-` 계열 문자열은 공통 redactor를 거친다. Anthropic Admin API 키처럼 필요 이상의 권한을 가진 키는 입력 단계와 호출 단계에서 거부한다.

환경 변수는 개발·관리 환경의 폴백일 뿐 일반 사용자 UI의 저장 방식으로 사용하지 않는다. OCR·sPDF 보조 프로세스를 시작할 때는 상속 환경에서 `OPENAI_API_KEY`와 `ANTHROPIC_API_KEY`를 제거해 관계없는 자식 프로세스에 키가 전달되지 않게 한다.

`.env` 계열 파일은 Git에서 제외하고 CI는 추적 파일의 OpenAI·Anthropic 키 형태를 검사한다. 탐지 보고에는 키 값 대신 파일·줄·키 종류만 표시한다. 탐지되면 커밋에서 문자열을 지우는 것에 그치지 않고 제공자 콘솔에서 해당 키를 폐기·교체한다.

클라우드를 처음 선택할 때 다음 내용을 보여주고 동의를 받는다.

- 제공자 이름과 실제 모델 ID
- 전송할 범위(제목·초록, 선택 청크, 분석용 추출 본문)와 대략적인 문자·토큰 수
- 논문 텍스트와 요청 결과가 외부 서비스로 전송된다는 사실
- 예상 입력·출력 토큰과, 가격 카탈로그가 최신일 때만 계산한 예상 비용
- 한 번만 허용 또는 이 제공자에 계속 허용하는 선택

동의값은 키와 별개이며 기본값은 `false`다. 논문별 “클라우드 전송 금지” 표시를 제공하고 이 표시가 있으면 전역 동의가 있어도 로컬만 사용한다. 실패 시 재시도는 동일 제공자에 한정해 지수 백오프와 `Retry-After`를 따르며, 다른 제공자 전환·더 비싼 모델 선택·전문 범위 확대에는 새 승인이 필요하다. 실제 호출 뒤 사용량 필드를 기록해 비용 내역을 확인할 수 있게 하되 API 키와 원문은 기록하지 않는다.

API 사용량이 넉넉한 사용자를 위해 클라우드 처리량 프로필을 별도로 둔다. 이 설정은 로컬 AI의 절전·고성능 프로필과 독립적이다.

| 클라우드 프로필 | 초기 동시 요청 | 동작 |
|---|---:|---|
| 보수적(기본) | 1 | 문서별 순차 처리, 낮은 속도 제한, 호출 전 비용 확인 |
| 표준 | 2 | 제공자 한도 안에서 제한적 병렬 처리 |
| 대량 처리 | 사용자 지정 1~16 | 밀린 대기열 병렬 처리, `Retry-After`와 제공자 rate-limit 헤더를 따라 자동 감속 |

월간 앱 비용 한도는 선택값이며 `0`은 “Paper Organizer 자체 한도 없음”을 뜻한다. 이 옵션은 API 서비스의 실제 결제·사용량 한도를 해제하지 않는다. 대량 처리나 앱 한도 없음을 처음 켤 때 예상 대상 수와 토큰을 보여주고 다시 확인받으며, 사용자는 언제든 새 요청 중지·대기열 일시정지를 할 수 있다. 병렬 처리 중에도 같은 PDF는 한 번만 분석하고 결과 저장은 문서별로 원자적으로 수행한다.

### 7.12 PC 사양 기반 로컬 AI 추천·선택

첫 실행과 사용자가 “사양 다시 검사”를 누른 시점에 로컬 하드웨어 프로필을 만든다. 이 검사는 모델이나 PyTorch가 설치되기 전에도 동작해야 한다.

- CPU 모델, 물리·논리 코어 수와 지원 명령어
- 전체·현재 사용 가능 RAM
- GPU 제조사·모델, GPU별 VRAM
- CUDA, DirectML과 CPU 실행 가능 여부
- 모델 저장 경로의 여유 디스크
- Ollama 실행 여부, 버전, 설치된 모델과 현재 로드된 모델
- sPDF OCR 런타임과 OCR 모델 설치 상태

기존 sPDF `vl.py`의 Windows `GlobalMemoryStatusEx`, `nvidia-smi`, `torch.cuda`, DirectML 감지 코드를 공통 `hardware.py`로 옮겨 재사용한다. NVIDIA가 아닌 GPU나 감지 실패 상황에서는 Windows 어댑터 정보와 CPU 모드를 안전한 폴백으로 사용한다.

#### 모델 카탈로그

모델 이름을 조건문에 직접 넣지 않고 `models/catalog.json`에서 관리한다. 각 항목에는 다음 정보를 둔다.

- Ollama 모델 식별자와 양자화 수준
- 다운로드 크기와 예상 실행 메모리
- 최소·권장 RAM/VRAM
- CPU, CUDA, DirectML 지원 여부
- 권장 컨텍스트 길이
- 논문 판정, 요약, 실험정보 추출, 자연어 질의별 적합도
- 한국어·영어 처리 품질 등급
- 카탈로그 버전과 검증일

따라서 모델이 바뀌어도 앱 코드를 고치지 않고 카탈로그만 갱신할 수 있다. 인터넷이 없는 환경에서는 설치본에 포함된 기본 카탈로그를 사용한다.

#### 설치 후 다운로드와 모델 용량군

Paper Organizer 설치본에는 요약용 LLM 가중치를 포함하지 않는다. 설치본에는 앱, sPDF 통합 코드와 모델 선택·다운로드 기능만 넣는다. 사용자는 설치 후 첫 실행 또는 AI 기능을 처음 사용할 때 사양 검사 결과를 보고 원하는 모델을 한 번 다운로드한다. 받은 모델은 Ollama 저장소에 캐시되어 이후 논문마다 다시 다운로드하지 않는다. 다른 모델 선택, 모델 업데이트 또는 사용자가 삭제한 경우에만 다시 받는다.

2026-07-22 Ollama 공식 라이브러리의 기본 양자화 태그 기준 용량군은 다음과 같다. 정확한 크기는 모델·태그·양자화에 따라 달라지므로 다운로드 직전 카탈로그 값을 다시 표시한다.

| 용량군 | 다운로드 예시 | 권장 용도 |
|---|---|---|
| 초경량, 약 0.5~1.5GB | Qwen3 0.6B `523MB`, 1.7B `1.4GB`; Gemma 3 1B `815MB` | 구역별 사실 추출·계층형 요약 벤치마크, 저사양 CPU |
| 경량, 약 2~4GB | Granite 4.1 3B `2.1GB`; Phi-4 Mini `2.5GB`; Ministral 3 3B `3.0GB`; Qwen3 4B `2.5GB` | 일반 논문 요약, 실논문 벤치마크 기반 추천 |
| 경량, 약 2.5~4GB | Qwen3 4B `2.5GB`; Phi-4 Mini `2.5GB`; Ministral 3 3B Q4_K_M `3.0GB`; Gemma 3 4B `3.3GB`; Gemma 3 4B IT QAT `4.0GB` | 구역별 요약과 기본 메타데이터 추출·교차 모델 벤치마크 |
| 균형, 약 5GB | Qwen3 8B `5.2GB` | 일반적인 정밀 요약·실험정보 추출의 상한 |
| 초대형, 100GB 이상 | Qwen3 235B `142GB` | 개인용 백그라운드 앱에서는 추천·표시 대상에서 기본 제외 |

공식 확인 자료: [Ollama Qwen3 모델 목록](https://ollama.com/library/qwen3), [Qwen3 전체 태그](https://registry.ollama.com/library/qwen3/tags), [Granite 4.1](https://ollama.com/library/granite4.1), [Gemma 3 전체 태그](https://ollama.com/library/gemma3/tags), [Phi-4 Mini](https://ollama.com/library/phi4-mini), [Ministral 3](https://ollama.com/library/ministral-3/tags).

다운로드 크기와 실제 실행 RAM/VRAM은 같지 않다. 컨텍스트 캐시와 런타임 오버헤드가 추가되므로 추천기는 파일 용량만 보고 모델을 고르지 않고 하드웨어 여유와 짧은 실측 벤치마크를 함께 사용한다. 현재 0.6B·1.7B·4B는 구역별 계층형 요약 후보로 벤치마크하고, 8B를 통합 요약과 기여·한계 분석의 품질 상한으로 제안한다. 12B 이상은 Paper Organizer의 요약 이득에 비해 자원 사용량이 커 카탈로그에서 제외한다.

모델을 고르는 화면과 모델 관리 화면에는 크기만 보여주지 않고 권장 용도,
환각 위험, 실제 요약 방식, 기여·한계 분석 지원 여부와 메모리 주의사항을 함께
표시한다. 이 안내는 초기에는 파라미터 등급별 보수적 정책을 사용하고, 이후 실제
벤치마크가 쌓이면 모델·양자화별 측정 결과로 교체한다.

같은 화면에서 `백그라운드 모델`과 `수동 요약 모델`을 따로 선택한다. 백그라운드
모델에는 상주 체크를 제공하며, 체크하면 첫 요청 뒤 메모리에 유지하고 해제하면
논문 한 편 처리 후 언로드한다. 수동 모델은 작업이 끝나면 항상 언로드한다.
기본 조합은 백그라운드 Qwen3 1.7B와 수동 Qwen3 4B이며, 24GB 이상 시스템은
벤치마크 결과에 따라 더 큰 모델을 직접 선택할 수 있다.

#### 첫 실행과 다운로드 흐름

1. Paper Organizer와 sPDF를 먼저 설치한다. 요약 모델이 없어도 PDF 보기·정리·일반 검색은 사용할 수 있다.
2. 앱이 Ollama 설치·실행 여부, CPU·RAM·GPU·VRAM과 여유 디스크를 검사한다.
3. `빠른/균형/고품질` 후보마다 다운로드 크기, 예상 실행 여유, 기능과 라이선스를 표시한다.
4. 사용자가 모델을 선택하고 다운로드를 승인한다. “나중에 받기”도 가능하다.
5. Ollama `POST /api/pull`의 스트리밍 상태를 이용해 현재 파일·진행률과 오류를 표시한다.
6. 다운로드 후 `GET /api/tags`의 실제 크기·양자화·digest를 확인하고 짧은 JSON 요약 테스트를 실행한다.
7. 테스트가 성공해야 선택 모델로 활성화하고, 실패하면 기존 모델을 유지하거나 더 작은 모델을 제안한다.

다운로드 전에는 모델 표시 크기에 임시 파일·컨텍스트 여유를 더한 디스크 안전 공간을 확인한다. 공간이 부족하면 다운로드를 시작하지 않는다. 다운로드 중 앱을 닫더라도 기존에 설치된 모델과 Paper Organizer 데이터에는 영향을 주지 않으며, 다음 실행에서 설치 상태를 다시 확인한다.

#### 모델 관리

설정 → 로컬 AI 모델 화면에서 다음 작업을 제공한다.

- 설치된 모델명, 실제 디스크 크기, 양자화, 최근 사용일 표시
- 추천 모델 다운로드와 진행률·재시도
- 모델별 빠른 벤치마크와 현재 선택 변경
- 사용하지 않는 모델 삭제 전 크기·사용 앱 경고
- 새 모델을 먼저 다운로드·검증한 뒤 이전 모델을 삭제하는 안전한 교체
- 모델 카탈로그 갱신 및 현재 선택과 새 버전 비교

Ollama 모델은 다른 앱도 함께 사용할 수 있으므로 아무 확인 없이 자동 삭제하지 않는다.
대신 Paper Organizer가 다운로드를 요청한 모델을 앱 관리 목록에 기록하고, 제거
마법사에서 모델명·용량·공유 가능성 경고를 보여준 뒤 `Paper Organizer가 관리한
Ollama 모델도 삭제`를 선택할 수 있게 한다. 선택한 경우에만 Ollama
`DELETE /api/delete`를 호출한다. 앱 제거 전 모델 관리 화면에서도 같은 방식으로
개별 삭제할 수 있다. 설치 모델과 크기는 `GET /api/tags`로 읽고, 모델 다운로드는
`POST /api/pull`로 처리한다. 관련 API: [모델 목록](https://docs.ollama.com/api/tags),
[모델 다운로드](https://docs.ollama.com/api/pull), [모델 삭제](https://docs.ollama.com/api/delete).

제거 마법사는 다음 범위를 서로 분리해 표시한다.

- 프로그램 파일 제거: 항상 수행
- Paper Organizer 설정·캐시·로그·분석 큐 제거: 기본 선택, API 자격 증명 포함
- 앱 관리 목록에 기록된 Ollama 모델 제거: 용량을 표시하고 사용자가 명시적으로 선택
- PDF 라이브러리·`.paperpack`·OneDrive JSON 미러 제거: 기본 해제이며 별도의
  강한 확인 없이는 삭제하지 않음

따라서 사용자는 완전 제거를 선택해 모델 가중치까지 지울 수 있지만, 단순 업데이트나
재설치 및 다른 Ollama 앱이 같은 모델을 쓰는 경우에는 모델을 보존할 수 있다.

#### 추천 규칙

설정 화면에서 다음 프로필을 제공한다.

- **자동(권장)**: 현재 사양과 설치된 모델 중 안정적으로 실행 가능한 최상 선택
- **속도 우선**: 작은 양자화 모델, 짧은 컨텍스트, 한 번에 한 작업 처리
- **균형**: 메모리 여유를 남기면서 상세 정보 추출 품질을 확보
- **품질 우선**: 충분한 RAM/VRAM이 있을 때 큰 모델과 긴 컨텍스트 사용
- **직접 선택**: 모든 모델을 표시하되 부적합 모델에는 예상 문제와 이유 표시

추천기는 모델이 요구하는 메모리에 컨텍스트 캐시와 sPDF·Windows가 사용할 안전 여유분을 더한다. 총 RAM이나 VRAM을 거의 전부 사용하는 모델은 “실행 가능”이더라도 추천하지 않는다. 여러 GPU가 있으면 사용 가능한 백엔드와 VRAM이 가장 적합한 장치를 표시하되 최종 배치는 Ollama 런타임에 맡긴다.

텍스트 분석 모델과 OCR 모델은 요구 특성이 다르므로 따로 추천한다.

- **논문 분석 AI**: 논문 판정, 요약, 실험정보 추출과 근거 기반 질의용 Ollama 모델
- **PDF OCR AI**: RapidOCR 또는 sPDF의 고품질 VL 모델

GPU가 없거나 메모리가 적어도 앱 자체와 기본 PDF 검색은 정상 동작한다. 로컬 AI는 작은 CPU 호환 모델을 추천하고, 상세 분석을 백그라운드 저속 작업으로 제한한다. 적합한 모델이 없으면 AI 기능을 자동 실행하지 않고 이유와 해결 방법을 보여준다.

#### 실제 성능 보정

하드웨어 수치만으로 속도를 정확히 예측하기 어렵기 때문에 선택한 모델에 짧은 대표 논문 프롬프트를 실행하는 선택적 벤치마크를 제공한다. 첫 응답 시간, 초당 토큰, JSON 스키마 준수, 메모리 부족 여부를 측정해 추천을 `권장`, `사용 가능`, `느림`, `비권장`으로 보정한다.

벤치마크는 전용 GPU뿐 아니라 Ollama Vulkan이 지원하는 내장 GPU도 먼저 시험한다.
실제 `/api/ps` 배치가 GPU인지 확인하고 CPU보다 빠르고 안정적이면 해당 PC의 기본
실행 방식으로 저장한다. GPU 적재 실패·시간 초과·메모리 부족 또는 속도 저하가
확인되면 같은 모델을 CPU로 다시 실행하며, 결과에는 처리 장치와 입출력 tokens/s를
함께 기록한다.

모델 다운로드는 크기, 예상 디스크 사용량과 라이선스 정보를 보여준 뒤 사용자가 승인한 경우에만 실행한다. 추천 결과가 모델을 자동으로 다운로드하거나 기존 선택을 임의로 교체하지 않는다. 사용자가 직접 고른 모델은 우선하되, 사양을 넘으면 경고하고 테스트 실행을 권한다.

설정에는 하드웨어 프로필, 추천 이유, 선택 모델, 카탈로그 버전과 마지막 벤치마크 결과를 저장한다. GPU 교체나 RAM 변경, Ollama 모델 설치·삭제가 감지되면 추천을 다시 계산하되 사용자의 명시적 선택은 보존한다.

### 7.13 백그라운드 절전 처리와 수동 우선 처리

논문 처리 대기열은 평소 절전 프로필로 한 편씩 실행한다. 사용자가 분석 큐에서 특정 논문을 선택해 수동 실행하면 해당 요청을 우선 처리하되 결과는 항상 정식 `.paperpack` 분석으로 저장한다.

#### 절전 백그라운드 모드

기본값은 시스템 사용을 방해하지 않는 `절전` 프로필이다.

- 논문 분석 동시 실행은 1개로 제한한다.
- 해시·텍스트 추출·OCR·Ollama 분석을 동시에 몰아서 실행하지 않고 단계별로 직렬화한다.
- 워커 프로세스는 Windows `BELOW_NORMAL` 또는 `IDLE` 우선순위를 사용한다.
- 작은 호환 모델, 제한된 컨텍스트와 보수적인 배치 크기를 사용한다.
- 문서 사이에 짧은 냉각 시간을 두고 UI 이벤트와 sPDF 작업에 항상 양보한다.
- CPU 사용률이 설정 기준보다 오래 높거나 사용 가능 RAM/VRAM이 안전 여유 아래로 내려가면 새 작업을 시작하지 않는다.
- sPDF에서 OCR·편집 중이거나 사용자가 분석 큐를 일시정지하면 새 추론을 시작하지 않는다.
- 노트북은 배터리 사용 시 기본 일시정지하고 사용자가 허용한 경우에만 실행한다.
- 작업 가능 시간대를 지정할 수 있다. 예: 평일 22:00~07:00 또는 PC 유휴 시에만 실행.
- 대기열이 비면 모델 유지 시간을 짧게 설정하거나 언로드하여 RAM·VRAM을 반환한다.

초기 권장 기준은 “CPU가 약 35% 미만이고, 선택 모델 실행 후에도 시스템 RAM 4GB 이상과 VRAM 20% 이상이 남을 때 새 작업 시작”으로 두되 설정에서 바꿀 수 있게 한다. 실행 중 CPU가 약 70% 이상으로 15초 지속되거나 RAM 여유가 3GB 아래로 내려가면 현재 안전 지점 이후 일시정지한다. 수치는 하드웨어 프로필과 벤치마크 결과에 따라 자동 보정한다.

외부에서 실행 중인 Ollama 서버는 앱이 CPU/GPU 사용량을 정확히 강제 제한할 수 없다. 이 경우 동시성, 모델 크기, 컨텍스트, 요청 간격과 모델 유지 시간을 이용한 소프트 제한을 적용한다. 추후 앱이 전용 Ollama 프로세스를 직접 실행하는 선택 모드에서는 Windows Job Object를 이용한 CPU 제한도 제공할 수 있다.

#### 수동 우선 처리

분석 큐에서 선택한 논문은 별도 임시 요약 창을 거치지 않고 정식 분석 흐름으로 바로
실행한다. 진행 중인 LLM 응답은 무리하게 끊지 않으며, 사용자는 큐에서 백그라운드
실행을 시작·중지하고 실패 항목을 재시도한다. 제공자·언어·제한 시간은
`요약 엔진 옵션`의 공통 설정을 사용하되, 자동 실행은 백그라운드 모델을,
사용자가 직접 실행한 항목과 AI 번역은 수동 모델을 사용한다.

#### 첫 페이지 선분류와 문서 유형별 파이프라인

모든 문서는 본문 요약을 시작하기 전에 첫 페이지를 AI에 먼저 보낸다. 이 첫 요청은
제목·저자·연도·저널명·DOI·출원번호 등 서지정보를 추출하는 동시에 문서 유형을
`연구논문`, `리뷰논문`, `특허` 중 하나로 분류한다. 분류 결과와 근거가 유효한지
검증한 뒤 유형에 맞는 전용 프롬프트를 선택하고 본 분석 파이프라인을 시작한다.

- 연구논문: 연구 질문, 재료와 방법, 결과, 논의 중심으로 분석한다.
- 리뷰논문: 검토 범위, 문헌 선택 기준, 주제 분류, 분야의 합의와 상충 결과,
  근거 수준, 연구 공백과 향후 과제를 분석한다.
- 특허: 기술적 과제, 청구된 발명의 핵심, 구현 및 실시예, 기술적 효과와 명시된
  제약을 분석하고 청구항 원문을 별도로 보존한다.

문서 유형 분류가 불확실하거나 서지정보와 모순되면 본 분석을 바로 시작하지 않고
대기열에 확인 필요 사유를 표시한다. 사용자는 분류를 교정한 뒤 해당 유형의
파이프라인을 다시 실행할 수 있어야 하며, 사용자가 확정한 유형은 이후 AI가
덮어쓰지 않는다.

기존 라이브러리에는 연구·리뷰 논문이 특허용 색인으로 저장된 항목이 있으므로
마이그레이션 도구를 제공한다. 첫 페이지를 다시 판별해 현재 유형과 다른 후보만
목록으로 보여주고, 사용자가 승인한 항목에 한해 PaperPack의 문서 유형과 색인을
교정한 뒤 올바른 전용 파이프라인으로 재분석한다. 기존 분석 결과는 즉시 삭제하지
않고 변경 전 유형과 분석 버전을 이력에 남긴다.

리뷰용 정답표와 점수는 연구논문과 분리하며, 전용 벤치마크가 기준을 넘은 뒤에만
리뷰 파이프라인을 자동 분석에 적용한다.

#### 대기열과 복구

대기열은 JSON 상태 파일에 원자적으로 저장하여 앱 재시작 후 이어간다. 각 작업은 우선순위, 요청 시각, 처리 단계, 재시도 횟수, 마지막 오류와 사용한 리소스 프로필을 기록한다. 같은 PDF가 두 대기열에 중복 등록되면 하나의 작업으로 합치고 높은 우선순위를 적용한다.

### 7.14 인덱스 내용 수정과 변경 이력

사용자는 검색 결과와 상세 패널에서 인덱스 내용을 직접 교정할 수 있어야 한다. 단, `index/library.json`과 `search.sqlite`는 재생성되는 파생 데이터이므로 UI가 이를 직접 수정하지 않는다. 편집 화면은 해당 `.paperpack`의 `metadata/paper.json` 또는 `content/content.json`을 트랜잭션 방식으로 수정하고, 저장 성공 후 파생 인덱스를 즉시 다시 만든다.

#### 편집 가능한 내용

- 제목, 저자와 저자 순서, 연도, 학회·저널, DOI, arXiv ID
- 분야, 세부분야, 태그와 사용자 지정 컬렉션
- 한국어 요약, 연구 질문, 방법, 기여와 한계
- 배지, 세포주, 균주, 시약, 항체, 장비, 데이터셋과 실험 조건
- 각 실험정보의 정규화명, 원문명, 근거 페이지·청크 연결
- 읽음 상태, 별점, 개인 메모와 사용자 지정 필드
- OCR이나 PDF 추출이 틀린 경우 `content.json`의 청크 텍스트

파일 ID, SHA-256, 실제 파일 크기처럼 파일에서 계산되는 값은 읽기 전용으로 둔다. PDF 경로, 카테고리와 파일명을 바꾸는 편집은 실제 파일 이동을 수반하므로 예상 이전·이후 경로를 보여주고 별도 확인을 받는다.

#### 편집 UI

- 기본 화면은 항목별 폼과 태그 편집기로 제공한다.
- 실험정보는 행 단위로 추가·삭제·병합하고 각 행에서 근거 페이지를 선택할 수 있게 한다.
- 여러 논문을 선택해 태그, 분야, 읽음 상태를 일괄 수정할 수 있다.
- 고급 사용자를 위해 원본 JSON 편집기를 제공하되 저장 전에 스키마와 경로를 검증한다.
- 저장 전 변경 내용을 이전 값/새 값으로 보여주고, 필수 값 누락이나 잘못된 형식은 설명과 함께 막는다.
- sPDF로 근거 페이지를 보면서 오른쪽 Organizer 패널에서 값을 수정할 수 있게 한다.
- 색인 편집 저장 옆의 AI 번역은 원본 필드를 덮어쓰지 않고 별도 번역을 만든 뒤,
  상세 패널에서 원문/번역을 토글한다. 번역은 원문 해시와 함께 PaperPack에 저장해
  분석이 그대로면 재사용하고, 재분석으로 해시가 달라지면 다시 생성한다.

#### 사용자 수정 보호

정식 필드의 현재값을 `paper.json`에 직접 저장하되 `curation.field_sources`에 `ai`, `extracted`, `external`, `user` 출처를 필드별로 기록한다. 사용자가 수정한 필드는 기본적으로 잠그며 이후 AI 재분석은 이를 자동으로 덮어쓰지 않는다.

재분석 결과는 바로 적용하지 않고 다음 세 열의 비교 화면으로 표시한다.

```text
필드                  현재값              새 AI 제안            적용
배지                  DMEM + 10% FBS      RPMI-1640             [유지/교체/병합]
세포주                A549                A549                  [동일]
요약                  사용자 수정 요약    새 요약                [유지/교체]
```

사용자는 필드별로 유지, 교체 또는 목록 병합을 선택한다. 잠긴 필드는 기본적으로 `유지`가 선택된다. “AI 값으로 모두 덮어쓰기”는 명시적인 추가 확인 없이는 실행하지 않는다.

#### 버전과 되돌리기

- 저장 전 현재 `paper.json`/`content.json`을 `history/<paper_sha256>/`에 원자적으로 스냅샷한다.
- 각 수정마다 `curation.revision`, 수정 시각, 변경 필드와 수정 원인을 기록한다.
- 논문별 최근 이력 목록에서 원하는 버전을 비교하고 복원할 수 있다.
- 기본 보존 개수는 논문당 20개로 하고 사용자가 조정할 수 있게 한다.
- 복원도 새 리비전으로 기록해 이력 자체가 사라지지 않게 한다.

호환 도구가 앱 외부에서 `.paperpack`을 수정한 경우 패키지 해시와 마지막 인덱스 리비전을 비교한다. 유효한 변경이면 가져와 인덱스를 다시 만들고, 앱의 미저장 편집과 충돌하면 양쪽 값을 비교하는 충돌 해결 화면을 연다.

## 8. 애플리케이션 구조

```text
paper_organizer/
  app.py               # QApplication과 메인 창 조립
  ui/
    main_window.py     # 검색·필터·목록·상세 화면
    settings_dialog.py
    review_dialog.py
    model_manager.py   # 설치 후 모델 추천·다운로드·교체·삭제
    metadata_editor.py # 서지·요약·실험정보·원본 JSON 편집
    merge_dialog.py    # 현재값과 AI 재분석 제안 비교·병합
    duplicate_review.py # 중복 비교, 대표본 선택과 안전한 제거
  core/
    discovery.py       # 입력 폴더 탐색과 안정성 판정
    pdf_extract.py     # PDF 검증, 텍스트·식별자 추출
    paper_detect.py    # 논문 여부 판정
    analyzer.py        # Ollama 요청과 프롬프트 버전
    schema.py          # 결과 정규화와 검증
    classifier.py      # 분류 체계와 목적지 계산
    duplicates.py      # 해시·DOI·제목 중복 확인
    document_identity.py # 래퍼 페이지 감지, 본문 지문과 work/edition 판정
    organizer.py       # 잠금, 이동, 롤백
    indexer.py         # paperpack/legacy sidecar 로드와 library.json 재생성
    content_index.py   # 페이지·섹션 청크와 실험정보 색인
    search.py          # 서지·전문·구조화 필드 검색
    question_answer.py # 검색 근거에 제한된 자연어 답변
    hardware.py        # CPU·RAM·GPU·VRAM·가속기·디스크 감지
    model_catalog.py   # 모델 요구사항과 작업 적합도 로드
    model_selector.py  # 사양·프로필·벤치마크 기반 추천
    resource_policy.py # 절전·균형·고성능 기준과 일시정지 판단
    scheduler.py       # 백그라운드/사용자 우선 대기열 실행
    revision_store.py  # 편집 스냅샷, 필드 출처와 되돌리기
    trash.py           # 중복 파일 묶음 이동·매니페스트·복원·비우기
  infra/
    settings.py        # 앱 설정의 원자적 저장
    logging.py
    ollama_client.py
    model_download.py  # pull 진행률, 설치 확인과 안전한 모델 교체
  models/
    catalog.json       # 로컬 AI 모델 요구사항과 추천 메타데이터
    paper.py           # 내부 데이터 모델
  integrations/
    spdf_bridge.py     # Organizer와 sPDF 공개 API 연결
  tests/
    fixtures/
    test_discovery.py
    test_detection.py
    test_schema.py
    test_organizer.py
    test_duplicates.py
    test_document_identity.py
    test_indexer.py
    test_content_index.py
    test_hardware.py
    test_model_selector.py
    test_model_download.py
    test_resource_policy.py
    test_scheduler.py
    test_revision_store.py
    test_metadata_edit.py
    test_trash.py
    test_search.py
  run.py
vendor/
  spdf/                 # github.com/loselessss/sPDF.git Git submodule
scripts/
  update_spdf.ps1       # main 최신 커밋 확인·테스트·포인터 갱신
.gitmodules             # vendor/spdf가 추적할 URL과 main 브랜치
```

Qt 비의존 코어와 GUI를 분리한다. 파일 처리와 검색은 단위 테스트에서 Qt 없이 실행할 수 있어야 한다. 논문 분석은 워커 스레드 또는 별도 프로세스에서 실행하고 GUI 스레드를 막지 않는다. Organizer는 `spdf_bridge.py` 외의 위치에서 sPDF 내부 모듈을 직접 참조하지 않는다.

### 8.1 sPDF 저장소 추적과 자동 업데이트

sPDF는 별도 저장소 `https://github.com/loselessss/sPDF.git`의 `main` 브랜치를 유지한다. 작성 시점 기준 앱 버전은 `1.6.1`이며, Paper Organizer는 `vendor/spdf` Git submodule로 이를 포함한다.

```ini
[submodule "vendor/spdf"]
    path = vendor/spdf
    url = https://github.com/loselessss/sPDF.git
    branch = main
```

submodule은 빌드마다 임의의 최신 코드를 받지 않고 Organizer 커밋이 가리키는 정확한 sPDF 커밋을 사용한다. 따라서 이전 Organizer 버전을 다시 빌드해도 같은 sPDF 코드가 들어간다. 한편 다음 자동화로 sPDF 업데이트를 신속하게 따라간다.

1. sPDF 변경을 `origin/main`에 커밋·푸시한다.
2. sPDF 릴리스 워크플로가 Organizer에 `repository_dispatch` 이벤트를 보낸다.
3. 보조로 Organizer의 예약 워크플로가 매일 `main`의 새 커밋을 확인한다.
4. 새 커밋이 있으면 `vendor/spdf` 포인터를 갱신한 의존성 PR을 자동 생성한다.
5. Organizer 단위 테스트, sPDF 열기·검색·OCR 통합 테스트와 PyInstaller 스모크 빌드를 실행한다.
6. 호환되는 패치·마이너 버전은 모든 테스트 통과 시 자동 병합할 수 있다.
7. 메이저 버전 또는 공개 API 변경은 자동 병합하지 않고 변경 내용을 검토한다.

릴리스가 아닌 sPDF `main`의 모든 커밋을 즉시 따라갈지, 버전 태그만 따라갈지는 설정할 수 있게 한다. 기본 권장은 안정성을 위해 **새 버전 태그 우선**이며, 개발 채널에서는 `main`의 최신 커밋을 추적한다.

로컬 개발에서는 `scripts/update_spdf.ps1`이 다음 작업을 한 번에 수행한다.

- 원격 `main` 조회와 현재/최신 커밋·`APP_VERSION` 비교
- submodule 업데이트
- Organizer와 sPDF 통합 테스트
- 성공 시 변경된 submodule 포인터와 버전 정보를 표시
- 실패 시 포인터를 커밋하지 않고 원인 보고

Paper Organizer의 도움말 → 버전 정보에는 Organizer 버전, 포함된 sPDF `APP_VERSION`, sPDF 커밋 SHA와 업데이트 채널을 함께 표시한다. sPDF 버전 정보는 `vendor/spdf/pdfeditor/meta.py`에서 읽고, Organizer가 별도 상수로 복사해 두지 않는다.

Organizer 전용 수정은 submodule 내부에 직접 넣지 않는다. sPDF 공용 기능 개선은 먼저 sPDF 저장소에 반영하고, Organizer는 업데이트된 submodule을 받아 사용한다. 그래야 두 저장소의 sPDF 코드가 갈라지지 않는다.

## 9. 상태 모델

각 파일은 다음 상태 중 하나를 갖는다.

| 상태 | 의미 | 다음 동작 |
|---|---|---|
| `discovered` | 안정된 PDF 발견 | 검증 |
| `queued` | 백그라운드 또는 사용자 우선 대기열 등록 | 실행 조건 확인 |
| `paused_resource` | 리소스·배터리·시간대 조건으로 대기 | 조건 회복 후 재개 |
| `duplicate` | 동일 파일 또는 판본 후보 | 사용자 선택 |
| `trashed` | 사용자가 제거해 복구 가능한 휴지통에 있음 | 복원 또는 영구 삭제 |
| `needs_ocr` | 텍스트 부족 | OCR 또는 수동 무시 |
| `not_paper` | 논문이 아님 | 입력 폴더 유지, 무시 목록 |
| `needs_review` | 판정·분류 신뢰도 부족 | 사용자가 수정·승인 |
| `analyzed` | 메타데이터 검증 완료 | 이동 |
| `organized` | `.paperpack` 보관 완료 | 검색·관리 |
| `failed` | 처리 실패 | 원인 확인 후 재시도 |

상태 전이를 명시하면 앱 재시작 후에도 중단 지점을 알 수 있고, 같은 파일에 대한 무한 재시도를 막을 수 있다.

## 10. 단계별 개발 일정

일정은 1인 개발과 기존 코드 재사용을 전제로 한 대략적인 작업량이다.

### 0단계 — 프로젝트 기반 정리와 sPDF 모듈화 (2~3일)

- 신규 패키지 구조와 실행 진입점 생성
- 기존 `settings.py`, `core.py`, `ui.py`의 필요한 코드 이관
- sPDF `origin/main`을 `vendor/spdf` submodule로 연결하고 브리지 API 정의
- 설정·로그·테스트 실행 환경 구성
- 첫 실행 필수 설정에서 Windows 로그인 자동 시작과 X 버튼 동작 선택
- 시스템 트레이 열기·완전 종료 및 이후 시작/종료 설정 변경
- 샘플 PDF 픽스처와 개인정보가 없는 테스트 자료 준비

완료 기준: 빈 라이브러리로 앱이 실행되고 설정을 저장·불러오며, 샘플 PDF를 내장 sPDF 창으로 열 수 있다.

### 1단계 — 안전한 수집 MVP (2~3일)

- 입력 탐색, 다운로드 완료 판정, PDF 검증
- SHA-256, DOI와 래퍼 제거 본문 지문 기반 중복·판본 탐지
- ResearchGate 표지 부착판·원본판을 같은 work로 그룹화
- PC 사양 검사, Ollama 설치 모델 탐색과 분석/OCR 모델 추천
- 설치 후 선택형 모델 다운로드, 진행률·디스크 검사와 활성화 검증
- 절전 리소스 정책과 재시작 가능한 단일 작업 대기열
- 텍스트 추출과 Ollama 분석
- 스키마 검증, 고정 카테고리, 목적지 계산
- 원자적 `.paperpack` 저장, 이동, 롤백

완료 기준: 앱이 현재 PC 사양과 설치된 모델을 설명하고 적합한 분석·OCR 모델을 추천한다. 정상 논문은 검증된 `.paperpack`으로 보관되고, 분석 실패·중복·비논문은 원본 위치에 남는다.

### 2단계 — 인덱스와 기본 검색 (2~3일)

- schema v2 확정
- `library.json` 생성 및 전체 재구축
- 제목·저자·키워드·요약 검색
- AI 없이 PDF 추출 텍스트의 `content.json` 생성
- 원문·정규화 텍스트의 단순 키워드 검색
- 분야·연도·상태 필터
- 손상·누락 `.paperpack` 엔트리와 체크섬 진단

완료 기준: AI 요약을 실행하지 않은 논문에서도 `DMEM`, `RPMI-1640` 같은 문자열을
원문 그대로 검색할 수 있다. 통합 인덱스를 삭제해도 `.paperpack`의 본문 JSON으로
검색 데이터를 복구할 수 있다.

### 3단계 — 관리 UI와 검토 흐름 (2~3일)

- 대기·OCR·중복·오류 목록
- AI 설정 화면의 사양 다시 검사, 프로필 선택, 모델 직접 선택과 벤치마크
- 설치 모델 용량 확인, 다운로드·업데이트·삭제와 실패 재시도
- 선택 항목 우선 처리와 백그라운드 분석 시작·중지
- CPU·RAM·GPU·VRAM 현황, 대기열 일시정지·재개·취소
- 메타데이터·요약·태그 편집, 일괄 수정과 분류 승인
- 사용자 수정 필드 잠금, 변경 이력·되돌리기와 AI 재분석 병합
- 중복 파일 비교, 대표본 선택, 휴지통 이동·복원과 수동 비우기
- 다시 분석, 이동 취소, 내장 sPDF/폴더 열기
- 동일 PDF의 sPDF 탭 재사용과 열린 파일 이동 방지
- 작업 진행률과 오류 상세 표시

완료 기준: 잘못 분류된 논문을 UI에서 수정하면 `.paperpack`과 인덱스가 일관되게 갱신된다.

### 4단계 — 자동화 안정화 (2일)

- 30초 주기 자동 스캔과 실패 백오프
- 앱 재시작 후 처리 상태 복원
- 대량 파일 처리 취소와 재개
- 로그 로테이션, 인덱스 무결성 검사
- PyInstaller 실행 파일 검증
- Organizer와 sPDF 의존성·OCR 모델의 중복 패키징 제거
- sPDF 업데이트 감지, 호환성 테스트와 submodule 갱신 PR 자동화

완료 기준: 100개 이상의 혼합 PDF를 연속 처리해도 중복 이동, 파일 유실, UI 정지가 없다.

### 5단계 — OCR 및 확장 기능 (선택, 2~4일)

- sPDF OCR 워커 연동 또는 독립 OCR 어댑터
- sPDF 선택 영역의 한국어 번역·요약, 페이지/좌표 복귀와 선택 영역 OCR
- DOI/arXiv 외부 보강 옵션
- BibTeX/RIS 내보내기
- 의미 검색 실험

### 6단계 — AI 실험정보 구조화와 자연어 질의 (마지막, 선택)

- 배지·세포주·시약·장비·데이터셋 구조화
- 필요한 경우에만 근거 청크·페이지 연결
- 라이브러리 전체를 모델에 넣지 않는 검색 후 질의 방식
- 로컬/클라우드 제공자별 정확도·비용·리소스 검증

완료 기준: 1.0 실사용 피드백으로 필요성이 확인되고, 기본 정리·검색 성능에 영향을
주지 않는 별도 선택 기능으로 동작한다.

## 11. 테스트 계획

### 단위 테스트

- 다운로드가 진행 중인 파일을 건너뛰는지
- GPU 없음, VRAM 미확인, 저RAM, 고사양 GPU 등 가상 사양별 추천 결과가 안전한지
- 현재 RAM/VRAM보다 큰 모델을 자동 추천하지 않는지
- 사용자 직접 선택이 재검사 후에도 유지되고 위험 경고만 표시되는지
- Ollama 중단·모델 없음·모델 삭제 상황에서 안전하게 AI 기능을 보류하는지
- 벤치마크 실패나 메모리 부족 결과가 추천 등급에 반영되는지
- 모델 미설치 상태에서도 PDF 보기·색인 UI가 실행되는지
- 다운로드 전 디스크 부족을 감지하고 pull 요청을 시작하지 않는지
- pull 진행·실패·재시도와 다운로드 후 digest·실제 크기 확인이 동작하는지
- 새 모델 검증 실패 시 기존 선택 모델이 유지되는지
- Organizer 제거 또는 모델 변경이 사용자의 승인 없이 Ollama 모델을 삭제하지 않는지
- 절전 모드에서 LLM·OCR 작업이 동시에 실행되지 않고 동시성 1을 지키는지
- CPU·RAM·VRAM·배터리 임계값에서 새 작업이 보류되고 회복 후 재개되는지
- 선택 항목 수동 분석이 현재 파일을 손상시키지 않고 정식 결과로 저장되는지
- 시간 제한 고성능 모드가 만료 후 자동으로 절전 모드로 복귀하는지
- 앱 강제 종료 후 대기열과 처리 단계가 중복 없이 복구되는지
- 수정한 `paper.json`이 원자적으로 저장되고 `library.json`·FTS가 즉시 갱신되는지
- 사용자 수정·잠금 필드가 AI 재분석으로 덮어써지지 않는지
- 유지·교체·목록 병합 결과와 필드 출처가 올바르게 기록되는지
- 과거 리비전 복원 후에도 새 이력이 남고 인덱스가 일치하는지
- 잘못된 원본 JSON과 library root 밖의 경로 입력이 저장되지 않는지
- 암호화·손상·텍스트 부족 PDF를 올바르게 분류하는지
- 느슨하거나 잘못된 모델 응답을 정규화·거부하는지
- 금지된 Windows 문자가 안전한 경로로 바뀌는지
- 동일 해시, 동일 DOI, 유사 제목을 각각 구분하는지
- 원본 PDF와 ResearchGate 표지 한 장이 추가된 PDF가 같은 work로 판정되는지
- 래퍼 페이지 수, 워터마크·머리말과 페이지 번호가 달라도 본문 지문이 유지되는지
- 첫 페이지가 정상 논문 본문인 PDF를 잘못 제외하지 않는지
- 앞부분은 같지만 결과·결론이 다른 확장판을 자동으로 합치지 않는지
- 텍스트가 부족한 문서에서 페이지 이미지 지문이 검토 후보만 만들고 자동 병합하지 않는지
- 검토 뒤 선택한 중복 PDF 또는 `.paperpack`만 같은 휴지통 작업으로 이동하는지
- 삭제 전 파일 해시가 바뀌거나 sPDF에서 열려 있으면 작업을 중단하는지
- 대표 파일 제거 전에 새 대표본 선택을 요구하고 마지막 파일 삭제를 막는지
- 휴지통 복원 시 PDF 또는 `.paperpack`·work 관계와 인덱스가 함께 복구되는지
- 중간 실패 시 일부 파일만 사라지지 않고 이전 상태로 롤백되는지
- 휴지통 영구 삭제가 별도 확인 없이 실행되지 않는지
- 같은 이름의 목적지 파일이 있을 때 충돌 없이 이름을 정하는지
- 이동 또는 JSON 확정 실패 시 원본이 복구되는지
- 잠금이 중복 처리를 막고 오래된 잠금만 회수하는지
- 손상된 통합 인덱스가 `.paperpack`으로 재구축되는지
- 한국어·영어 혼합 검색과 필터가 기대대로 동작하는지
- 배지 약어·정식명·상품명 변형이 같은 정규화 항목으로 검색되는지
- 구조화된 배지 정보가 올바른 페이지·청크 근거를 가리키는지
- 근거가 없는 실험정보 질문에 모델이 값을 만들어내지 않는지

### 통합 테스트

- 논문, 비논문, 스캔본, 암호 PDF, 중복 PDF가 섞인 입력 폴더 처리
- Ollama 미실행·시간 초과·비정상 JSON 응답 상황
- 처리 중 앱 강제 종료 후 재시작 복구
- 파일을 외부 탐색기에서 이동·이름 변경·삭제한 뒤 무결성 검사
- OneDrive 등 동기화 폴더를 목적지로 사용했을 때의 파일 잠금 충돌
- Organizer에서 sPDF를 반복해서 열고 닫아도 `QApplication` 충돌이나 창 누수가 없는지
- sPDF에서 열려 있거나 미저장 편집이 있는 PDF의 자동 이동이 보류되는지
- 최신 sPDF submodule에서 열기·탭·검색·OCR 브리지 계약이 유지되는지
- sPDF `APP_VERSION`과 포함된 submodule 커밋이 버전 화면과 빌드 기록에 일치하는지
- 다중 페이지 선택의 텍스트·페이지·bounding box가 Organizer 브리지에 정확히
  전달되고 선택 밖 본문이 AI 요청에 포함되지 않는지
- 선택 영역 번역·요약을 취소해도 sPDF 선택 상태와 PaperPack이 바뀌지 않는지

### 수동 인수 테스트

- 새 논문 다운로드 후 UI에 나타나고 올바른 폴더로 이동되는지
- 생성된 JSON을 메모장과 다른 도구에서 정상적으로 읽을 수 있는지
- 검색 결과를 더블클릭해 내장 sPDF로 바로 열고 검색·복사할 수 있는지
- 같은 PDF를 두 번 열 때 기존 sPDF 탭이 활성화되는지
- sPDF 선택 영역을 번역·요약하고 결과의 페이지 링크로 같은 위치에 돌아가는지
- 클라우드 제공자에서 선택 범위와 전송량을 확인하기 전에는 텍스트를 보내지 않는지
- ResearchGate판과 원본판이 한 논문 아래 다른 파일로 표시되는지
- 중복 비교 화면에서 보관본·제거본을 선택하고 제거 후 바로 복원할 수 있는지
- 분류 수정, 다시 분석, 이동 취소가 예상대로 동작하는지
- 100개 이상 라이브러리에서 검색·필터 반응이 즉각적인지
- 배지 검색 결과를 클릭했을 때 sPDF가 근거 페이지를 여는지
- 백그라운드 처리 중 sPDF 보기·검색·스크롤 반응이 저하되지 않는지
- 분석 큐에서 선택한 논문이 먼저 처리되고 정식 분석으로 표시되는지
- 배지·요약·태그를 수정한 직후 새 값으로 검색되고 앱 재시작 후에도 유지되는지
- AI 재분석 비교 화면에서 사용자 수정값 유지와 선택적 병합이 동작하는지

## 12. 품질·보안·개인정보 기준

- 기본 분석 엔드포인트는 `127.0.0.1`의 Ollama로 제한한다.
- OpenAI·Anthropic 분석과 외부 메타데이터 보강은 기본 비활성화하고 전송 항목을 UI에 명시한다.
- 클라우드 분석은 명시적 동의가 없으면 네트워크 요청을 만들지 않으며 제공자 간 자동 폴백을 하지 않는다.
- API 키는 Windows 자격 증명 저장소 또는 환경 변수에서만 읽고 설정·`.paperpack`·로그·Git에 넣지 않는다.
- OpenAI 요청은 저장 비활성화 옵션을 사용하고, 모든 제공자에는 필요한 텍스트만 최소 전송한다.
- 로그에 논문 본문 전체를 남기지 않는다. 파일명, 상태, 오류 코드 중심으로 기록한다.
- 경로 순회(`..`)와 절대 경로 주입을 막고 모든 목적지가 library root 내부인지 확인한다.
- 원본 삭제는 하지 않으며 이동도 분석·검증 완료 후에만 수행한다.
- 사용자가 지정한 라이브러리 밖의 파일을 일괄 수정하지 않는다.
- JSON에는 `schema_version`을 넣고 마이그레이션과 이전 버전 읽기를 지원한다.

### 12.1 공개 배포 전 보안 마감 목표

로컬 기본값과 Windows 자격 증명 저장소를 유지하되, 공개 배포 범위가 넓어지기
전에 다음 항목을 완료한다.

- 공통 비밀값 redactor를 분석 실패 진단, 분석 큐, UI 오류, 로그와 크래시 보고 등
  외부로 보이거나 저장되는 모든 예외 경로에 적용한다. 가짜 OpenAI·Anthropic 키를
  포함한 예외를 주입해 설정 JSON, `.paperpack`, 로그와 화면 어디에도 원문 키가
  남지 않는 통합 테스트를 둔다.
- Ollama·OCR·sPDF·`winget`·하드웨어 검사 등 모든 자식 프로세스에는 필요한 환경
  변수만 전달한다. 최소한 `OPENAI_API_KEY`와 `ANTHROPIC_API_KEY`는 공통
  `sanitized_child_environment()`를 거쳐 제거하며, 새 `subprocess` 호출도 같은
  정책을 통과하도록 테스트한다.
- 제거 마법사에 Windows 자격 증명 저장소의 Paper Organizer API 키 삭제 여부를
  별도 항목으로 표시한다. 기본값은 삭제로 두되, 설정·캐시·Ollama 모델·논문
  라이브러리 삭제와 범위를 분리하고 실행 전 정확한 대상을 보여준다.
- 공개 설치파일은 SHA-256 검증을 계속 수행하고, 배포 규모가 커지기 전 신뢰할 수
  있는 인증서와 타임스탬프를 사용해 Authenticode 서명한다. 자동 업데이트는 실행
  직전에 파일명·해시뿐 아니라 서명 유효성과 예상 게시자도 확인한다.
- 클라우드 키는 서비스별 전용 프로젝트 키와 최소 권한 사용을 안내하고 제공자
  콘솔의 지출 한도를 우선 권장한다. 앱의 월간 예산값은 실제 사용량 누적과 요청 전
  차단이 구현되기 전까지 강제 한도로 표현하지 않으며, 구현 후에는 재시도 비용까지
  포함해 한도 초과 요청을 중단한다.
- 클라우드 전송 미리보기에는 제공자·모델·전송 범위·예상 토큰과 함께 미공개·기밀
  논문은 로컬 Ollama 사용을 권장한다. 논문별 `클라우드 전송 금지`는 전역 동의보다
  항상 우선한다.
- CI에서 추적 파일의 비밀값 패턴 검사와 의존성 취약점 점검을 실행한다. 탐지된
  실제 키는 Git 기록에서 문자열만 지우는 것으로 끝내지 않고 제공자에서 즉시
  폐기·교체한다.

## 13. 주요 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 모델이 제목·저자·분류를 추측 | 원문 근거만 사용하도록 프롬프트 고정, 신뢰도 기준과 검토함 적용 |
| 카테고리가 계속 분열 | 허용된 상위 카테고리 목록, 별칭 정규화, 최대 2단계 폴더 |
| 다운로드 중 파일을 이동 | 시간+크기 안정성 검사와 파일 열기 확인 |
| 처리 중 종료로 PDF/JSON 불일치 | 잠금, 임시 파일, 원자적 교체, 롤백, 시작 시 복구 검사 |
| 같은 논문의 여러 판본 | 해시+DOI/arXiv+서지 유사도 단계적 판정 |
| ResearchGate 표지로 동일 논문을 놓침 | 래퍼 페이지 감지, 본문 다구간 지문과 work/edition/file 3단계 식별 |
| 유사한 확장판을 같은 논문으로 잘못 병합 | DOI·결론·다구간 지문 교차검증, 불일치는 자동 병합하지 않고 검토 |
| 잘못된 중복 판정으로 필요한 파일을 삭제 | 자동 삭제 금지, 상세 비교와 명시적 선택, 30일 앱 휴지통·복원 제공 |
| 삭제 도중 논문 파일과 색인 관계가 깨짐 | 단일 `.paperpack`, 매니페스트, 원자적 인덱스 갱신과 실패 롤백 |
| 스캔 PDF 분석 실패 | `needs_ocr`로 보존하고 후속 OCR 워커 제공 |
| 대규모 라이브러리 검색 저하 | compact `library.json`; 규모 증가 시 SQLite/FTS를 재생성 가능한 캐시로 추가 |
| 모델 교체로 결과 일관성 저하 | 모델명·프롬프트 버전 기록, 재분석 전 변경 내용 미리보기 |
| sPDF 결합으로 Organizer 시작이 느려짐 | sPDF와 OCR 런타임 지연 로드, 공개 API 외 직접 의존 금지 |
| 열린 PDF를 Organizer가 이동 | sPDF 활성 문서 조회, 저장되지 않은 변경 확인, 이동 잠금 |
| 하드웨어 감지 오류로 과도한 모델 추천 | 알 수 없는 값은 보수적으로 판정, 안전 메모리 여유 적용, 짧은 벤치마크 제공 |
| 모델 카탈로그가 오래됨 | 버전·검증일 표시, 내장 카탈로그 유지, 선택적 갱신과 수동 모델 입력 지원 |
| 설치본이 AI 모델 때문에 지나치게 커짐 | LLM 가중치는 번들하지 않고 설치 후 선택 다운로드, 모델별 크기와 디스크 여유 표시 |
| 모델 업데이트 실패로 AI가 동작하지 않음 | 새 모델 다운로드·테스트 후에만 활성화하고 기존 검증 모델 유지 |
| 앱 제거 후 Ollama 모델 가중치가 남아 공간을 차지함 | 제거 마법사에서 앱 관리 모델과 총 용량을 표시하고 명시적으로 선택하면 함께 삭제; 공유 가능성을 경고하고 PDF·JSON은 별도 보호 |
| 외부 Ollama가 리소스를 과다 사용 | 동시성 1, 작은 모델·컨텍스트, 임계값 일시정지와 짧은 모델 유지; 엄격한 제한은 전용 프로세스 옵션 |
| 고성능 모드를 끄지 않아 시스템이 계속 바쁨 | 시간·개수 제한 필수, 종료 후 절전 자동 복귀, 리소스 위험 시 강제 완화 |
| 즉시 요청이 진행 중 파일을 중단시킴 | 원자적 단계 저장, 현재 청크의 안전 지점에서만 우선순위 전환 |
| 사용자가 수정한 인덱스가 재생성 때 사라짐 | `.paperpack` 원본 수정 후 파생 인덱스 재생성, 필드 출처·잠금과 리비전 이력 유지 |
| 잘못된 수동 편집으로 JSON 손상 | 폼 우선, 고급 JSON 스키마 검증, 저장 전 스냅샷과 원자적 교체 |
| sPDF 최신 변경이 Organizer 통합을 깨뜨림 | submodule 커밋 고정, 자동 업데이트 PR, 브리지 계약 테스트; 메이저 변경은 수동 승인 |
| Organizer 내부 sPDF가 원본과 갈라짐 | submodule 내부 직접 수정 금지, 공용 변경은 sPDF 저장소에 먼저 반영 |
| API 키가 설정·로그·Git에 노출 | OS 자격 증명 저장소와 환경 변수만 사용, 비밀값 마스킹·커밋 전 검사 |
| 예외 메시지나 자식 프로세스 환경으로 API 키가 노출 | 모든 저장·표시 경로에 공통 redactor 적용, 모든 `subprocess` 환경에서 클라우드 키 제거 |
| 앱 제거 후 Windows 자격 증명 저장소에 키가 남음 | 제거 마법사에서 API 키 삭제를 별도 기본 선택으로 제공하고 대상과 결과 표시 |
| 변조되거나 게시자를 확인할 수 없는 설치파일 실행 | GitHub 자산 SHA-256 검증 유지, Authenticode 서명·타임스탬프와 실행 전 게시자 확인 |
| 의도하지 않은 논문 외부 전송 | 로컬 기본값, 클라우드 명시적 동의, 논문별 전송 금지와 전송 미리보기 |
| 클라우드 비용이 예상보다 증가 | 제공자 프로젝트별 지출 한도 우선, 호출 전 토큰·비용 추정, 재시도 포함 실제 사용량 누적과 한도 초과 중지 |
| 원격 모델 폐기·API 장애 | 모델 목록 새로고침, 안전한 재시도, 사용자 승인 없는 제공자·고가 모델 폴백 금지 |

## 14. 구현 우선순위와 결정 권고

1. 첫 버전은 **수동 검색 + 검토 후 이동**을 기본값으로 한다. 충분히 검증한 뒤 신뢰도 0.85 이상의 자동 이동을 켠다.
2. PDF별 `.paperpack`을 진실의 원천으로 하고 `library.json`은 파생 인덱스로 둔다.
3. 폴더 분류는 `분야/세부분야`까지만 사용하고 연도·태그는 JSON과 검색 필터로 관리한다.
4. 파일명은 기본적으로 원본을 보존한다. `연도 - 제목.pdf` 자동 이름 변경은 사용자가 켜는 옵션으로 둔다.
5. sPDF는 `vendor/spdf` submodule로 처음부터 저장소와 설치본에 포함하고, MVP에서는 안정적인 별도 자식 창 방식으로 연다. 중앙 탭 완전 내장은 후속 리팩터링으로 진행한다.
6. Paper Organizer와 sPDF는 같은 프로그램과 PDF 엔진을 공유하되 설정·데이터·모듈 경계를 분리한다.
7. 자동 OCR 분석과 외부 서지정보 조회는 안정적인 텍스트 PDF 처리 이후에 추가한다. 사용자가 sPDF에서 실행하는 기존 OCR은 처음부터 사용할 수 있다.
8. 로컬 AI 기본값은 특정 모델명으로 고정하지 않고 `자동(권장)` 프로필로 둔다. 추천 결과와 이유를 항상 사용자에게 보여준다.
9. 자동 감시는 기본적으로 `절전` 프로필을 사용하고, `균형/고성능`은 사용자가 요약 감시 옵션에서 명시적으로 선택한다.
10. 사용자가 수정하는 대상은 파생 `library.json`이 아니라 논문별 `.paperpack` 안 `metadata/paper.json`의 정식 필드다. 모든 사용자 수정은 AI 값보다 우선하고 이력에서 되돌릴 수 있어야 한다.
11. sPDF 업데이트는 원본 저장소 `main` 또는 버전 태그를 자동 감지해 테스트 PR로 반영한다. 배포본은 검증된 정확한 submodule 커밋에 고정한다.
12. PDF 파일 해시는 파일 중복에만 사용한다. 논문 중복은 래퍼를 제외한 본문 지문과 서지 식별자를 결합한 `work_id`로 관리한다.
13. 확인된 중복은 사용자만 제거할 수 있으며 기본 동작은 30일간 복구 가능한 앱 휴지통 이동이다. 자동 영구 삭제는 지원하지 않는다.
14. 요약 LLM은 설치본에 포함하지 않는다. 설치 후 사용자가 용량·사양·라이선스를 확인하고 선택한 모델만 한 번 다운로드해 재사용한다.
15. 요약 제공자는 로컬 Ollama가 기본이며 OpenAI·Anthropic은 선택 기능으로 둔다. 클라우드 사용에는 별도 API 키와 전송 동의가 필요하고 키는 JSON에 저장하지 않는다.
16. 검색의 기준은 AI 요약이 아니라 `.paperpack`에 보존한 페이지별 전체 추출 텍스트다. 요약·구조화 필드·임베딩은 검색 편의를 높이는 보조 데이터로 취급하며, 없어도 정확 검색과 FTS 재구축이 가능해야 한다.

## 14.1 0.4.0 구현 현황

- 표준 ZIP 기반 `.paperpack` v1 저장 계층을 추가했다. PDF 원본, 메타데이터 JSON,
  본문 색인 JSON과 리비전 이력을 한 파일에 보관하며 해시·CRC·안전 경로를 검증한다.
- `.paperpack`의 PDF 추출과 메타데이터 원자적 수정, 미래 확장 엔트리 보존을 지원한다.
- 신규 수집·라이브러리 편집·클라우드 편집의 쓰기 경로를 `.paperpack`으로 전환했다.
  기존 sidecar와 paperpack이 함께 있으면 같은 `file_id`의 paperpack을 우선한다.
- 입력 PDF 원본 유지/검증 후 삭제 설정과 유지한 원본의 반복 스캔 방지 영수증을 추가했다.
- sPDF는 paperpack 내부 PDF를 요청 시에만 검증·캐시 추출한다.
- `레거시 변환` 탭과 CLI에서 기존 PDF/sidecar를 일괄 paperpack으로 변환한다.
  기본은 기존 파일 유지이며 선택 시 전체 성공 후 앱 휴지통으로 이동·복원한다.
- 기존 sidecar 묶음을 일괄 변환하는 마이그레이션 UI와 복원 흐름을 제공한다.

- 다운로드 폴더를 기본 입력으로 제안하고 사용자 지정 입력 폴더를 저장한다.
- 저사양 300초·균형 60초·고성능 15초 프리셋과 5~3600초 사용자 지정 주기로
  두 번 안정성이 확인된 PDF만 분석한다.
- 발견된 파일은 원자적 `state/analysis-queue.json`에 등록하고 우선순위 변경,
  큐 기록 제거와 선택 항목 수동 분석을 지원한다.
- 논문 구조 휴리스틱, 본문 지문, DOI와 ResearchGate 래퍼 제거 지문을 검토 UI에 표시한다.
- 사용자가 색인을 수정하고 승인한 PDF만 `papers/분야/세부분야`로 이동한다.
- 확인된 중복 후보는 영구 삭제하지 않고 매니페스트가 있는 앱 휴지통으로 이동·복원한다.
- 라이브러리 paperpack의 제목·저자·연도·분류·태그·설명을 편집하고 내부 이력 보존 후 재색인한다.
- 저널명 또는 학회명을 `bibliography.venue`로 편집·검색·목록 표시하고 portable JSON에
  포함한다. 저널 영향력 평가는 외부 근거 연동 전에는 자동 추정하지 않는다.
- 선택한 OneDrive 폴더에 클라우드 편집용 단일 `portable-library.json`과 paperpack
  메타데이터·index·history·state의 `backup/` JSON 복사본을 분리해 저장한다.
- 마지막 동기화 기준선으로 한쪽 변경은 반영하고 양쪽 변경은 충돌 보고서에 남긴다.
- 충돌 화면에서 로컬·클라우드 JSON을 비교하고 사용자가 선택하며 클라우드 적용 전
  로컬 원본을 revision history에 보관한다.
- 버전과 제작자 표기가 있는 스플래시에서 백그라운드로 JSON 상태를 읽는다.
- **후속 점검:** 일부 설치 환경에서 시작할 때
  `QPixmap::scaled: Pixmap is a null pixmap` 경고가 출력된다. 설치본의 스플래시
  이미지 포함 여부와 실행 중 실제 자산 경로를 확인하고, 이미지 로딩에 실패하면
  빈 `QPixmap`에 `scaled()`를 호출하지 않고 단색 대체 화면을 사용하도록 보강한다.

## 14.2 0.5.0 구현 현황

- sPDF는 paperpack의 읽기 캐시가 아니라 별도 편집 작업 복사본을 연다.
- 사용자가 sPDF에서 저장한 뒤 명시적으로 적용하거나 작업 복사본만 폐기할 수 있다.
- 적용 전 기준 PDF SHA-256과 package 리비전을 함께 검사해 동시 수정 충돌을 차단한다.
- 편집본 적용은 임시 ZIP 생성, 전체 CRC·해시 검증, 원자적 파일 교체 순서로 처리한다.
- PDF 교체 리비전에는 이전·현재 PDF 해시와 변경 유형을 기록한다.
- 새 PDF 바이트에 맞춰 `file_id`, `edition_id`, 크기, 페이지 수와 본문 지문을 다시
  계산하고, 본문 색인과 AI 분석 결과는 stale 상태로 표시해 분석 큐에 재등록한다.
- 읽기 캐시와 편집 작업공간을 분리해 sPDF 저장이 paperpack을 암묵적으로 바꾸지
  않도록 했다.

## 14.3 0.6.0 구현 현황

- 기존 사용자와 신규 사용자는 첫 실행 완료 표식이 없으면 메인 화면 전에 시작 및
  종료 설정을 반드시 확인한다.
- Windows 로그인 자동 시작은 기본 꺼짐이며 현재 사용자 `HKCU Run` 항목만 사용해
  관리자 권한 없이 켜고 끈다. 레지스트리 또는 JSON 저장 실패 시 양쪽을 롤백한다.
- X 버튼 동작은 `시스템 트레이에서 백그라운드 유지`와 `완전 종료` 중 명시적으로
  선택하며, 설정 메뉴에서 언제든 다시 바꿀 수 있다.
- 트레이 메뉴에서 창 다시 열기와 완전 종료를 제공하고 진행 중 작업은 기존 종료
  보호 정책을 그대로 적용한다.
- 로그인 자동 실행은 `--background` 모드로 시작하되 백그라운드 닫기 설정과 시스템
  트레이를 사용할 수 있을 때만 숨기고, 그렇지 않으면 메인 창을 표시한다.

설치 프로그램 단계에서는 같은 선택을 설치 UI에도 노출하되, 설치 시 선택하지 않은
사용자에게는 첫 실행 화면을 생략하지 않는다. 제거 프로그램은 Paper Organizer의
`HKCU Run` 항목을 제거해야 하며 PDF, `.paperpack`, JSON, Ollama 모델은 건드리지 않는다.

## 14.4 0.7.0 구현 현황

- PyTorch나 모델 설치 없이 Windows CPU·논리/물리 코어, 전체/가용 RAM, NVIDIA
  GPU·VRAM과 Windows 그래픽 어댑터, Ollama 모델 저장 디스크 여유를 검사한다.
- 로컬 `127.0.0.1` Ollama의 버전과 설치 모델명·실제 크기·양자화를 읽되, 연결되지
  않아도 앱 시작과 PDF 기능에는 영향을 주지 않는다.
- 검증일과 라이선스·다운로드 크기·예상 실행 메모리·최소/권장 RAM이 포함된 오프라인
  모델 카탈로그를 설치본에 포함한다.
- `자동/속도/균형/품질/직접 선택` 프로필별로 Windows와 sPDF의 메모리 여유,
  현재 가용 RAM, 디스크와 GPU VRAM을 함께 평가해 추천 이유와 경고를 표시한다.
- 자동 프로필은 호환되는 설치 모델을 우선하고, 설치 모델이 없을 때만 다운로드
  후보를 추천한다. 현재 가용 RAM이 부족하면 실행 보류 경고를 표시한다.
- 추천 결과는 사용자가 `추천 모델 선택`을 누를 때만 모델명에 반영하며 다운로드,
  모델 교체 또는 삭제를 자동 실행하지 않는다.

## 14.5 0.8.0 구현 현황

- 별도 Ollama 모델 관리 화면에서 카탈로그 예상 다운로드 크기, 디스크 안전 여유,
  설치 후 실제 크기·파라미터·양자화를 함께 표시한다.
- 다운로드는 GUI를 막지 않는 작업 스레드에서 NDJSON 진행률을 표시하며 사용자가
  취소한 뒤 같은 모델을 다시 시도할 수 있다.
- 설치 완료 뒤 `/api/tags`에서 모델과 실제 용량을 확인하고, 짧은 구조화 JSON 생성
  테스트까지 통과해야 AI 설정의 모델 칸에 반영한다. 설정 저장 전에는 활성화하지 않는다.
- 기존 공유 Ollama 모델은 재다운로드하지 않고 같은 응답 검증 후 선택할 수 있다.
- 앱이 성공적으로 다운로드한 모델명만 설정에 기록하되 공용 Ollama 저장소의 소유권을
  주장하지 않는다. 삭제는 모델별 경고와 사용자 확인을 거친 명시적 작업으로만 실행한다.
- 활성 모델을 삭제하면 저장된 선택을 비우고, 삭제 후 설치 목록에 남아 있지 않은지
  다시 확인한다. 앱 제거 프로그램은 여전히 어떤 Ollama 모델도 자동 삭제하지 않는다.
- 모델 변경 API는 `127.0.0.1` 계열 로컬 HTTP에만 제한하며 원격 주소나 insecure pull을
  허용하지 않는다.

## 14.6 0.8.1 구현 현황

- `vendor/spdf` 고정 커밋을 sPDF 1.4.2에서 1.5.3으로 갱신했다.
- 손 도구·텍스트/영역 선택 도구, 즐겨찾기 툴바 동작, OCR 엔진 표시 개선과 Windows
  빌드 안정화 변경을 포함한다.
- sPDF의 공개 버전 상수와 설치 프로그램 버전이 모두 1.5.3인지 확인하고 Organizer
  브리지·전체 회귀 테스트 및 sPDF 상호작용 도구 테스트를 실행한다.
- Organizer 전용 패치를 submodule 내부에 추가하지 않고 sPDF 원본 `main`의 정확한
  커밋 `b0a5b99`를 그대로 고정한다.

## 14.7 0.9.0 구현 현황

- 설치·검증된 Ollama 모델 또는 사용자가 동의한 OpenAI·Anthropic 제공자를 실제 분석
  큐에 연결했다.
- 백그라운드 분석은 기본 활성화하되 동시성 1로 한 논문씩 처리하고, 절전/균형/고성능
  프로필에 따라 다음 작업 확인 간격만 조절한다.
- AI 또는 키가 준비되지 않으면 큐 항목을 선점하지 않고 대기하며, 앱 종료로 중단된
  `analyzing` 항목은 다음 실행 때 안전하게 `pending`으로 복구한다.
- 큐 화면에서 백그라운드 실행기 시작·중지, 선택 항목 즉시 분석과 실패 항목 재시도를
  실행할 수 있다.
- 분석 결과는 paperpack의 메타데이터와 provenance에 원자적으로 저장하되 사용자가
  이미 수정한 비어 있지 않은 요약 필드는 덮어쓰지 않는다.
- PyInstaller one-folder 실행본에 sPDF와 격리된 RapidOCR 한국어 워커를 포함하고,
  Inno Setup 기반 사용자별 Windows 설치·제거 프로그램을 추가했다.
- 설치 프로그램은 로그인 자동 시작을 선택 작업으로 제공하며 제거 시 앱 파일과 해당
  레지스트리 항목만 정리하고 사용자 논문·설정·공용 Ollama 모델은 보존한다.
- 빌드된 설치본을 격리 경로에 설치하고 GUI 실행 후 제거하는 스모크 테스트를 통과했다.

다음 우선순위는 1.0 설치본의 실제 사용자 테스트와 기본 텍스트 검색이다. AI 실험정보
구조화·근거 페이지 연결·자연어 질의는 마지막 선택 단계로 미룬다. 어떤 분석도 매번
라이브러리 전체를 LLM 컨텍스트에 넣지 않고 현재 논문 하나만 읽는다.

OneDrive 편집본은 PDF나 절대 로컬 경로를 포함하지 않는다. 클라우드 항목 삭제 또는
로컬 PDF가 없는 새 항목은 자동으로 원본을 만들거나 지우지 않고 충돌로 보고한다.
필드별 자동 병합과 여러 장치의 동시 쓰기 잠금은 후속 범위로 남긴다.

## 15. MVP 완료 정의

아래 조건을 모두 만족하면 첫 실사용 버전으로 본다.

- 최초 실행에서 Windows 로그인 자동 시작 여부와 X 버튼 동작을 사용자가 명시적으로
  선택하고, 이후 설정 화면에서 변경할 수 있다.
- 백그라운드 닫기를 선택하면 시스템 트레이에서 다시 열거나 완전 종료할 수 있고,
  트레이를 사용할 수 없는 환경에서는 창이 보이지 않은 채 남지 않는다.
- 사용자가 입력 폴더와 논문 보관 폴더를 지정할 수 있다.
- 앱이 논문 PDF와 비논문 PDF를 구별하고 불확실한 파일을 자동 이동하지 않는다.
- 앱이 CPU·RAM·GPU·VRAM과 설치된 Ollama 모델을 검사해 호환 모델을 추천하고 사용자가 변경할 수 있다.
- 사용자가 요약 제공자를 Ollama·OpenAI·Anthropic 중 선택하고 제공자별 모델과 키 등록 상태를 관리할 수 있다.
- 동의하지 않은 상태에서는 OpenAI·Anthropic으로 논문 텍스트가 전송되지 않고 API 키가 어떤 JSON·로그에도 기록되지 않는다.
- 세 제공자가 같은 요약 스키마를 만들며 사용한 제공자·모델·프롬프트 버전·토큰 사용량이 provenance에 기록된다.
- 모델 다운로드나 고사양 모델 실행은 예상 용량·위험을 표시한 뒤 사용자 승인으로만 진행된다.
- 모델이 없는 최초 설치 상태에서도 앱과 sPDF가 동작하며, 선택한 요약 모델만 진행률과 용량을 확인하며 설치 후 다운로드할 수 있다.
- 모델 교체는 새 모델 검증 후 이루어지고 사용하지 않는 모델은 사용자가 명시적으로 삭제할 수 있다.
- 자동 백그라운드 분석은 동시성 1과 저우선순위로 실행되며 리소스·배터리 조건이 나쁘면 일시정지된다.
- 분석 큐에서 특정 논문을 우선 처리하고 백그라운드 실행을 시작·중지할 수 있다.
- 집중 처리 모드는 만료되면 자동으로 절전 모드로 돌아가고 대기열은 재시작 후 복구된다.
- 제목·분류·요약·태그 등 인덱스 내용을 UI에서 수정하고 즉시 다시 검색할 수 있다.
- 사용자 수정값은 AI 재분석으로 자동 덮어쓰기 되지 않으며 비교·병합과 버전 복원이 가능하다.
- 정상 논문의 메타데이터와 한국어 설명이 유효한 JSON으로 저장된다.
- PDF와 JSON이 목적지의 단일 `.paperpack`에 함께 보관되며 실패 시 입력 원본이 보존된다.
- 동일 PDF를 두 번 처리하지 않는다.
- ResearchGate 표지가 붙은 PDF와 원본 PDF를 같은 work의 다른 파일로 인식하며 사용자가 대표본을 선택할 수 있다.
- 학회판·저널 확장판처럼 판단이 애매한 경우 자동 병합하지 않고 검토 대상으로 남긴다.
- 사용자가 확인한 중복 PDF 또는 `.paperpack`을 휴지통으로 옮기고 인덱스에서 제거하며, 보존 기간 안에 완전히 복원할 수 있다.
- 통합 인덱스를 삭제하거나 손상시켜도 재생성할 수 있다.
- 제목, 저자, 연도, 분야, 키워드, 요약으로 검색·필터할 수 있다.
- 검색 캐시를 삭제해도 `.paperpack`의 `metadata/paper.json`과 `content/content.json`으로 재구성할 수 있다.
- 인수인계용 일괄 반출에서 여러 `.paperpack`을 PDF로 추출하고, 전체 검증 뒤 명시적으로 선택한 경우에만 원본 패키지를 제거할 수 있다.
- 검색 결과의 PDF를 내장 sPDF로 즉시 열고 기존 보기·검색·복사 기능을 사용할 수 있다.
- 포함된 sPDF 버전과 커밋을 확인할 수 있고 sPDF 업데이트를 자동 감지·검증하는 워크플로가 동작한다.
- sPDF에서 열려 있거나 미저장 변경이 있는 파일은 Organizer가 자동 이동하지 않는다.
- 오류·OCR 필요·검토 필요 항목을 UI에서 확인하고 재시도할 수 있다.
- 핵심 파일 처리 테스트와 혼합 입력 폴더 통합 테스트가 통과한다.
