# Paper Organizer 개발계획

- 현재 버전: `1.8.1`
- 대상: Windows 10/11, Python 3.12+, PyQt5 데스크톱 앱
- 최근 변경 기록: `CHANGELOG.md`
- 현재 작업과 주의사항: `docs/HANDOFF.md`
- PaperPack 형식: `PAPERPACK_FORMAT.md`

이 문서는 앞으로의 방향과 완료 기준만 관리한다. 이미 구현된 버전별 세부 내역은
`CHANGELOG.md`, 작업 재현법과 단기 인수인계는 `docs/HANDOFF.md`에 둔다.

## 1. 제품 목표

다운로드한 PDF 중 학술 문서를 판별하고, 서지정보·분류·요약·본문 색인을 생성해
하나의 `.paperpack`으로 안전하게 보관한다. 사용자는 라이브러리에서 검색·편집·
재분석하고 내장 sPDF로 원문을 읽는다.

기본값은 로컬·보수적·복구 가능이어야 한다.

- 불확실한 문서는 자동 이동하지 않고 검토함에 둔다.
- 논문 본문은 기본적으로 PC 밖으로 보내지 않는다.
- AI가 없어도 수집, 정확 검색, PaperPack 관리와 sPDF 열기는 동작한다.
- 자동 삭제는 하지 않으며 사용자 승인을 받은 항목도 먼저 앱 휴지통으로 옮긴다.

## 2. 절대 원칙

1. `.paperpack`이 유일한 영구 원본이다.
2. `index/library.json`과 `index/search.sqlite`는 언제든 재생성 가능한 캐시다.
3. 필드 출처 우선순위는 `auto:regex < ai:* < user`다. 사용자가 수정하거나 잠근
   값은 AI가 덮어쓰지 않는다.
4. 모든 쓰기는 임시 파일 작성·검증 후 `os.replace`로 원자적으로 교체한다.
5. 파일 이동·제거는 정확한 대상과 해시를 확인하고 사용자의 승인을 받는다.
6. 삭제는 기본적으로 `trash/` 이동이며 원래 경로와 복원 정보를 남긴다.
7. `ui/` 밖에서는 PyQt5를 import하지 않는다.
8. sPDF 공용 변경은 sPDF 저장소에 먼저 반영하고 Organizer는 고정된 submodule
   커밋과 공개 브리지만 사용한다.
9. 클라우드 전송은 제공자·모델·범위를 보여준 뒤 명시적 동의를 받는다. API 키와
   논문 원문은 설정·로그·PaperPack에 기록하지 않는다.

## 3. 현재 구현 범위

1.8.1 기준으로 다음 기반 기능이 동작한다.

- 안정된 PDF 탐색, 학술 문서 판정, OCR 필요·중복·검토 상태 분리
- PDF, 메타데이터, 페이지별 본문, 변경 이력을 담는 원자적 PaperPack 저장
- PaperPack 기반 라이브러리·SQLite FTS 재구축과 한국어 자연어 검색어 확장
- 제목·저자·연도·저널·분류·태그·요약 편집과 사용자 필드 보호
- Ollama·OpenAI·Anthropic 공통 요약 스키마와 단일 분석 큐
- PC 사양·설치 모델 기반 Ollama 추천, 다운로드·검증·삭제 관리
- 분석과 번역의 모델 분리, 직렬 실행, 중지·재시도·재시작 복구
- 라이브러리 삭제의 앱 휴지통 이동·복원과 Windows 점유 오류 안내
- 내장 sPDF 열기·편집본 적용, HiDPI 렌더링과 submodule 업데이트
- 태그 기반 Windows 설치파일·체크섬·GitHub Release 자동 배포

세부 구현 이력과 버전별 변경은 `CHANGELOG.md`를 기준으로 한다.

## 4. 현재 우선순위

### P0. 리뷰 논문 전용 분석

상태: **구현 완료, 반복 실측 누적 중** (2026-08-01)

연구논문용 프롬프트가 리뷰 논문에 실험 방법과 결과를 억지로 채우지 않도록 문서
유형을 먼저 결정하고 전용 파이프라인을 사용한다.

- 특허 형식 판정 후 연구논문·리뷰논문을 분류한다.
- 리뷰 프롬프트는 검토 범위, 문헌 선택 기준, 주제 분류, 합의·상충 결과, 근거
  수준, 연구 공백과 향후 과제를 구조화한다.
- 연구논문과 리뷰논문의 정답표·점수·통과 기준을 분리한다.
- 사용자가 확정한 문서 유형은 이후 AI가 덮어쓰지 않는다.
- 기존 오분류 PaperPack은 후보만 보여주고 사용자 승인 후 이력을 남겨 교정한다.
- 서로 다른 완성 문서가 한 PDF에 묶이면 복수 문서로 표시하고 AI 요약하지 않는다.

완료 기준:

- 일반 논문이 특허 파이프라인에 들어가지 않는다.
- 리뷰 논문이 연구논문 전용 필드를 허위로 채우지 않는다.
- 세 유형의 회귀 테스트와 분리된 벤치마크가 통과한다.

구현 결과:

- 미국·PCT/WO·한국의 표제와 번호 조합만 특허로 확정하고, 나머지는 연구논문 또는 리뷰논문으로 분류한다.
- 리뷰논문은 첫 장 서지 추출, 섹션별 문헌 종합, 전체 종합의 전용 프롬프트를 사용한다.
- 라이브러리에 자동 재분류 후보만 표시하며 사용자가 문서 유형을 저장하면 `document.type`을 잠그고 history에 남긴다.
- 특허 표지 번호 또는 DOI·초록 표지가 둘 이상 반복되는 묶음 PDF는 `skipped_multi_document`로 기록하고 분석 큐에서 제외한다.
- 연구논문과 리뷰논문 rubric 및 평균을 분리했다. 리뷰 rubric은 개별 실험 재현보다 검토 범위·선정 방법·통합 결론 보존을 평가한다.

### P1. 경량 모델 벤치마크와 추천 갱신

상태: **동일 실논문 재측정 완료, 16GB 안정성 검증만 유지** (2026-08-01)

- Qwen3.5 2B·4B를 Qwen3 1.7B·4B와 같은 비공개 실논문, 프롬프트, 출력
  스키마로 비교한다.
- 서지정보 정확도, 구조화 성공률, 금지 환각, 재시도, 시간, tokens/s,
  RAM·VRAM과 실제 CPU/GPU 배치를 기록한다.
- 비공개 논문·원문·모델 출력은 저장소에 올리지 않는다. 익명 집계만
  `tests/benchmark/score_history/`에 게시한다.
- Qwen3.5 2B가 품질과 16GB 안정성에서 Qwen3 1.7B를 이길 때만 백그라운드
  추천을 교체한다. 4B도 같은 기준으로 수동 분석 추천을 조정한다.
- 공식 경량 체크포인트·라이선스·Ollama 지원이 확인되지 않은 Qwen4는 추가하지
  않는다.

현재 기준선은 다음과 같다.

| 역할 | 현재 기본 | 근거 |
|---|---|---|
| 백그라운드 기본 | Qwen3 1.7B | 리뷰 51.67점·해당 리뷰 서지 50점, 빠른 저비용 처리 |
| 수동 분석 기본 | Qwen3.5 4B | 리뷰 61.67점·해당 리뷰 서지 100점, 구체 결론 보존 우수 |
| 우선 다운로드 후보 | Qwen3.5 2B·4B | 합성 6편 평균 21.72·27.64점으로 추가 검증 필요 |
| 종합 추천 | Granite 4.1 3B | 실논문 품질 82.5/100, 평균 15.6초 |
| 정밀 상한 | Qwen3 8B | 기여·한계를 포함하는 고급 분석 |

리뷰 프롬프트 v4의 실리뷰 논문 재측정에서는 Qwen3 1.7B가 리뷰 51.67점·서지
50점·6.975초, Qwen3.5 4B가 리뷰 61.67점·서지 100점·14.391초였다. 두 모델 모두
형식·언어 재시도 0회였고 수동 QA에서도 근거 없는 체계적 검색법과 혼합 언어가 없었다. 백그라운드는 Qwen3 1.7B를 유지하고,
구체 결론과 서지 정확도를 우선하는 수동 분석은 Qwen3.5 4B를 사용한다. 원문·정답·
출력은 비공개이며 익명 결과만 `tests/benchmark/score_history/real_papers_qwen_comparison_20260801.json`에 둔다. 리뷰 자동 분석을 4B로 강제할지는 별도 정책 결정으로 남긴다.

점수와 시간은 특정 샘플·하드웨어의 비교값이며 일반 성능 보증이 아니다.

### P1. 분류 체계 보정

- 실제 오분류가 확인된 분야만 `paper_organizer/models/taxonomy.json`의 키워드를
  보강한다.
- AI 교정을 전제로 정규식 분류를 과도하게 복잡하게 만들지 않는다.
- 각 수정은 `tests/test_classifier.py`의 재현 사례와 함께 적용한다.

### P2. sPDF 선택 영역 번역·요약

상태: **공개 DTO·임시 결과 UI 구현 완료, 선택 영역 전용 OCR은 후속** (2026-08-01)

- sPDF가 선택 텍스트, 실제 PDF 페이지, bounding box와 문서 ID를 공개 DTO로
  전달한다.
- Organizer는 선택 영역만 현재 제공자에 보내며 sPDF 내부 객체를 직접 읽지 않는다.
- 결과는 임시 보기와 복사를 기본으로 하고 사용자 승인 없이 PaperPack의 정식
  분석 필드를 수정하지 않는다.
- 클라우드 전송 동의, 선택 영역 OCR의 명시적 실행, 취소와 원문 위치 복귀를
  통합 테스트한다.

### P2. 공개 배포 보안 마감

상태: **비밀값·자식 환경·서명 검증 구현 완료, 실제 인증서 운영 설정 필요** (2026-08-01)

- 오류·로그·분석 큐·크래시 보고에 공통 비밀값 redactor를 적용한다.
- Ollama·OCR·sPDF·winget 등 모든 자식 프로세스 환경에서 클라우드 API 키를
  제거한다.
- 제거 마법사에서 Windows 자격 증명 삭제를 설정·모델·논문 삭제와 분리한다.
- 설치파일에 Authenticode 서명과 타임스탬프를 적용하고 업데이트 실행 전 게시자
  검증을 추가한다.
- 실제 사용량 누적과 요청 전 차단이 구현되기 전에는 앱 예산을 강제 한도로
  표현하지 않는다.

## 5. 보류 기능

실사용 피드백으로 필요성이 확인되기 전에는 다음 기능을 우선순위에 넣지 않는다.

- 배지·세포주·시약·장비 등 실험정보의 전면 구조화
- 라이브러리 전체 임베딩과 의미 검색
- 근거 청크 기반 자연어 질의의 고도화
- DOI·arXiv 외부 서지정보 자동 보강
- BibTeX·RIS 내보내기
- 여러 PC의 동시 편집과 서버형 동기화
- Zotero 대체 기능

이 기능을 추가해도 검색의 기준 데이터는 PaperPack의 추출 원문이고, 임베딩·FTS·
구조화 필드는 재생성 가능한 보조 데이터여야 한다.

## 6. 구조와 책임

```text
paper_organizer/
  core/          PaperPack, PDF 판정·추출, 분류, 검색 색인
  application/   라이브러리 흐름, 분석 큐, 요약·번역 서비스
  providers/     Ollama, OpenAI, Anthropic 어댑터
  infra/         설정, 하드웨어, Ollama 런타임·설치
  ui/            PyQt5 화면과 대화상자
  integrations/  sPDF 공개 브리지
  models/        분류 체계와 모델 카탈로그
vendor/spdf/     고정 커밋의 sPDF submodule
tests/           표준 unittest와 오프라인 가짜 제공자 테스트
```

핵심 경계:

- `.paperpack` 쓰기는 `core/paperpack.py`의 검증·원자적 교체 경로를 사용한다.
- AI 서지정보 적용과 필드 우선순위 판단은 `_apply_ai_bibliography()`에 모은다.
- SQLite 연결은 `contextlib.closing`으로 닫아 Windows 파일 점유를 남기지 않는다.
- 정렬 가능한 UI 항목은 행 번호가 아니라 `Qt.UserRole`의 안정 ID로 찾는다.
- 요약 스키마 변경은 세 provider와 모든 픽스처를 함께 갱신한다.

## 7. 처리 흐름

```text
안정된 PDF 발견
  → 유효성·암호·텍스트/OCR 필요 판정
  → 파일 해시와 본문 지문으로 중복 후보 확인
  → 학술 문서·특허 형식 판정
  → 검토 또는 자동 PaperPack 생성
  → 첫 페이지 서지정보 추출
  → 문서 유형별 AI 분석과 스키마 검증
  → 사용자 필드를 보존해 PaperPack 원자적 갱신
  → library.json·search.sqlite 재생성
  → 라이브러리 검색·편집·sPDF 열기
```

실패 시 입력 PDF와 기존 PaperPack을 유지한다. 파일 이동이나 휴지통 작업은 패키지,
큐와 인덱스를 함께 갱신하며 중간 실패 시 롤백한다.

## 8. 검증 정책

모든 변경은 다음을 만족해야 한다.

- 버그 수정은 재현 테스트를 먼저 추가한다.
- core·application 테스트는 PyQt5 없이 실행된다.
- 외부 네트워크를 호출하지 않고 가짜 HTTP 클라이언트를 주입한다.
- Windows 파일 잠금, 중간 실패와 재시작 복구를 포함한다.
- 파일 이동·삭제 테스트는 임시 라이브러리와 휴지통만 사용한다.
- 릴리스 전 전체 테스트를 실행한다.

```powershell
.venv\Scripts\python -m unittest discover -s tests
```

고위험 변경의 추가 완료 기준:

| 변경 | 필수 검증 |
|---|---|
| PaperPack 쓰기 | CRC·해시·스키마·원자적 교체·롤백 |
| 파일 이동·삭제 | 해시 재확인·점유 처리·휴지통 복원·인덱스 일관성 |
| AI 스키마 | 세 provider·축소/고급 경로·사용자 필드 보호 |
| sPDF 연동 | 고정 submodule·브리지 계약·열린 문서/미저장 편집 보호 |
| 모델 추천 | 저RAM·GPU 없음·설치 실패·사용자 선택 보존 |
| 클라우드 | 동의 없음 요청 0건·키 비노출·자동 제공자 전환 금지 |

## 9. 릴리스 기준

- 버전은 `x.y.z` 규칙을 사용하고 모든 활성 표기를 함께 갱신한다.
- 전체 테스트와 Windows 설치·제거 스모크 테스트가 통과한다.
- `.paperpack` 및 이전 버전 설정의 하위 호환성을 확인한다.
- `CHANGELOG.md`에 사용자 영향과 마이그레이션 주의를 기록한다.
- 태그 `vX.Y.Z` 푸시로 GitHub Actions가 버전별·`latest` 설치파일과 SHA-256을
  게시한다.
- 작업트리가 깨끗하고 코드·패키지·설치 프로그램·태그 버전이 일치해야 한다.

## 10. 완료 정의

Paper Organizer의 핵심 기능은 다음 조건이 계속 유지될 때 완료 상태로 본다.

- 파일 유실 없이 PDF가 검증된 PaperPack으로 보관되고 복원된다.
- 캐시를 삭제해도 라이브러리와 검색을 PaperPack에서 다시 만든다.
- AI가 없거나 실패해도 원문 검색과 PDF 열기가 가능하다.
- AI 분석은 근거 없는 서지정보를 저장하지 않고 사용자 편집을 보존한다.
- 불확실한 판정·중복·OCR 필요 항목은 자동 삭제·병합되지 않는다.
- 백그라운드 작업은 직렬 실행되고 앱 재시작 후 안전하게 이어진다.
- 클라우드는 동의한 범위만 전송하며 키와 원문을 로그에 남기지 않는다.
- 내장 sPDF로 원문을 열 수 있고 열린 문서의 이동·삭제를 안전하게 막는다.
- Windows 설치, 앱 내 업데이트와 제거가 논문 라이브러리 및 공용 Ollama 모델을
  사용자 승인 없이 건드리지 않는다.
