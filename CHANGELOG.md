# 변경 기록

이 프로젝트의 주요 변경 사항을 버전별로 기록합니다.

## [Unreleased]

## [2.4.0] - 2026-08-31

### 추가

- Ollama 없이 앱 전용 GGUF 모델을 다운로드하고 GPU·CPU에서 실행할 수 있는 내장 로컬 AI를 추가했습니다.
- NVIDIA GPU용 CUDA는 필요할 때 AI 설정에서 선택 다운로드할 수 있습니다. CUDA를 사용할 수 없으면 Vulkan·CPU로 전환합니다.
- 모델·API 키 없이 서지와 분류만 정리하는 모드를 추가했습니다. 기존 요약과 직접 수정한 정보는 보존합니다.
- 모델 다운로드·관리 화면에서 모든 후보 모델을 직접 다운로드할 수 있도록 했습니다.

### 변경

- 기존 Ollama 사용자에게 내장 로컬 AI 전환과 기존 모델 파일의 수동 정리 안내를 한 번 표시합니다.
- 내장 로컬 AI 설정 화면에서 앱 모델 폴더 상태를 표시합니다.

### 수정

- 내장 로컬 AI 모델 다운로드 버튼이 비활성화되던 문제를 수정했습니다.
- 요약 엔진 옵션의 체크박스 행 배경색이 주변 패널과 다르게 보이던 문제를 수정했습니다.

## [2.3.0] - 2026-08-20

### 추가

- AI가 제목·저자·연도·저널/학회 서지를 뽑은 뒤 PubMed·Crossref로 자동 재검증하도록 변경
- 선택 논문만 PubMed·Crossref로 다시 확인하는 서지 재검증 버튼 추가
- 제목·저자/발명자·연도·저널/학회 이상값과 필드 출처를 내부 진단 정보로 기록

### 변경

- AI 요약으로 이미 온라인 서지 확인을 거친 논문에서는 서지 재검증 버튼을 숨김

## [2.2.0] - 2026-08-14

### 추가

- PubMed·Crossref 기반 외부 서지 검증을 추가해 약한 제목·저자·연도·저널 정보를 보강
- 요약 감시 옵션에서 연구분야를 선택하면 연결된 세부분야를 확인하고 직접 추가·삭제할 수 있는 관리 화면 추가

### 변경

- sPDF 서브모듈을 v1.10.1 커밋으로 갱신
- sPDF 내장 호출을 업데이트 비활성화 경로로 전환
- 연구논문과 리뷰논문 요약 프롬프트 버전 표시 및 저장 ID를 분리
- 요약 감시 옵션을 감시 설정과 연구분야 관리 중심으로 재구성하고 시작/종료 설정을 통합
- 라이브러리 작업 명령을 리본 메뉴와 오른쪽 클릭 메뉴로 정리하고 PaperPack 관리 항목을 묶음
- 요약 엔진 옵션에서 Ollama 모델 관리 화면과 중복되던 후보 상세 목록을 제거
- 새 PDF 수동 탐색은 검색 버튼을 눌렀을 때만 시작하고 불필요한 후속 재검사를 줄임
- 로컬 AI 실행 전 시스템 메모리 여유 기준을 0.5GB로 완화
- AI 추천 연구분야를 분석 결과 저장 시 자동 반영하고 색인 편집 저장 시 재요약 여부를 선택하도록 변경
- 라이브러리 표의 열 순서와 표시 여부를 사용자 설정으로 저장하도록 변경
- 라이브러리 검색 시 `검색 위치` 열을 앞으로 이동하지 않고 사용자가 정한 열 배치를 유지하도록 변경
- 라이브러리와 설정 화면의 버튼·메뉴·패널 UI를 정리
- 새 PDF 검색이 멈췄을 때 검색 워커를 강제 종료할 수 있는 버튼 추가
- 향후 UI 폰트 검토를 위해 Pretendard 파일과 라이선스를 앱 자산에 포함

### 수정

- 분석 큐와 새 PDF 팝업의 토글·닫기·메인 창 종속 동작을 안정화
- 분석 큐가 비어 있을 때 상태 표시줄 진행바가 움직이던 문제 수정
- 한국어 특허의 발명자 필드가 깨지거나 잘못 합쳐지던 문제 수정
- Fluent UI 적용 후 남은 버튼·입력·패널 정렬과 여백을 조정

### 알려진 문제

- Ollama 미설치 상태에서 WindowsApps 실행 별칭을 실제 설치로 오인해 App Installer가 뜰 수 있음

## [2.1.0] - 2026-08-11

### 변경

- sPDF 서브모듈을 공식 v1.9.0 태그로 갱신
- 새 PDF 탐색과 분석 큐를 별도 팝업으로 분리하고 라이브러리 작업 버튼을 한 줄로 정리
- 라이브러리 문서 수를 검색줄 제목 옆에 표시해 세로 공간을 줄임

### 수정

- 새 PDF 팝업을 열 때 자동 검색을 시작하지 않고 창 안의 검색 버튼을 눌렀을 때만 탐색하도록 수정

## [2.0.1] - 2026-08-11

### 변경

- 설치 앱의 자동 업데이트 확인 타이머를 하루 한 번만 실행하도록 조정
- 자연어 논문 검색 창에 실행 중인 AI 검색·답변 생성을 정지하는 버튼 추가

### 수정

- 분석 큐 팝업이 열린 상태에서 메인 창을 닫거나 숨기면 팝업도 함께 닫히도록 수정

## [2.0.0] - 2026-08-11

### 변경

- PyQt 앱 전반에 Fluent UI 기반 디자인 적용
- sPDF 서브모듈을 공식 v1.8.0 태그로 갱신
- 앱, 패키지, 설치 관리자 버전을 2.0.0으로 갱신

## [1.10.2] - 2026-08-09

### 수정

- 벤치마크 테스트의 가상 모델 경로가 개인정보 경로 보안 검사에 걸리던 오탐 수정

## [1.10.1] - 2026-08-09

### 수정

- 모든 AI 추론 작업을 하나의 공통 실행 큐에서 직렬 처리하고, 실행 중인 작업이
  끝난 뒤 `자연어 검색 > 선택 영역 AI > 수동 분석·번역 > 모델 검증 > 백그라운드
  분석` 순서로 대기 작업을 실행하도록 변경

## [1.10.0] - 2026-08-09

### 추가

- 라이브러리 검색 결과에 검색어가 제목·요약·서지·본문 중 어디에서 발견됐는지와
  본문 페이지를 표시하고, 본문 일치 문맥을 툴팁으로 제공
- AI 설정에서 자동 백그라운드 분석과 사용자가 요청한 수동 분석의 작업 간격을
  각각 초 단위로 설정하는 옵션 추가

### 수정

- Abstract가 없는 서지 전용 백그라운드 분석이 기존 정상 요약을 빈 값으로
  덮어쓰지 않도록 보존
- 첫 페이지에 Abstract가 없고 두 번째 페이지에 실제 논문 서지가 있는 래퍼 PDF는
  두 번째 페이지에서 제목·저자·연도·저널을 추출
- `Abstract` 표제와 첫 문장이 같은 줄에 있는 논문도 서로 다른 DOI 표지가 반복되면
  복수 문서 묶음으로 판정해 자동 보관과 AI 요약에서 제외
- 라이브러리의 선택·전체 재요약 요청은 백그라운드 설정과 무관하게 수동 모델과
  수동 분석 간격을 사용하는 즉시 분석으로 처리
- 출판사 PDF가 제목 단어 사이 공백을 누락해 추출해도 AI가 복원한 정상 제목을
  검증하고, 언더스코어로 이어진 파일명 메타데이터는 실제 논문 제목보다 우선하지 않게 보정
- 논문 제목이 첫 페이지에서 여러 줄로 줄바꿈되면 같은 크기의 연속 제목 줄을
  합치고 저자·소속·초록 줄이 시작되는 지점에서 중단
- 여러 페이지에서 같은 위치에 반복되는 러닝 헤더와 `Results` 같은 절 표제는
  제목 후보 및 AI 서지 결과에서 제외하고 첫 페이지의 다음 후보를 다시 탐색하되,
  래퍼 표지와 실제 논문 첫 장에 각각 나온 제목은 반복 헤더로 간주하지 않음
- `Abstract` 표제 없이 첫 페이지에 초록 성격의 머리말을 두는 Scientific Reports
  형식은 두 번째 페이지를 래퍼 후속 표지로 오인하지 않고, 제목 아래 저자 블록의
  소속 번호를 제거해 전체 저자 이름을 결정적으로 추출
- AI가 저널 로고를 짧은 제목으로 반환하거나 본문 문장을 저자 목록으로 반환하면
  첫 페이지 레이아웃에서 확인한 제목·저자를 우선하고 본문형 저자 항목을 거부
- 특허 INID 코드가 한 줄에 합쳐지거나 `[ 54 ]`처럼 괄호 안 공백을 포함해도
  `(54) 발명의 명칭` 블록만 제목으로 추출하고 뒤의 분류·요약 필드를 분리
- 한국 특허의 발명자·출원인 INID 블록에서 주소와 우편 정보를 제외하고 이름만
  가져오며, 기존 특허도 재조회할 때 정규식 서지정보로 보정
- 선택 영역 번역·요약의 원문, 실행 버튼, 결과를 별도 창으로 옮겨 라이브러리
  상세 패널을 간결하게 정리하고, sPDF에서 텍스트를 선택하면 해당 창을 자동 표시
- 선택 영역 AI 창을 sPDF의 빈 옆 공간에 배치하고 공간이 없으면 두 창을 좌우로
  분할하며, 제목 표시줄의 불필요한 `?` 도움말 버튼 제거
- 라이브러리 더블클릭은 sPDF만 열고 `sPDF + AI` 명령에서만 독립 선택 영역
  번역·요약 창을 함께 열며, 텍스트 선택 중에는 sPDF가 앞에 유지되도록 변경
- 로컬 AI 번역은 최소 4B 모델만 허용하고 설정 화면에서 8B 이상을 권장
- 중복 검토 화면과 라이브러리에 선택한 원본 PDF 또는 PaperPack을 앱 휴지통을
  거치지 않고 영구 삭제하는 버튼과 복구 불가 확인 절차 추가
- 라이브러리 항목을 완전 삭제하면 목록 캐시에 숨은 항목을 포함해 PDF 해시가 같은
  모든 PaperPack을 함께 제거하고, 재요약은 동일 PDF당 한 번만 요청하도록 변경
- 라이브러리 표와 상세 패널의 폭을 끝까지 조절할 수 있게 하고 제목 열도 직접
  줄일 수 있도록 변경
- 창 닫기 동작 설정과 관계없이 앱 실행 중에는 시스템 트레이 아이콘을 항상 표시
- sPDF를 공식 1.7.3 태그에 고정하고 `main` 브랜치 원격 추적 설정을 제거해
  명시적인 프로젝트 업데이트 없이 sPDF 버전을 확인하거나 갱신하지 않도록 변경

## [1.9.1] - 2026-08-03

### 수정

- 설치 앱을 트레이에 계속 실행해도 매시간 일정 상태를 확인해 24시간마다 GitHub
  업데이트를 검사하고, 네트워크 실패는 완료로 기록하지 않아 한 시간 뒤 재시도
- 요약 감시 옵션에 하위 폴더 포함 설정을 추가하고, 재귀 감시 시 라이브러리가
  감시 폴더 안에 들어가 다시 수집되지 않도록 경로 검증
- 제외 파일 복원 창에서 제외 동작과 판정 근거를 별도 열과 선택 상세 영역으로
  표시해 긴 사유도 바로 확인 가능
- AI 번역·분석 완료로 라이브러리를 갱신해도 현재 선택, 다중 선택, 정렬과 스크롤
  위치를 PaperPack 경로 기준으로 유지
- 모든 앱 대화상자에서 실제 기능이 없는 Windows 제목 표시줄 `?` 버튼을 공통 제거
- 스플래시 이미지를 읽지 못하면 null pixmap을 확대하지 않고 단색 대체 화면 사용
- 스플래시 제작자 표기를 작게 줄여 이미지 오른쪽 아래에 배치

## [1.9.0] - 2026-08-01

### 추가

- 미국·PCT/WO·한국 번호 형식 기반 특허 판정과 연구논문·리뷰논문 사전 분류,
  리뷰 전용 섹션 종합 프롬프트 및 분리 벤치마크 기준
- 라이브러리 문서 유형 확정·잠금과 기존 PaperPack 자동 재분류 후보 안내
- 서로 다른 특허 표지나 DOI·초록 표지가 반복되는 PDF를 복수 문서 묶음으로
  표시하고 AI 요약·재요약 대기열에서 제외
- sPDF 1.7.1 기반 공개 선택 영역 DTO와 읽기 순서 연속 선택, 선택 텍스트만 보내는
  임시 번역·요약·복사 UI

### 수정

- 저장 화면에서 실제로 바뀐 필드만 `user`로 잠그고, 그대로 저장한 자동 서지는 AI 교정 가능 상태 유지
- 실리뷰 논문에서 섹션별 구체 대상·메커니즘·전략·응용과 최종 결론을 보존하고,
  명시되지 않은 체계적 검색법을 만들지 않도록 리뷰 프롬프트를 보정
- Ollama 구조화 분석을 `temperature=0`, `seed=0`으로 재현 가능하게 만들고,
  영어 원문 결과의 한중일 문자 혼입과 근거 없는 정식 리뷰 방법을 검증·정리
- OCR·하드웨어·Ollama·업데이트 자식 프로세스에서 클라우드 API 키를 제거하고 오류 문자열에 공통 redactor 적용
- 제거 시 Windows 자격 증명 삭제를 별도 기본 해제 옵션으로 제공하고, 앱 예산을 강제 한도로 오인하지 않게 표시
- Qwen3.5 실논문 비교 결과에 따라 백그라운드는 Qwen3 1.7B, 수동 분석은 Qwen3.5 4B로 역할 분리
- Granite 4.1 3B를 현재 4B급 본문 요약 조건으로 다시 측정해 빠른 수동 대안으로
  내리고, 연구·리뷰·서지 정확도가 더 높은 Qwen3.5 4B를 종합 추천 1순위로 변경
- `a review of` 제목과 `this paper reviews` 초록 표현은 리뷰 후보로만 판별하고,
  제목·초록 AI 2차 분류가 확정한 유형으로 전용 프롬프트를 선택하도록 보정
- 비공개 벤치마크 논문을 추가해도 기존 익명 문서 ID가 바뀌지 않도록 보정
- 연구논문 요약에서 배경·절차보다 실험 설계, 비교군, 종점, 정확한 수치·단위와
  명시된 한계를 먼저 보존하도록 연구 프롬프트 v10으로 개선
- `Results and discussion` 복합 절 제목을 인식하고, 방법·참고문헌 수치를 제외한
  정량 결과 문장을 우선순위화해 최종 계층 종합 단계에 근거 후보로 직접 전달
- AI 서지 제목 앞에 붙은 문서 유형 표지를 제거하고, 접수·승인·본문 인용연도는
  발행연도로 저장하지 않도록 첫 페이지 필드별 문자열 위치 검증 강화
- `Journal Pre-proof`, `Accepted manuscript`, `Article in press` 등 출판사 교정본
  표지만 AI 입력에서 제거하되 본문의 `proof-of-concept`와 수학적 proof는 보존
- Qwen3 1.7B는 본문 구역별 종합을 중단하고 검증된 서지정보와 논문 자체
  Abstract만 정리하며, Abstract가 없으면 서지정보만 저장하도록 역할 제한
- 요약 모델 변경 시 실행 중인 Ollama를 재시작하지 않고 그대로 사용하며, 서버가
  꺼져 있을 때만 창 없는 `ollama serve`를 시작하도록 변경. GPU 설정 적용처럼
  재시작이 꼭 필요한 경우에도 Ollama 트레이 앱 대신 숨김 서버만 실행
- 라이브러리에서 sPDF로 여는 PaperPack 편집본에 고정 `working.pdf` 대신
  PaperPack에 보존된 원본 PDF 파일명을 사용하고, 기존 편집본도 변경 내용을
  잃지 않도록 새 이름으로 자동 이전

## [1.8.1] - 2026-07-30

### 추가

- 로컬 Ollama 요약 모델을 `백그라운드 모델`과 `수동 요약 모델`로 분리하고,
  백그라운드 모델만 선택적으로 메모리에 상주시킬 수 있는 체크 옵션 추가
- 1~2B 백그라운드, 3~4B 수동 정밀 요약, 8B 이상 고급 분석으로 모델 추천
  용도를 구분하고 Granite 3.3 2B 후보 추가
- 비공개 실논문 벤치마크에서 원문·제목·정답·프롬프트를 제외한 익명 점수 기록을
  Git으로 누적할 수 있는 공개 도구와 점수 이력 추가

### 수정

- 125% 이상 HiDPI에서 새 PDF·분석 큐의 긴 버튼 행이 메인 창 최소 폭을
  화면보다 크게 만들던 문제를 여러 줄 격자 배치로 수정했습니다.
- 로컬 AI 메모리 안내를 8GB급 비지원, 16GB급 시스템 여유 1GB,
  24GB 이상 자유 선택으로 단순화하고 기본 백그라운드 추천을 Qwen3 1.7B로
  조정했습니다.
- 활성 Ollama 모델이 바뀌면 이전 모델의 RAM·VRAM을 확실히 해제하도록
  Ollama를 항상 재시작합니다.
- 가용 메모리가 선택 모델의 예상 사용량과 시스템 안전 여유보다 적으면 분석을
  시작하지 않고 대기열에 현재·필요 메모리를 사유로 표시하며, 메모리가 회복되면
  자동으로 다시 시도합니다.
- 구버전에서 문서 유형 머리말인 `Research Article`이 사용자 제목으로 잘못
  잠긴 PaperPack을 실제 첫 페이지 제목으로 안전하게 복구하되, 사용자가 직접
  같은 제목으로 고친 이력이 있으면 그대로 유지
- 라이브러리 제거를 영구 삭제 대신 복원 가능한 앱 휴지통 이동으로 바꾸고,
  Windows 미리보기·색인기의 일시적인 파일 점유는 짧게 재시도한 뒤 실패 원인을 안내
- Ollama 모델 다운로드의 상세 상태를 진행 막대 위에 분리해 모델명·단계·전송량·
  속도·남은 시간을 선명하게 표시하고, 진행 막대에는 퍼센트만 표시하며 좌우 여백 보강
- 저장된 내장 GPU 허용 설정을 앱 시작 때 Windows와 현재 프로세스에 다시 동기화하고,
  검증 후 CPU로 폴백한 경우 Ollama 재시작이 필요하다는 안내를 명확히 표시
- Paper Organizer가 직접 시작한 Ollama 서버를 앱 정상 종료 때 함께 정리해,
  다음 Ollama 트레이 실행이 11434 포트 충돌로 반복 종료되는 문제 방지
- 내장 GPU 허용 옵션을 변경해 저장하면 다른 Ollama 작업 중단 여부를 확인한 뒤
  트레이와 서버를 백그라운드에서 재시작해 새 환경값을 즉시 적용
- 전용 VRAM이 없는 16GB PC의 3~4B 계층형 요약은 기본 컨텍스트를 8K로 낮춰,
  iGPU 공유 메모리 적재 후 앱에 남는 RAM이 지나치게 줄어드는 현상 완화
- 자동 분석은 Qwen3 1.7B 같은 경량 모델을 사용하고, 사용자가 큐에서 직접 실행한
  분석과 AI 번역은 별도의 수동 모델을 사용하도록 처리 경로 분리
- 모델을 바꿔 저장할 때 여러 번 재시작하지 않고 두 모델 선택을 한 번에 적용한 뒤
  Ollama를 재시작하도록 정리
- 실제 논문 4편 비교에서 Qwen3 1.7B가 평균 27.81점, 연구논문 평균 33.75점,
  평균 97.13초를 기록해 같은 환경의 Granite 1B·2B보다 백그라운드 용도에
  적합하다는 안내 반영

## [1.8.0] - 2026-07-29

### 추가

- 비공개 실논문 벤치마크에서 82.5점을 기록한 `granite4.1:3b`를 일반 논문
  요약 1순위로 추가하고, 모델 관리 화면에 후보별 완료율·품질 점수·평균 시간·
  서지정보 입력 재시도와 강점, 측정 환경(`NVIDIA GeForce RTX 3080 Ti`)을 표시
- 같은 조건에서 80.8점을 기록한 `qwen3:4b`를 수치 보존과 간결한 구조화 추출에
  강한 2순위로 표시
- Ollama 모델 실행은 내장 GPU Vulkan을 포함한 GPU 우선을 기본으로 하고, 사용할
  수 없으면 CPU로 대체하도록 설정하며 변경 시 재시작 필요 여부를 안내
- Ollama 사양 검사 결과에 현재 적재된 모델의 CPU·GPU·혼합 실행 상태를 표시
- 모델 설치 후 간이 서지정보 입력 검증에서 실제 처리 장치와 입출력 속도를 측정해 저장하고
  다음 모델 선택과 정식 벤치마크에서 참고할 수 있도록 표시
- 라이브러리를 첫 번째 시작 화면으로 바꾸고, 두 번째 탭에서 새 PDF 검토와
  분석 큐를 한 흐름으로 함께 확인
- 라이브러리에서 유형/출처를 제거한 대신 분석 버전·번역 상태·날짜 전용 열을
  배치하며 머리글 정렬과 정렬 후 안전한 다중 작업을 지원
- AI 번역을 분석 큐의 단일 작업자에 통합해 요약과 동시에 여러 모델 작업이 실행되지
  않도록 하고 번역 대기·진행·실패 상태를 라이브러리와 분석 큐에 표시
- 모든 요약 모델의 임시 입력에서 참고문헌과 그림·표·특허 도면 설명을 제외해
  본문 분석에 컨텍스트를 집중하되 PaperPack 원문은 유지
- 특허는 기술적 과제·청구된 발명의 핵심·구현 및 실시예·기술적 효과·명시된
  제약을 중심으로 분석하는 전용 프롬프트 v1을 사용
- 특허 PDF의 `Claims`·`청구범위` 구간은 AI가 고쳐 쓰지 않고 추출 문구 그대로
  PaperPack에 보존해 분석 화면의 `청구항 원문`에서 확인하며, 특허와 논문에서
  독립된 페이지 번호·표식은 정규식으로 제거
- 기존 안전 정지와 별도로 현재 Ollama·OCR 작업을 중단하고 항목을 실패가 아닌
  대기열로 되돌리는 `즉시 정지` 버튼 추가
- 제외 목록은 원본 PDF를 이동하지 않고 파일 ID만 기록하며, 기존 방식으로 이동된
  제외 파일의 복원 호환성은 유지
- 내장 sPDF를 1.6.2로 갱신하고 Windows 화면 배율에 맞춘 고해상도로 페이지와
  썸네일을 렌더링하도록 개선
- Ollama 모델 관리를 추천·모델·설치 상태·용량·규모/양자화·용도/강점 열을
  갖춘 단일 표로 통합하고, 선택한 행에서 설치·검증·삭제를 바로 실행

### 수정

- v1.7.0에서 생성되지 않은 편집 캐시 폴더와 불완전한 작업공간을 자동 복구해
  `paperpack 편집본을 준비할 수 없습니다` 오류를 해결
- 새 PDF 검색의 긴 경로·상태 문구가 기본 창 너비를 강제로 키우지 않도록 수정
- 재요약 대기열 등록 안내가 라이브러리와 상태 표시줄에 중복 출력되던 문제 수정
- 모델 관리·시작 화면·오류 안내에서 일반 사용자에게 노출되던 `JSON` 용어를
  `서지정보 입력` 또는 `기존 색인`으로 교체
- 한국·미국 특허 첫 장의 INID 표기에서 발명의 명칭·발명자·등록/공개번호·
  출원번호·출원인/권리자·공고연도를 읽고, 청구항 원문은 추출 과정의 불필요한
  줄바꿈을 합치면서 청구항 경계는 넓게 구분해 표시
- 여러 특허의 첫 장을 묶은 PDF에서 뒤 문서의 서지정보를 첫 특허에 합치지 않고,
  한국 특허의 발명자·권리자 아래 주소는 이름에서 제외
- 특허 번호 표시는 `출원/등록번호`로 통합하고 등록번호가 있으면 대표값으로
  우선 표시하며, 검색 색인에는 등록번호와 출원번호를 모두 포함

## [1.7.0] - 2026-07-29

### 개선

- 라이브러리 목록에서 PaperPack 등록 시간과 마지막 성공 분석일을 함께 표시
- 라이브러리 상세 패널에 원문 색인을 보존하는 AI 번역 버튼과 원문/번역 토글을
  추가하고, 분석이 바뀌지 않은 번역은 PaperPack에서 재사용
- Windows HiDPI·분수 배율을 앱 시작 전에 활성화하고, 좁은 화면에서는 요약 엔진
  옵션을 2열에서 1열로 전환하며 세로 스크롤을 제공
- 내장 sPDF 모듈을 1.6.1로 갱신해 페이지 구성·업데이트 기능과 긴 문서의 안정적인
  썸네일 렌더링 개선을 반영
- 요약 AI 설정과 Ollama 모델 관리 화면에서 모델별 권장 용도, 환각 위험,
  요약 전략과 기여·한계 분석 지원 여부를 함께 안내
- 8B 미만 로컬 모델은 구역별 요약 후 통합하는 보수적 경로를 사용하고,
  기여·한계처럼 추론이 필요한 항목은 8B 이상 모델에서만 생성
- 한국어·긴 문서 요약·정보 추출을 지원하는 `granite3.3:2b`를 저사양 선택 및
  교차 모델 벤치마크 목록에 추가
- 한국어 자연어 검색에서 AI가 만든 검색어와 함께 원 질문의 기술 식별자를 보존하고
  배지·세포주·배양 등 주요 실험 용어의 영문 원문 표현도 SQLite 검색에 추가
- 자연어 검색은 설치된 선택 Ollama 모델을 먼저 사용하고, 선택 모델이 없으면
  설치된 1.5~4B 호환 모델을 우선해 클라우드 호출과 불필요한 대형 모델 적재를 절감
- Ollama 모델 상주 여부와 상주 모델을 선택할 수 있게 하고, 자동 모드에서는 충분한
  시스템 메모리가 있는 경우에만 선택 모델을 미리 적재
- 추천 프로필을 바꾸면 PC 사양과 Ollama 상태를 즉시 다시 평가하고, 실제 추천
  후보에 별도 표식을 표시하면서 활성 모델은 사용자 선택 없이 변경하지 않음
- 최초 실행에서 시작·종료 방식에 이어 다운로드 폴더·스캔 주기·자동 보관 설정과
  요약 AI 제공자·모델 설정을 차례로 안내
- 요약 엔진 옵션의 긴 버전 변경 안내를 제거하고 창 제목에서 내부 프롬프트 v9를
  간결하게 확인할 수 있도록 정리

### 수정

- 깨진 PDF 제목 메타데이터의 UTF-8·한국어 인코딩을 복구하고, 복구 후에도 문서
  작업명에 불과한 값이면 첫 페이지에서 시각적으로 두드러진 제목을 사용

## [1.6.2] - 2026-07-29

### 개선

- 요약 엔진 옵션을 좌우 패널로 넓혀 제공자·출력 설정과 모델 선택·Ollama
  설치·삭제·사양 추천을 한 화면에 배치
- Ollama가 설치되어 있지만 꺼져 있으면 기본 설치 경로에서 서버를 시작한 뒤 설치
  모델 목록을 다시 불러오도록 개선
- 중단된 업데이트 조각과 현재 버전 이하 설치 파일을 앱 시작 때 정리하고, 아직
  설치하지 않은 최신 설치 파일 하나만 유지
- 이미 받은 설치 파일의 크기와 SHA-256이 일치하면 다시 다운로드하지 않고 재사용
- Ollama 모델 삭제 완료 시 수동 새로고침 없이 설치 목록에서 즉시 제거하고 백그라운드
  재조회로 실제 상태를 다시 확인
- 요약 JSON 생성이 두 번 실패하면 타입이 맞는 빈 JSON 틀과 직렬화 규칙을 명시한
  형식 교정 전용 프롬프트로 세 번째이자 마지막 복구 시도 실행
- AI 요약이 끝내 실패하면 PaperPack과 분석 큐를 실패 상태로 유지하면서 원문의
  Abstract·DOI·연도 후보 등 정규식 추출본만 별도 표시
- 실패 화면에 오류 분류·예외 형식·제공자/모델·요청 횟수·분석 전략·출력 언어·
  포함 구역·실패 시각과 안전하게 정리한 실제 오류 메시지를 표시

## [1.6.1] - 2026-07-29

### 개선

- 설정 메뉴를 `요약 감시 옵션`과 `요약 엔진 옵션` 두 카테고리로 재구성
- 요약 엔진 옵션에서 앱 1.6 엔진 변경점·내부 프롬프트 버전·요약 언어를 한눈에 표시
- AI 요청 제한 시간을 설정에서 60~3,600초로 조정하고 기본값을 900초로 확대
- 두 번째 앱 실행은 새 트레이 아이콘을 만들지 않고 기존 창을 앞으로 가져오도록 단일 실행 보장
- 설치된 Ollama 모델을 요약 엔진 옵션에서 불러와 선택 즉시 활성 모델로 적용

### 변경

- 모델 추천과 설치·삭제는 하나의 모델 관리 창으로 통합하고 중복 선택 버튼 제거

### 제거

- 분석 큐와 기능이 겹치던 레거시 즉시 요약 메뉴·단축키·다이얼로그 제거

## [1.6.0] - 2026-07-28

### 개선

- Ollama 모델 관리 창을 넓히고 다운로드 진행 문구를 왼쪽에 고정
- 조각별 진행률 때문에 막대가 뒤로 움직이지 않도록 다운로드 진행률을 단조 증가로 표시
- 설치된 모델 목록을 직접 선택할 수 있게 바꾸고 목록에서 선택한 모델을 바로 삭제
- OCR이 필요한 문서의 로컬 AI 분석은 8B 이상 Ollama 모델에서만 실행해 저사양
  PC의 과부하와 저용량 모델의 낮은 OCR 요약 품질 방지
- AI 응답·내부 모델·PaperPack·파생 색인의 요약 필드를 언어 중립적인 `summary`로
  통일하고, 기존 PaperPack은 읽을 때 변환한 뒤 다음 저장에서 구 필드를 제거
- 계층형 요약의 섹션 응답에서 JSON을 제거하고 짧은 평문 근거만 받도록 바꿔
  작은 모델의 반복적인 누락·잘림·형식 오류를 방지
- 4B 이하 최종 JSON에서는 사용하지 않는 기여·한계 필드를 스키마에서 제외하고
  내부에서 빈 값으로 정규화해 출력 부담을 추가로 축소
- 제목·저자·연도·저널을 요약 JSON에서 분리해 첫 페이지 전용 소형 JSON으로
  추출하고, 원문에서 확인되지 않는 값은 저장하지 않도록 검증
- ResearchGate·Academia.edu·PubMed 등 배포·검색 플랫폼과 다운로드 배너를
  저널명으로 오인하지 않도록 입력 정리와 결과 검증 양쪽에서 차단
- 모델 벤치마크에 제목·저자·연도·저널의 독립 서지정보 정확도 점수 추가
- 원문 언어 유지와 한국어 번역 프롬프트를 별도 계약으로 분리하고, 설명 필드가
  요청 언어를 위반하면 한 번 재시도한 뒤 반복 위반 결과는 저장하지 않도록 보강
- 모델 비교에서는 OCR 문서를 제외하고 텍스트 레이어 논문의 요약 품질만 평가

### 수정

- Ollama 모델 다운로드 작업 시작 직후 `다운로드 취소` 버튼이 활성화되지 않던 상태 판정 수정

## [1.5.0] - 2026-07-28

### 추가

- Phi-4 Mini, Gemma 3 4B IT QAT, Ministral 3 3B Instruct Q4_K_M을 모델 관리와 합성 논문 벤치마크 후보에 추가

### 수정

- 모델명에 파라미터 수가 없는 Phi-4 Mini도 카탈로그 사양을 사용해 4B 이하 계층형 요약과 안전 컨텍스트 정책 적용

## [1.4.2] - 2026-07-28

### 추가

- 라이브러리의 `분석 완료` 상태 옆에 해당 분석을 저장한 Paper Organizer 버전 표시
- 기존 분석처럼 앱 버전이 없는 경우 저장된 요약 프롬프트 버전을 대신 표시
- 분석 상세 하단에 제공자·모델과 함께 앱 및 요약 프롬프트 버전 표시

### 수정

- AI가 JSON을 코드블록이나 설명문으로 감싸도 내부의 완전한 JSON 객체를 복구
- 불완전하거나 스키마가 잘못된 JSON 응답은 JSON 전용 강화 프롬프트로 한 번 재시도
- JSON 재시도까지 실패하면 원인과 대안을 한국어로 안내하고 재시도 횟수를
  PaperPack 분석 provenance에 기록

## [1.4.1] - 2026-07-28

### 추가

- 합성 벤치마크 논문 9편 각각의 100점 배점표와 모델·논문별 실제 점수 CSV
- 제목·연구질문·방법·핵심결과·수치·중요 부정문·결론의 세부 점수와 금지 주장 감점
- 동일 문서에서 측정한 정확도·처리 시간·완료율을 함께 반영하는 실측 모델 추천 결과

### 변경

- 정적 모델 카탈로그는 RAM·디스크 안전성 판단에만 사용하고 요약 품질은 실측
  벤치마크 결과로 별도 평가
- 정밀 모델 비교는 향후 사용자가 선택한 경우에만 기준 추천 모델과 인접 등급을
  대상으로 실행하도록 개발 방향 명확화

### 수정

- 금지 주장 검출 시 문장 인접성과 부정 표현을 함께 확인해 단어가 흩어져 나타나는
  정답 문장을 잘못 감점하지 않도록 보강

## [1.4.0] - 2026-07-28

### 추가

- 논문을 Abstract·Introduction·Materials and Methods·Results·Discussion·Conclusion
  구역으로 나눈 뒤 문단별 컨텍스트로 요약하는 전처리 엔진
- 요약 설명 필드를 한국어로 번역하거나 논문 원문 언어로 유지하는 설정
- 라이브러리 오른쪽 메뉴의 sPDF 열기·탐색기에서 열기·재요약·추천 분야 승인·삭제
- exact_file 중복과 분석 큐 항목에서 기존 라이브러리 분석으로 바로 이동하는 동작
- 업데이트 버전별 변경 내역 확인과 설치·나중에·해당 버전 건너뛰기 선택
- 합성 논문 9편으로 Qwen3 0.6B·1.7B·4B·8B를 같은 조건에서 비교하는 Ollama 실행기

### 변경

- 반복 머리말·꼬리말·쪽번호를 제거하고 OCR 줄바꿈 하이픈·고립 특수문자·제어문자를
  정리한 뒤 DOI와 연도 후보를 정규식으로 먼저 검증
- 컨텍스트를 줄여야 할 때도 검출된 각 논문 구역의 근거를 최소 한 문단씩 유지
- 빠른 요약은 18페이지 이하는 모두 사용하고 긴 문서는 앞·중간·끝을 고르게 표본화
- AI 추천 연구분야가 있으면 라이브러리 분석 상태와 승인 버튼에서 눈에 띄게 표시
- 0.6B·1.7B·4B는 구역별 사실 요약 후 최종 요약을 만드는 2단계 경로를 사용하고,
  8B 이상은 정리된 전체 구역을 한 번에 분석
- 핵심 기여와 한계는 8B 이상 및 클라우드 모델만 생성하며, 작은 모델 재분석은
  기존 고급 분석 필드를 덮어쓰지 않음

### 수정

- 정렬 가능한 표의 선택 항목을 행 번호가 아닌 저장된 PaperPack ID로 찾도록 보강
- 앱 종료 시 Windows 네이티브 트레이 아이콘을 명시적으로 해제해 잔상 누적 완화
- 영어 원문 제목을 한국어로 번역한 결과가 감지되면 원문 복사를 강화해 최종 JSON을
  한 번만 자동 재시도

## [1.3.4] - 2026-07-26

### 변경

- 첫 실행 AI 안내에서 기본 요약 AI로 Ollama를 사용하므로 런타임과 모델을 먼저 설치해야 함을 명시
- Ollama 모델 추천·다운로드 카탈로그를 8B 이하로 단순화하고 12B 이상 선택지 제거
- 이미 설치된 12B 이상 공유 모델은 설치 현황에만 표시하고 Paper Organizer의 활성 모델 선택에서는 제외

### 수정

- Ollama가 꺼진 상태에서 즉시 요약을 실행하면 서버를 자동으로 다시 시작
- 앱이 시작한 Ollama 서버가 조기에 종료되면 60초를 기다리지 않고 즉시 실패 상태로 전환
- 사용자 환경의 `OLLAMA_HOST`와 관계없이 앱 관리 서버를 로컬 전용 주소에서 시작

## [1.3.3] - 2026-07-26

### 추가

- 라이브러리에서 여러 논문을 선택해 PaperPack과 저장된 분석 내용을 완전히 삭제하는 기능

### 변경

- 완전 삭제 전에 선택 개수와 제목을 표시하고 복원 불가능함을 명시해 사용자 확인
- 분석 중인 항목은 삭제하지 않고, 삭제된 항목의 분석 큐·통합 색인·전문 색인·임시 PDF·sPDF 편집 캐시를 함께 정리
- 라이브러리 삭제 시 감시 폴더에 남아 있는 원본 PDF는 유지

## [1.3.2] - 2026-07-26

### 변경

- 수집 검토 항목의 보관 동작을 `분석 큐로 보내기`로 명확히 표시하고, 버튼 또는 드래그 앤 드롭으로 보낸 항목은 즉시 분석 시작
- 분석 큐의 `즉시 요약으로 보내기`를 `선택 항목 바로 분석`으로 교체
- 사용자가 바로 분석을 요청한 여러 항목은 절전 대기 없이 한 편씩 연속 처리하고 마지막 항목까지 Ollama 런타임 유지
- 대기 중인 로컬 분석이 있으면 Ollama를 필요할 때 시작하고 작업 종료 후 기존 정책에 따라 정리
- AI 추천 모델 버튼에서 추천 모델이 선택된 모델 관리 창으로 바로 이어져 설치·검증·선택·삭제를 한 흐름에서 처리
- 라이브러리의 색인 편집 저장 버튼을 목록 아래에서 상세 편집 폼 바로 아래로 이동
- 라이브러리 검색창의 회색 안내문에 일반 키워드 검색과 자연어 질문 검색을 함께 명시
- 로컬 설치파일 빌드 완료 문구가 `installer.iss`의 현재 버전을 따라가도록 수정

## [1.3.1] - 2026-07-26

### 변경

- 앱 내부 업데이터는 릴리스 버전과 정확히 일치하는 번호 포함 설치파일만 다운로드
- 업데이트 창에 다운로드할 설치파일의 전체 이름과 용량을 함께 표시
- 정확한 버전의 설치파일이 없을 때 다른 버전 파일로 대체하지 않고 등록 대기로 표시

## [1.3.0] - 2026-07-26

### 추가

- 자연어 질문을 영어 검색어와 연도·분야 조건으로 바꿔 로컬 전문 색인에서 후보 논문을 찾는 RAG-lite 검색
- 최대 5편의 실제 PaperPack 페이지 본문만 근거로 한국어 답변을 만들고 논문·PDF 페이지를 함께 표시
- 자연어 검색 결과를 두 번 누르면 라이브러리의 해당 논문과 AI 분석 내용으로 바로 이동하는 기능
- `Ctrl+Shift+F`로 자연어 논문 찾기를 여는 단축키

### 변경

- 라이브러리 검색창이 짧은 제목·저자·DOI·기술명은 SQLite로 즉시 검색하고, 질문·비교·설명형 입력은 AI 검색으로 자동 전환
- 로컬 AI와 전송 동의가 저장된 클라우드 AI는 후보 검색에서 근거 답변까지 자동으로 연속 실행
- 클라우드 AI는 질문 해석과 논문 본문 전송을 구분해 동의를 받고, 전송 전에 후보 수와 실제 글자 수를 표시
- AI가 검색 후보에 없는 논문이나 제공되지 않은 페이지를 인용하면 결과에서 제거
- 참고문헌은 후보 검색에는 활용하되 논문의 연구 결과를 설명하는 근거로 사용하지 않도록 검색 답변 프롬프트 보강
- 자연어 검색 중에는 백그라운드 분석을 잠시 멈추고, 앱이 시작한 Ollama는 검색 완료·실패·창 닫기 뒤 종료

## [1.2.0] - 2026-07-26

### 추가

- 라이브러리에서 선택한 논문 또는 전체 논문을 재요약 대기열에 넣는 기능
- AI가 논문별 검색용 메타태그를 최대 5개 생성하고 사용자 태그와 별도로 보관하는 기능
- 저장된 연구분야가 맞지 않을 때 새 연구분야를 추천하고, 사용자 승인 후 설정에 추가해 재분석하는 흐름

### 변경

- Qwen3 4B는 PC 사양과 논문 길이에 따라 16K~24K, Qwen3 8B는 최대 40K 컨텍스트를 사용
- 로컬 컨텍스트 크기에 맞춰 본문 전송 범위를 자동 조절하고 즉시요약 미리보기에 실제 컨텍스트를 표시
- 재요약은 기존 AI 결과를 새 결과로 교체하되 사용자 편집 제목·저자·분류·태그는 계속 보호
- 참고문헌은 검색용 PaperPack 본문에는 유지하고 AI 분석 입력에서만 제외

## [1.1.3] - 2026-07-26

### 추가

- 설정에서 AI 분류용 연구분야를 직접 추가·이름 수정·삭제하는 관리 화면

### 변경

- PaperPack의 전체 본문과 검색 색인은 보존하면서 AI 요약 입력에서는 참고문헌 절 이후를 제외
- 남은 참고문헌을 연구 결과나 저자 판정 근거로 사용하지 않도록 요약 프롬프트 보강
- 체크한 연구분야만 AI 분류 후보로 보내고, 미선택 시 편집한 전체 분야 목록 사용
- 자동 신버전 확인은 마지막 확인 후 24시간에 한 번만 실행하고 수동 확인은 제한 없이 허용

## [1.1.2] - 2026-07-26

### 변경

- AI 메뉴를 설정 메뉴 안으로 이동
- 검토 대기 PDF를 분석 큐로 끌어다 놓으면 확인창 없이 보관 후 분석 대기로 전환
- 검토 대기 항목은 분석 큐에 중복 표시하지 않고, 분석 완료 후 큐 기록을 자동 삭제
- 라이브러리 목록을 다중 선택으로 바꾸고 sPDF 열기·편집본 적용·편집 취소를 일괄 처리
- AI가 자동 추정 제목을 원문 언어 그대로 더 정확하게 교정하도록 프롬프트와 필드 우선순위 보강
- 사용자가 직접 수정한 제목은 AI가 덮어쓰지 않도록 기존 보호 규칙 유지
- `지금 분석`으로 선택한 항목은 절전 대기 없이 연속 처리하고 마지막 항목 뒤 Ollama 정리

## [1.1.1] - 2026-07-26

### 변경

- 수집·검토 대기 목록에서 여러 PDF를 선택해 일괄 열기·보관·제외
- 대기 목록에 전체 선택 버튼을 추가하고 다중 보관 시 각 PDF의 추정 메타데이터 적용
- 보관 성공 대화상자를 없애고 상태 문구로만 조용히 알림
- 일괄 작업은 실패나 주의사항이 있을 때만 대화상자를 표시

## [1.1.0] - 2026-07-26

### 추가

- 분석 큐 실패 사유 표시, 전체 선택, 실패 항목 재분석
- 라이브러리 목록의 분석 대기·진행·완료·실패 상태
- 새 PDF를 복구 가능한 제외 목록으로 보내고 파일 ID를 보관하는 제외 흐름
- 폴더 및 감시 설정 복원과 라이브러리 변경 시 PaperPack·DB·상태 파일 이동
- 도움말, 제작자·기여자 정보와 기본 단축키

### 변경

- 저사양 PC를 위해 AI 분석은 항상 한 건씩 실행
- 즉시 요약 중에는 백그라운드 분석이 겹치지 않도록 상호 배제
- 즉시 요약 PDF 선택 시 전송 미리보기를 자동 준비하고 실행 버튼 활성화
- 라이브러리의 판본 열과 중복되는 한국어 설명 편집란 제거
- 요약 요청의 Ollama thinking 및 OpenAI reasoning 비활성화
- 앱이 직접 시작한 Ollama 서버는 AI 작업 후 종료
- WindowsApps 실행 별칭에 접근할 수 없는 경우에도 Ollama 탐색을 계속 처리
- Ollama 자동 설치의 경과 시간과 Delivery Optimization 사용 안내를 표시하고,
  3분 타임아웃 후 공식 다운로드 버튼 제공
- 사용하지 않는 대화상자 제목 표시줄의 `?` 도움말 버튼 제거
- 일반 논문 후보가 아닌 1~2쪽 PDF는 새 PDF 감시·분석 큐에서 제외
- 설치 후 첫 실행에서 AI 제공자 설정과 Ollama 런타임·모델 준비를 순서대로 안내
- Ollama 모델 설치·검증 직후 활성 모델로 자동 선택·저장하고 삭제 후 목록을 즉시 갱신
- 여러 감시 폴더를 설정에서 추가·제거하고 한 번의 스캔으로 함께 처리
- 특허 문서 표식을 판별해 논문과 함께 자동 수집·검색
- Ollama 모델 다운로드에 전송량, MB/s 속도와 예상 남은 시간 표시
- 분석 큐의 완료 항목을 더블클릭하면 라이브러리의 해당 분석 결과로 이동
- 백그라운드 분석 완료 즉시 라이브러리 캐시와 화면을 새로고침
- AI 분석 시 기존 제목·저자·저널명을 번역하지 않고 추출된 원문 서지정보 보존
- 분석 큐에서 여러 검토 대기 항목을 선택해 한 번에 제외
- 분석 큐의 우선순위·즉시요약·지금 분석·재분석 작업을 다중 선택에 맞게 일괄 처리
- 완료된 큐 항목으로 이동할 때 같은 라이브러리 행이어도 AI 분석 상세를 즉시 다시 표시
- 리뷰·체계적 문헌고찰·메타분석도 첫 페이지 byline과 AI 분석에서 원문 저자를 추출
- 라이브러리 검색 결과의 첫 논문을 자동 선택해 AI 분석 상세를 즉시 표시
- 라이브러리 검색창에 한 번에 지우는 X 버튼을 제공하고 앱 제목 표시줄에 현재 버전 표시
- 검색 중 큐의 완료 논문을 열면 검색어를 초기화하고 라이브러리에서 정확히 선택
- 텍스트가 없는 특허·논문도 즉시요약과 분석 준비 중 내장 RapidOCR을 자동 실행
- 새 PDF 검색 OCR은 앞 5페이지만 한 장씩 저우선순위로 처리하고 결과를 디스크에 캐시
- PaperPack 보관 후 전체 OCR을 백그라운드에서 한 장씩 완료하며 페이지 진행률 표시
- 사용자 요청 OCR은 일반 우선순위로 빠르게 실행하고 자동 OCR은 CPU 스레드와 우선순위 제한
- 백그라운드 OCR의 ONNX·OpenCV 스레드를 1개로 직접 고정하고 렌더링 배율을 3배로 제한
- 새 PDF 검색 결과를 두 번 누르면 큐 이동 전에 원본을 sPDF로 열어 확인
- 라이브러리를 논문과 특허 유형별 폼으로 표시하고 특허에 잘못된 저널 값이 들어가지 않도록 차단
- 2페이지 미만 문서는 OCR에서 제외하고 앱 종료 시 실행 중인 OCR 자식 프로세스 정리
- 제외 파일 복원을 파일·판정·중복·추정 제목이 보이는 넓은 다중 선택 표로 개선
- GitHub Releases에서 새 버전을 확인하고 다운로드 속도·SHA-256 검증 후 설치 실행
- 버전 태그를 올리면 GitHub Actions가 Windows 설치파일을 빌드·릴리스하도록 자동화

## [1.0.0] - 2026-07-26

### 추가

- 학술 논문 PDF를 판별해 승인 없이 자동 보관하는 파이프라인
- 학과·세부 전공 기반 분류 체계와 정규식 1차 분류기
- AI 분석을 통한 분류 및 제목·저자·연도·저널 정보 보정
- `.paperpack` 페이지별 본문 저장과 SQLite FTS5 전문 검색
- Ollama 런타임 설치·실행 온보딩 및 `winget` 기본 경로 탐색
- 전용 애플리케이션 아이콘과 PDF 환원 대화상자
- 분석 진행 상태 표시와 분석 큐 정렬

### 변경

- 수집·분석 화면과 라이브러리 화면을 좌우 2단 구조로 재편
- 즉시 요약, 레거시 변환, 폴더 설정 기능을 전용 메뉴·대화상자로 이동
- AI 요약 스키마를 `paper-summary-v2`로 확장
- 패키지 버전을 1.0.0으로 갱신

### 제거

- OneDrive JSON 미러 및 클라우드 메타데이터 동기화 기능
