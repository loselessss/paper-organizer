# 변경 기록

이 프로젝트의 주요 변경 사항을 버전별로 기록합니다.

## [Unreleased]

### 개선

- 내장 sPDF 모듈을 1.6.1로 갱신해 페이지 구성·업데이트 기능과 긴 문서의 안정적인
  썸네일 렌더링 개선을 반영
- 요약 AI 설정과 Ollama 모델 관리 화면에서 모델별 권장 용도, 환각 위험,
  요약 전략과 기여·한계 분석 지원 여부를 함께 안내
- 8B 미만 로컬 모델은 구역별 요약 후 통합하는 보수적 경로를 사용하고,
  기여·한계처럼 추론이 필요한 항목은 8B 이상 모델에서만 생성
- 한국어·긴 문서 요약·정보 추출을 지원하는 `granite3.3:2b`를 저사양 선택 및
  교차 모델 벤치마크 목록에 추가
- 한국어 자연어 검색에서 AI가 만든 검색어와 함께 원 질문의 기술 식별자를 보존하고
  배지·세포주·배양 등 주요 실험 용어의 영문 원문 표현도 SQLite 검색에 추가
- 자연어 검색은 설치된 선택 Ollama 모델을 먼저 사용하고, 선택 모델이 없으면
  설치된 1.5~4B 호환 모델을 우선해 클라우드 호출과 불필요한 대형 모델 적재를 절감

### 수정

- 깨진 PDF 제목 메타데이터의 UTF-8·한국어 인코딩을 복구하고, 복구 후에도 문서
  작업명에 불과한 값이면 첫 페이지에서 시각적으로 두드러진 제목을 사용

## [1.6.2] - 2026-07-29

### 개선

- 요약 엔진 옵션을 좌우 패널로 넓혀 제공자·출력 설정과 모델 선택·Ollama
  설치·삭제·사양 추천을 한 화면에 배치
- Ollama가 설치되어 있지만 꺼져 있으면 기본 설치 경로에서 서버를 시작한 뒤 설치
  모델 목록을 다시 불러오도록 개선
- 중단된 업데이트 조각과 현재 버전 이하 설치 파일을 앱 시작 때 정리하고, 아직
  설치하지 않은 최신 설치 파일 하나만 유지
- 이미 받은 설치 파일의 크기와 SHA-256이 일치하면 다시 다운로드하지 않고 재사용
- Ollama 모델 삭제 완료 시 수동 새로고침 없이 설치 목록에서 즉시 제거하고 백그라운드
  재조회로 실제 상태를 다시 확인
- 요약 JSON 생성이 두 번 실패하면 타입이 맞는 빈 JSON 틀과 직렬화 규칙을 명시한
  형식 교정 전용 프롬프트로 세 번째이자 마지막 복구 시도 실행
- AI 요약이 끝내 실패하면 PaperPack과 분석 큐를 실패 상태로 유지하면서 원문의
  Abstract·DOI·연도 후보 등 정규식 추출본만 별도 표시
- 실패 화면에 오류 분류·예외 형식·제공자/모델·요청 횟수·분석 전략·출력 언어·
  포함 구역·실패 시각과 안전하게 정리한 실제 오류 메시지를 표시

## [1.6.1] - 2026-07-29

### 개선

- 설정 메뉴를 `요약 감시 옵션`과 `요약 엔진 옵션` 두 카테고리로 재구성
- 요약 엔진 옵션에서 앱 1.6 엔진 변경점·내부 프롬프트 버전·요약 언어를 한눈에 표시
- AI 요청 제한 시간을 설정에서 60~3,600초로 조정하고 기본값을 900초로 확대
- 두 번째 앱 실행은 새 트레이 아이콘을 만들지 않고 기존 창을 앞으로 가져오도록 단일 실행 보장
- 설치된 Ollama 모델을 요약 엔진 옵션에서 불러와 선택 즉시 활성 모델로 적용

### 변경

- 모델 추천과 설치·삭제는 하나의 모델 관리 창으로 통합하고 중복 선택 버튼 제거

### 제거

- 분석 큐와 기능이 겹치던 레거시 즉시 요약 메뉴·단축키·다이얼로그 제거

## [1.6.0] - 2026-07-28

### 개선

- Ollama 모델 관리 창을 넓히고 다운로드 진행 문구를 왼쪽에 고정
- 조각별 진행률 때문에 막대가 뒤로 움직이지 않도록 다운로드 진행률을 단조 증가로 표시
- 설치된 모델 목록을 직접 선택할 수 있게 바꾸고 목록에서 선택한 모델을 바로 삭제
- OCR이 필요한 문서의 로컬 AI 분석은 8B 이상 Ollama 모델에서만 실행해 저사양
  PC의 과부하와 저용량 모델의 낮은 OCR 요약 품질 방지
- AI 응답·내부 모델·PaperPack·파생 색인의 요약 필드를 언어 중립적인 `summary`로
  통일하고, 기존 PaperPack은 읽을 때 변환한 뒤 다음 저장에서 구 필드를 제거
- 계층형 요약의 섹션 응답에서 JSON을 제거하고 짧은 평문 근거만 받도록 바꿔
  작은 모델의 반복적인 누락·잘림·형식 오류를 방지
- 4B 이하 최종 JSON에서는 사용하지 않는 기여·한계 필드를 스키마에서 제외하고
  내부에서 빈 값으로 정규화해 출력 부담을 추가로 축소
- 제목·저자·연도·저널을 요약 JSON에서 분리해 첫 페이지 전용 소형 JSON으로
  추출하고, 원문에서 확인되지 않는 값은 저장하지 않도록 검증
- ResearchGate·Academia.edu·PubMed 등 배포·검색 플랫폼과 다운로드 배너를
  저널명으로 오인하지 않도록 입력 정리와 결과 검증 양쪽에서 차단
- 모델 벤치마크에 제목·저자·연도·저널의 독립 서지정보 정확도 점수 추가
- 원문 언어 유지와 한국어 번역 프롬프트를 별도 계약으로 분리하고, 설명 필드가
  요청 언어를 위반하면 한 번 재시도한 뒤 반복 위반 결과는 저장하지 않도록 보강
- 모델 비교에서는 OCR 문서를 제외하고 텍스트 레이어 논문의 요약 품질만 평가

### 수정

- Ollama 모델 다운로드 작업 시작 직후 `다운로드 취소` 버튼이 활성화되지 않던 상태 판정 수정

## [1.5.0] - 2026-07-28

### 추가

- Phi-4 Mini, Gemma 3 4B IT QAT, Ministral 3 3B Instruct Q4_K_M을 모델 관리와 합성 논문 벤치마크 후보에 추가

### 수정

- 모델명에 파라미터 수가 없는 Phi-4 Mini도 카탈로그 사양을 사용해 4B 이하 계층형 요약과 안전 컨텍스트 정책 적용

## [1.4.2] - 2026-07-28

### 추가

- 라이브러리의 `분석 완료` 상태 옆에 해당 분석을 저장한 Paper Organizer 버전 표시
- 기존 분석처럼 앱 버전이 없는 경우 저장된 요약 프롬프트 버전을 대신 표시
- 분석 상세 하단에 제공자·모델과 함께 앱 및 요약 프롬프트 버전 표시

### 수정

- AI가 JSON을 코드블록이나 설명문으로 감싸도 내부의 완전한 JSON 객체를 복구
- 불완전하거나 스키마가 잘못된 JSON 응답은 JSON 전용 강화 프롬프트로 한 번 재시도
- JSON 재시도까지 실패하면 원인과 대안을 한국어로 안내하고 재시도 횟수를
  PaperPack 분석 provenance에 기록

## [1.4.1] - 2026-07-28

### 추가

- 합성 벤치마크 논문 9편 각각의 100점 배점표와 모델·논문별 실제 점수 CSV
- 제목·연구질문·방법·핵심결과·수치·중요 부정문·결론의 세부 점수와 금지 주장 감점
- 동일 문서에서 측정한 정확도·처리 시간·완료율을 함께 반영하는 실측 모델 추천 결과

### 변경

- 정적 모델 카탈로그는 RAM·디스크 안전성 판단에만 사용하고 요약 품질은 실측
  벤치마크 결과로 별도 평가
- 정밀 모델 비교는 향후 사용자가 선택한 경우에만 기준 추천 모델과 인접 등급을
  대상으로 실행하도록 개발 방향 명확화

### 수정

- 금지 주장 검출 시 문장 인접성과 부정 표현을 함께 확인해 단어가 흩어져 나타나는
  정답 문장을 잘못 감점하지 않도록 보강

## [1.4.0] - 2026-07-28

### 추가

- 논문을 Abstract·Introduction·Materials and Methods·Results·Discussion·Conclusion
  구역으로 나눈 뒤 문단별 컨텍스트로 요약하는 전처리 엔진
- 요약 설명 필드를 한국어로 번역하거나 논문 원문 언어로 유지하는 설정
- 라이브러리 오른쪽 메뉴의 sPDF 열기·탐색기에서 열기·재요약·추천 분야 승인·삭제
- exact_file 중복과 분석 큐 항목에서 기존 라이브러리 분석으로 바로 이동하는 동작
- 업데이트 버전별 변경 내역 확인과 설치·나중에·해당 버전 건너뛰기 선택
- 합성 논문 9편으로 Qwen3 0.6B·1.7B·4B·8B를 같은 조건에서 비교하는 Ollama 실행기

### 변경

- 반복 머리말·꼬리말·쪽번호를 제거하고 OCR 줄바꿈 하이픈·고립 특수문자·제어문자를
  정리한 뒤 DOI와 연도 후보를 정규식으로 먼저 검증
- 컨텍스트를 줄여야 할 때도 검출된 각 논문 구역의 근거를 최소 한 문단씩 유지
- 빠른 요약은 18페이지 이하는 모두 사용하고 긴 문서는 앞·중간·끝을 고르게 표본화
- AI 추천 연구분야가 있으면 라이브러리 분석 상태와 승인 버튼에서 눈에 띄게 표시
- 0.6B·1.7B·4B는 구역별 사실 요약 후 최종 요약을 만드는 2단계 경로를 사용하고,
  8B 이상은 정리된 전체 구역을 한 번에 분석
- 핵심 기여와 한계는 8B 이상 및 클라우드 모델만 생성하며, 작은 모델 재분석은
  기존 고급 분석 필드를 덮어쓰지 않음

### 수정

- 정렬 가능한 표의 선택 항목을 행 번호가 아닌 저장된 PaperPack ID로 찾도록 보강
- 앱 종료 시 Windows 네이티브 트레이 아이콘을 명시적으로 해제해 잔상 누적 완화
- 영어 원문 제목을 한국어로 번역한 결과가 감지되면 원문 복사를 강화해 최종 JSON을
  한 번만 자동 재시도

## [1.3.4] - 2026-07-26

### 변경

- 첫 실행 AI 안내에서 기본 요약 AI로 Ollama를 사용하므로 런타임과 모델을 먼저 설치해야 함을 명시
- Ollama 모델 추천·다운로드 카탈로그를 8B 이하로 단순화하고 12B 이상 선택지 제거
- 이미 설치된 12B 이상 공유 모델은 설치 현황에만 표시하고 Paper Organizer의 활성 모델 선택에서는 제외

### 수정

- Ollama가 꺼진 상태에서 즉시 요약을 실행하면 서버를 자동으로 다시 시작
- 앱이 시작한 Ollama 서버가 조기에 종료되면 60초를 기다리지 않고 즉시 실패 상태로 전환
- 사용자 환경의 `OLLAMA_HOST`와 관계없이 앱 관리 서버를 로컬 전용 주소에서 시작

## [1.3.3] - 2026-07-26

### 추가

- 라이브러리에서 여러 논문을 선택해 PaperPack과 저장된 분석 내용을 완전히 삭제하는 기능

### 변경

- 완전 삭제 전에 선택 개수와 제목을 표시하고 복원 불가능함을 명시해 사용자 확인
- 분석 중인 항목은 삭제하지 않고, 삭제된 항목의 분석 큐·통합 색인·전문 색인·임시 PDF·sPDF 편집 캐시를 함께 정리
- 라이브러리 삭제 시 감시 폴더에 남아 있는 원본 PDF는 유지

## [1.3.2] - 2026-07-26

### 변경

- 수집 검토 항목의 보관 동작을 `분석 큐로 보내기`로 명확히 표시하고, 버튼 또는 드래그 앤 드롭으로 보낸 항목은 즉시 분석 시작
- 분석 큐의 `즉시 요약으로 보내기`를 `선택 항목 바로 분석`으로 교체
- 사용자가 바로 분석을 요청한 여러 항목은 절전 대기 없이 한 편씩 연속 처리하고 마지막 항목까지 Ollama 런타임 유지
- 대기 중인 로컬 분석이 있으면 Ollama를 필요할 때 시작하고 작업 종료 후 기존 정책에 따라 정리
- AI 추천 모델 버튼에서 추천 모델이 선택된 모델 관리 창으로 바로 이어져 설치·검증·선택·삭제를 한 흐름에서 처리
- 라이브러리의 색인 편집 저장 버튼을 목록 아래에서 상세 편집 폼 바로 아래로 이동
- 라이브러리 검색창의 회색 안내문에 일반 키워드 검색과 자연어 질문 검색을 함께 명시
- 로컬 설치파일 빌드 완료 문구가 `installer.iss`의 현재 버전을 따라가도록 수정

## [1.3.1] - 2026-07-26

### 변경

- 앱 내부 업데이터는 릴리스 버전과 정확히 일치하는 번호 포함 설치파일만 다운로드
- 업데이트 창에 다운로드할 설치파일의 전체 이름과 용량을 함께 표시
- 정확한 버전의 설치파일이 없을 때 다른 버전 파일로 대체하지 않고 등록 대기로 표시

## [1.3.0] - 2026-07-26

### 추가

- 자연어 질문을 영어 검색어와 연도·분야 조건으로 바꿔 로컬 전문 색인에서 후보 논문을 찾는 RAG-lite 검색
- 최대 5편의 실제 PaperPack 페이지 본문만 근거로 한국어 답변을 만들고 논문·PDF 페이지를 함께 표시
- 자연어 검색 결과를 두 번 누르면 라이브러리의 해당 논문과 AI 분석 내용으로 바로 이동하는 기능
- `Ctrl+Shift+F`로 자연어 논문 찾기를 여는 단축키

### 변경

- 라이브러리 검색창이 짧은 제목·저자·DOI·기술명은 SQLite로 즉시 검색하고, 질문·비교·설명형 입력은 AI 검색으로 자동 전환
- 로컬 AI와 전송 동의가 저장된 클라우드 AI는 후보 검색에서 근거 답변까지 자동으로 연속 실행
- 클라우드 AI는 질문 해석과 논문 본문 전송을 구분해 동의를 받고, 전송 전에 후보 수와 실제 글자 수를 표시
- AI가 검색 후보에 없는 논문이나 제공되지 않은 페이지를 인용하면 결과에서 제거
- 참고문헌은 후보 검색에는 활용하되 논문의 연구 결과를 설명하는 근거로 사용하지 않도록 검색 답변 프롬프트 보강
- 자연어 검색 중에는 백그라운드 분석을 잠시 멈추고, 앱이 시작한 Ollama는 검색 완료·실패·창 닫기 뒤 종료

## [1.2.0] - 2026-07-26

### 추가

- 라이브러리에서 선택한 논문 또는 전체 논문을 재요약 대기열에 넣는 기능
- AI가 논문별 검색용 메타태그를 최대 5개 생성하고 사용자 태그와 별도로 보관하는 기능
- 저장된 연구분야가 맞지 않을 때 새 연구분야를 추천하고, 사용자 승인 후 설정에 추가해 재분석하는 흐름

### 변경

- Qwen3 4B는 PC 사양과 논문 길이에 따라 16K~24K, Qwen3 8B는 최대 40K 컨텍스트를 사용
- 로컬 컨텍스트 크기에 맞춰 본문 전송 범위를 자동 조절하고 즉시요약 미리보기에 실제 컨텍스트를 표시
- 재요약은 기존 AI 결과를 새 결과로 교체하되 사용자 편집 제목·저자·분류·태그는 계속 보호
- 참고문헌은 검색용 PaperPack 본문에는 유지하고 AI 분석 입력에서만 제외

## [1.1.3] - 2026-07-26

### 추가

- 설정에서 AI 분류용 연구분야를 직접 추가·이름 수정·삭제하는 관리 화면

### 변경

- PaperPack의 전체 본문과 검색 색인은 보존하면서 AI 요약 입력에서는 참고문헌 절 이후를 제외
- 남은 참고문헌을 연구 결과나 저자 판정 근거로 사용하지 않도록 요약 프롬프트 보강
- 체크한 연구분야만 AI 분류 후보로 보내고, 미선택 시 편집한 전체 분야 목록 사용
- 자동 신버전 확인은 마지막 확인 후 24시간에 한 번만 실행하고 수동 확인은 제한 없이 허용

## [1.1.2] - 2026-07-26

### 변경

- AI 메뉴를 설정 메뉴 안으로 이동
- 검토 대기 PDF를 분석 큐로 끌어다 놓으면 확인창 없이 보관 후 분석 대기로 전환
- 검토 대기 항목은 분석 큐에 중복 표시하지 않고, 분석 완료 후 큐 기록을 자동 삭제
- 라이브러리 목록을 다중 선택으로 바꾸고 sPDF 열기·편집본 적용·폐기를 일괄 처리
- AI가 자동 추정 제목을 원문 언어 그대로 더 정확하게 교정하도록 프롬프트와 필드 우선순위 보강
- 사용자가 직접 수정한 제목은 AI가 덮어쓰지 않도록 기존 보호 규칙 유지
- `지금 분석`으로 선택한 항목은 절전 대기 없이 연속 처리하고 마지막 항목 뒤 Ollama 정리

## [1.1.1] - 2026-07-26

### 변경

- 수집·검토 대기 목록에서 여러 PDF를 선택해 일괄 열기·보관·제외
- 대기 목록에 전체 선택 버튼을 추가하고 다중 보관 시 각 PDF의 추정 메타데이터 적용
- 보관 성공 대화상자를 없애고 상태 문구로만 조용히 알림
- 일괄 작업은 실패나 주의사항이 있을 때만 대화상자를 표시

## [1.1.0] - 2026-07-26

### 추가

- 분석 큐 실패 사유 표시, 전체 선택, 실패 항목 재분석
- 라이브러리 목록의 분석 대기·진행·완료·실패 상태
- 새 PDF를 복구 가능한 제외 목록으로 보내고 파일 ID를 보관하는 제외 흐름
- 폴더 및 감시 설정 복원과 라이브러리 변경 시 PaperPack·DB·상태 파일 이동
- 도움말, 제작자·기여자 정보와 기본 단축키

### 변경

- 저사양 PC를 위해 AI 분석은 항상 한 건씩 실행
- 즉시 요약 중에는 백그라운드 분석이 겹치지 않도록 상호 배제
- 즉시 요약 PDF 선택 시 전송 미리보기를 자동 준비하고 실행 버튼 활성화
- 라이브러리의 판본 열과 중복되는 한국어 설명 편집란 제거
- 요약 요청의 Ollama thinking 및 OpenAI reasoning 비활성화
- 앱이 직접 시작한 Ollama 서버는 AI 작업 후 종료
- WindowsApps 실행 별칭에 접근할 수 없는 경우에도 Ollama 탐색을 계속 처리
- Ollama 자동 설치의 경과 시간과 Delivery Optimization 사용 안내를 표시하고,
  3분 타임아웃 후 공식 다운로드 버튼 제공
- 사용하지 않는 대화상자 제목 표시줄의 `?` 도움말 버튼 제거
- 일반 논문 후보가 아닌 1~2쪽 PDF는 새 PDF 감시·분석 큐에서 제외
- 설치 후 첫 실행에서 AI 제공자 설정과 Ollama 런타임·모델 준비를 순서대로 안내
- Ollama 모델 설치·검증 직후 활성 모델로 자동 선택·저장하고 삭제 후 목록을 즉시 갱신
- 여러 감시 폴더를 설정에서 추가·제거하고 한 번의 스캔으로 함께 처리
- 특허 문서 표식을 판별해 논문과 함께 자동 수집·검색
- Ollama 모델 다운로드에 전송량, MB/s 속도와 예상 남은 시간 표시
- 분석 큐의 완료 항목을 더블클릭하면 라이브러리의 해당 분석 결과로 이동
- 백그라운드 분석 완료 즉시 라이브러리 캐시와 화면을 새로고침
- AI 분석 시 기존 제목·저자·저널명을 번역하지 않고 추출된 원문 서지정보 보존
- 분석 큐에서 여러 검토 대기 항목을 선택해 한 번에 제외
- 분석 큐의 우선순위·즉시요약·지금 분석·재분석 작업을 다중 선택에 맞게 일괄 처리
- 완료된 큐 항목으로 이동할 때 같은 라이브러리 행이어도 AI 분석 상세를 즉시 다시 표시
- 리뷰·체계적 문헌고찰·메타분석도 첫 페이지 byline과 AI 분석에서 원문 저자를 추출
- 라이브러리 검색 결과의 첫 논문을 자동 선택해 AI 분석 상세를 즉시 표시
- 라이브러리 검색창에 한 번에 지우는 X 버튼을 제공하고 앱 제목 표시줄에 현재 버전 표시
- 검색 중 큐의 완료 논문을 열면 검색어를 초기화하고 라이브러리에서 정확히 선택
- 텍스트가 없는 특허·논문도 즉시요약과 분석 준비 중 내장 RapidOCR을 자동 실행
- 새 PDF 검색 OCR은 앞 5페이지만 한 장씩 저우선순위로 처리하고 결과를 디스크에 캐시
- PaperPack 보관 후 전체 OCR을 백그라운드에서 한 장씩 완료하며 페이지 진행률 표시
- 사용자 요청 OCR은 일반 우선순위로 빠르게 실행하고 자동 OCR은 CPU 스레드와 우선순위 제한
- 백그라운드 OCR의 ONNX·OpenCV 스레드를 1개로 직접 고정하고 렌더링 배율을 3배로 제한
- 새 PDF 검색 결과를 두 번 누르면 큐 이동 전에 원본을 sPDF로 열어 확인
- 라이브러리를 논문과 특허 유형별 폼으로 표시하고 특허에 잘못된 저널 값이 들어가지 않도록 차단
- 2페이지 미만 문서는 OCR에서 제외하고 앱 종료 시 실행 중인 OCR 자식 프로세스 정리
- 제외 파일 복원을 파일·판정·중복·추정 제목이 보이는 넓은 다중 선택 표로 개선
- GitHub Releases에서 새 버전을 확인하고 다운로드 속도·SHA-256 검증 후 설치 실행
- 버전 태그를 올리면 GitHub Actions가 Windows 설치파일을 빌드·릴리스하도록 자동화

## [1.0.0] - 2026-07-26

### 추가

- 학술 논문 PDF를 판별해 승인 없이 자동 보관하는 파이프라인
- 학과·세부 전공 기반 분류 체계와 정규식 1차 분류기
- AI 분석을 통한 분류 및 제목·저자·연도·저널 정보 보정
- `.paperpack` 페이지별 본문 저장과 SQLite FTS5 전문 검색
- Ollama 런타임 설치·실행 온보딩 및 `winget` 기본 경로 탐색
- 전용 애플리케이션 아이콘과 PDF 환원 대화상자
- 분석 진행 상태 표시와 분석 큐 정렬

### 변경

- 수집·분석 화면과 라이브러리 화면을 좌우 2단 구조로 재편
- 즉시 요약, 레거시 변환, 폴더 설정 기능을 전용 메뉴·대화상자로 이동
- AI 요약 스키마를 `paper-summary-v2`로 확장
- 패키지 버전을 1.0.0으로 갱신

### 제거

- OneDrive JSON 미러 및 클라우드 메타데이터 동기화 기능
