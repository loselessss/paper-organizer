# 변경 기록

이 프로젝트의 주요 변경 사항을 버전별로 기록합니다.

## [1.3.2] - 2026-07-26

### 변경

- 수집 검토 항목의 보관 동작을 `분석 큐로 보내기`로 명확히 표시하고, 버튼 또는 드래그 앤 드롭으로 보낸 항목은 즉시 분석 시작
- 분석 큐의 `즉시 요약으로 보내기`를 `선택 항목 바로 분석`으로 교체
- 사용자가 바로 분석을 요청한 여러 항목은 절전 대기 없이 한 편씩 연속 처리하고 마지막 항목까지 Ollama 런타임 유지
- 대기 중인 로컬 분석이 있으면 Ollama를 필요할 때 시작하고 작업 종료 후 기존 정책에 따라 정리
- AI 추천 모델 버튼에서 추천 모델이 선택된 모델 관리 창으로 바로 이어져 설치·검증·선택·삭제를 한 흐름에서 처리
- 라이브러리의 색인 편집 저장 버튼을 목록 아래에서 상세 편집 폼 바로 아래로 이동
- 라이브러리 검색창의 회색 안내문에 일반 키워드 검색과 자연어 질문 검색을 함께 명시
- 로컬 설치파일 빌드 완료 문구가 `installer.iss`의 현재 버전을 따라가도록 수정

## [1.3.1] - 2026-07-26

### 변경

- 앱 내부 업데이터는 릴리스 버전과 정확히 일치하는 번호 포함 설치파일만 다운로드
- 업데이트 창에 다운로드할 설치파일의 전체 이름과 용량을 함께 표시
- 정확한 버전의 설치파일이 없을 때 다른 버전 파일로 대체하지 않고 등록 대기로 표시

## [1.3.0] - 2026-07-26

### 추가

- 자연어 질문을 영어 검색어와 연도·분야 조건으로 바꿔 로컬 전문 색인에서 후보 논문을 찾는 RAG-lite 검색
- 최대 5편의 실제 PaperPack 페이지 본문만 근거로 한국어 답변을 만들고 논문·PDF 페이지를 함께 표시
- 자연어 검색 결과를 두 번 누르면 라이브러리의 해당 논문과 AI 분석 내용으로 바로 이동하는 기능
- `Ctrl+Shift+F`로 자연어 논문 찾기를 여는 단축키

### 변경

- 라이브러리 검색창이 짧은 제목·저자·DOI·기술명은 SQLite로 즉시 검색하고, 질문·비교·설명형 입력은 AI 검색으로 자동 전환
- 로컬 AI와 전송 동의가 저장된 클라우드 AI는 후보 검색에서 근거 답변까지 자동으로 연속 실행
- 클라우드 AI는 질문 해석과 논문 본문 전송을 구분해 동의를 받고, 전송 전에 후보 수와 실제 글자 수를 표시
- AI가 검색 후보에 없는 논문이나 제공되지 않은 페이지를 인용하면 결과에서 제거
- 참고문헌은 후보 검색에는 활용하되 논문의 연구 결과를 설명하는 근거로 사용하지 않도록 검색 답변 프롬프트 보강
- 자연어 검색 중에는 백그라운드 분석을 잠시 멈추고, 앱이 시작한 Ollama는 검색 완료·실패·창 닫기 뒤 종료

## [1.2.0] - 2026-07-26

### 추가

- 라이브러리에서 선택한 논문 또는 전체 논문을 재요약 대기열에 넣는 기능
- AI가 논문별 검색용 메타태그를 최대 5개 생성하고 사용자 태그와 별도로 보관하는 기능
- 저장된 연구분야가 맞지 않을 때 새 연구분야를 추천하고, 사용자 승인 후 설정에 추가해 재분석하는 흐름

### 변경

- Qwen3 4B는 PC 사양과 논문 길이에 따라 16K~24K, Qwen3 8B는 최대 40K 컨텍스트를 사용
- 로컬 컨텍스트 크기에 맞춰 본문 전송 범위를 자동 조절하고 즉시요약 미리보기에 실제 컨텍스트를 표시
- 재요약은 기존 AI 결과를 새 결과로 교체하되 사용자 편집 제목·저자·분류·태그는 계속 보호
- 참고문헌은 검색용 PaperPack 본문에는 유지하고 AI 분석 입력에서만 제외

## [1.1.3] - 2026-07-26

### 추가

- 설정에서 AI 분류용 연구분야를 직접 추가·이름 수정·삭제하는 관리 화면

### 변경

- PaperPack의 전체 본문과 검색 색인은 보존하면서 AI 요약 입력에서는 참고문헌 절 이후를 제외
- 남은 참고문헌을 연구 결과나 저자 판정 근거로 사용하지 않도록 요약 프롬프트 보강
- 체크한 연구분야만 AI 분류 후보로 보내고, 미선택 시 편집한 전체 분야 목록 사용
- 자동 신버전 확인은 마지막 확인 후 24시간에 한 번만 실행하고 수동 확인은 제한 없이 허용

## [1.1.2] - 2026-07-26

### 변경

- AI 메뉴를 설정 메뉴 안으로 이동
- 검토 대기 PDF를 분석 큐로 끌어다 놓으면 확인창 없이 보관 후 분석 대기로 전환
- 검토 대기 항목은 분석 큐에 중복 표시하지 않고, 분석 완료 후 큐 기록을 자동 삭제
- 라이브러리 목록을 다중 선택으로 바꾸고 sPDF 열기·편집본 적용·폐기를 일괄 처리
- AI가 자동 추정 제목을 원문 언어 그대로 더 정확하게 교정하도록 프롬프트와 필드 우선순위 보강
- 사용자가 직접 수정한 제목은 AI가 덮어쓰지 않도록 기존 보호 규칙 유지
- `지금 분석`으로 선택한 항목은 절전 대기 없이 연속 처리하고 마지막 항목 뒤 Ollama 정리

## [1.1.1] - 2026-07-26

### 변경

- 수집·검토 대기 목록에서 여러 PDF를 선택해 일괄 열기·보관·제외
- 대기 목록에 전체 선택 버튼을 추가하고 다중 보관 시 각 PDF의 추정 메타데이터 적용
- 보관 성공 대화상자를 없애고 상태 문구로만 조용히 알림
- 일괄 작업은 실패나 주의사항이 있을 때만 대화상자를 표시

## [1.1.0] - 2026-07-26

### 추가

- 분석 큐 실패 사유 표시, 전체 선택, 실패 항목 재분석
- 라이브러리 목록의 분석 대기·진행·완료·실패 상태
- 새 PDF를 복구 가능한 제외 목록으로 보내고 파일 ID를 보관하는 제외 흐름
- 폴더 및 감시 설정 복원과 라이브러리 변경 시 PaperPack·DB·상태 파일 이동
- 도움말, 제작자·기여자 정보와 기본 단축키

### 변경

- 저사양 PC를 위해 AI 분석은 항상 한 건씩 실행
- 즉시 요약 중에는 백그라운드 분석이 겹치지 않도록 상호 배제
- 즉시 요약 PDF 선택 시 전송 미리보기를 자동 준비하고 실행 버튼 활성화
- 라이브러리의 판본 열과 중복되는 한국어 설명 편집란 제거
- 요약 요청의 Ollama thinking 및 OpenAI reasoning 비활성화
- 앱이 직접 시작한 Ollama 서버는 AI 작업 후 종료
- WindowsApps 실행 별칭에 접근할 수 없는 경우에도 Ollama 탐색을 계속 처리
- Ollama 자동 설치의 경과 시간과 Delivery Optimization 사용 안내를 표시하고,
  3분 타임아웃 후 공식 다운로드 버튼 제공
- 사용하지 않는 대화상자 제목 표시줄의 `?` 도움말 버튼 제거
- 일반 논문 후보가 아닌 1~2쪽 PDF는 새 PDF 감시·분석 큐에서 제외
- 설치 후 첫 실행에서 AI 제공자 설정과 Ollama 런타임·모델 준비를 순서대로 안내
- Ollama 모델 설치·검증 직후 활성 모델로 자동 선택·저장하고 삭제 후 목록을 즉시 갱신
- 여러 감시 폴더를 설정에서 추가·제거하고 한 번의 스캔으로 함께 처리
- 특허 문서 표식을 판별해 논문과 함께 자동 수집·검색
- Ollama 모델 다운로드에 전송량, MB/s 속도와 예상 남은 시간 표시
- 분석 큐의 완료 항목을 더블클릭하면 라이브러리의 해당 분석 결과로 이동
- 백그라운드 분석 완료 즉시 라이브러리 캐시와 화면을 새로고침
- AI 분석 시 기존 제목·저자·저널명을 번역하지 않고 추출된 원문 서지정보 보존
- 분석 큐에서 여러 검토 대기 항목을 선택해 한 번에 제외
- 분석 큐의 우선순위·즉시요약·지금 분석·재분석 작업을 다중 선택에 맞게 일괄 처리
- 완료된 큐 항목으로 이동할 때 같은 라이브러리 행이어도 AI 분석 상세를 즉시 다시 표시
- 리뷰·체계적 문헌고찰·메타분석도 첫 페이지 byline과 AI 분석에서 원문 저자를 추출
- 라이브러리 검색 결과의 첫 논문을 자동 선택해 AI 분석 상세를 즉시 표시
- 라이브러리 검색창에 한 번에 지우는 X 버튼을 제공하고 앱 제목 표시줄에 현재 버전 표시
- 검색 중 큐의 완료 논문을 열면 검색어를 초기화하고 라이브러리에서 정확히 선택
- 텍스트가 없는 특허·논문도 즉시요약과 분석 준비 중 내장 RapidOCR을 자동 실행
- 새 PDF 검색 OCR은 앞 5페이지만 한 장씩 저우선순위로 처리하고 결과를 디스크에 캐시
- PaperPack 보관 후 전체 OCR을 백그라운드에서 한 장씩 완료하며 페이지 진행률 표시
- 사용자 요청 OCR은 일반 우선순위로 빠르게 실행하고 자동 OCR은 CPU 스레드와 우선순위 제한
- 백그라운드 OCR의 ONNX·OpenCV 스레드를 1개로 직접 고정하고 렌더링 배율을 3배로 제한
- 새 PDF 검색 결과를 두 번 누르면 큐 이동 전에 원본을 sPDF로 열어 확인
- 라이브러리를 논문과 특허 유형별 폼으로 표시하고 특허에 잘못된 저널 값이 들어가지 않도록 차단
- 2페이지 미만 문서는 OCR에서 제외하고 앱 종료 시 실행 중인 OCR 자식 프로세스 정리
- 제외 파일 복원을 파일·판정·중복·추정 제목이 보이는 넓은 다중 선택 표로 개선
- GitHub Releases에서 새 버전을 확인하고 다운로드 속도·SHA-256 검증 후 설치 실행
- 버전 태그를 올리면 GitHub Actions가 Windows 설치파일을 빌드·릴리스하도록 자동화

## [1.0.0] - 2026-07-26

### 추가

- 학술 논문 PDF를 판별해 승인 없이 자동 보관하는 파이프라인
- 학과·세부 전공 기반 분류 체계와 정규식 1차 분류기
- AI 분석을 통한 분류 및 제목·저자·연도·저널 정보 보정
- `.paperpack` 페이지별 본문 저장과 SQLite FTS5 전문 검색
- Ollama 런타임 설치·실행 온보딩 및 `winget` 기본 경로 탐색
- 전용 애플리케이션 아이콘과 PDF 환원 대화상자
- 분석 진행 상태 표시와 분석 큐 정렬

### 변경

- 수집·분석 화면과 라이브러리 화면을 좌우 2단 구조로 재편
- 즉시 요약, 레거시 변환, 폴더 설정 기능을 전용 메뉴·대화상자로 이동
- AI 요약 스키마를 `paper-summary-v2`로 확장
- 패키지 버전을 1.0.0으로 갱신

### 제거

- OneDrive JSON 미러 및 클라우드 메타데이터 동기화 기능
